# 배포본 안내문 보관

지난 배포본 폴더는 삭제했다 — `git archive <커밋>` 으로 언제든 다시 만들 수 있으므로
파일을 쌓아 둘 이유가 없다. 다만 **그 시점의 상태 기록**(무엇이 달랐는지·무엇이
미확정이었는지)은 저장소에 남길 값이 있어 안내문만 옮겨 둔다.

| 파일 | 시점 | 소스 커밋 |
|---|---|---|
| `배포-안내-20260804.md` | 2026-08-04 | `a0e3533` |

현행 배포본의 안내문은 배포 폴더 안에 있다(저장소 밖 — 배포 대상이 아니다).
