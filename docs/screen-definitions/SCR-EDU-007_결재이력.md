# SCR-EDU-007 결재 이력

> v6 · 구현 정합본 · 2026-08-11

## 1. 화면 정보

| 항목 | 내용 |
|---|---|
| 화면 ID | SCR-EDU-007 |
| 화면명 | 결재 이력 |
| URL 경로 | edu-approval.html |
| 상위 메뉴 | 안전보건교육 > 결재 이력 |
| 화면 유형 | 결재 문서 통합 조회 |

- 안전보건교육에서 상신된 온나라 결재 문서의 총괄·교육별·개인별 이력을 조회한다.
- 현재 신규 상신은 막혀 있으며 저장된 이력이 없으면 0건 상태를 표시한다.

## 2. 권한 정보

- 메뉴 접근·보기는 시스템 관리의 메뉴 관리(SCR-ADMIN-005)와 권한 관리(SCR-ADMIN-006)에서 설정한다.
- 이 화면 정의서에서는 역할별 권한표를 두지 않는다.
- 초기 메뉴 열람 범위는 재난안전과로 설정한다.

## 3. 진입 / 이탈 경로 및 사용자 시나리오

| 구분 | 연결 화면 | 조건 / 트리거 |
|---|---|---|
| 진입 | 결재 이력 | 메뉴 선택 |
| 이탈 | 결재 문서 보기 | 이력 행의 문서 선택 |
| 이탈 | 결재 상태 보기 | 이력 행의 상태 선택 |

| 단계 | 사용자 행동 | 화면 반응 |
|:--:|---|---|
| 1 | 요약 지표를 확인한다 | 전체 상신·결재중·결재완료·반려 건수 표시 |
| 2 | 검색어, 구분, 상태와 상신연도를 선택한다 | 조건에 맞는 이력만 표시 |
| 3 | 이력 행에서 문서 또는 상태를 연다 | 저장된 문서·상태 상세 표시 |

## 4. 화면 구성별 정리

### 4-1. 신규 상신 차단 안내
- 2026-07-30 회의에 따라 교육 화면의 결재 상신 버튼을 제거했고 공문 작성 단계가 마련될 때까지 신규 상신을 차단한다고 안내한다.
- 화면에는 신규 상신, 결재 완료, 반려 또는 재상신 버튼을 표시하지 않는다.

| 트리거 | 동작 결과 | 이동 / 메시지 |
|---|---|---|
| 화면 진입 | 왜 신규 상신이 막혀 있는지 안내를 상시 표시한다 | 기능을 지운 것이 아니라 순서를 바로잡는 중임을 밝힌다 |

### 4-2. 요약과 필터
- 전체 상신, 결재중, 결재완료, 반려 건수를 표시한다.
- 문서번호·문서명·대상 검색, 구분, 상태와 상신연도로 조회한다.

| 트리거 | 동작 결과 | 이동 / 메시지 |
|---|---|---|
| 검색어 입력 | 문서번호·문서명·대상을 함께 찾아 즉시 좁힌다 | — |
| 구분·상태·상신연도 선택 | 선택 즉시 목록과 요약 건수를 갱신한다 | — |

### 4-3. 이력 목록
- 문서번호, 상신일, 구분, 문서명, 대상, 결재선, 상태와 관리 액션을 표시한다.
- 저장된 이력이 있으면 문서 보기와 결재 상태 보기를 제공한다.

| 트리거 | 동작 결과 | 이동 / 메시지 |
|---|---|---|
| [문서 보기] | 상신한 문서를 그대로 다시 연다 | 조회 전용으로 연다 |
| [결재 상태] | 그 건의 결재 진행 상태를 연다 | 상태를 바꾸는 수단은 내지 않는다 |
| 재상신 건 | 이전 건을 덮지 않고 별도 이력으로 표시한다 | 최신이 아닌 건은 상태를 바꿀 수 없다 |

### 4-4. 빈 상태
- 실데이터가 없으면 모든 지표를 0으로 표시한다.
- 목록에는 아직 상신한 결재 문서가 없으며 각 교육 화면의 상신에서 문서를 올리면 이 화면에 쌓인다는 현재 안내문을 표시한다.

| 트리거 | 동작 결과 | 이동 / 메시지 |
|---|---|---|
| 이력이 하나도 없음 | 요약 지표를 0으로 두고 빈 상태 안내를 표시한다 | 무엇이 생기면 채워지는지 함께 밝힌다 |

## 5. 유효성 검사

| 입력 항목 | 필수 | 규칙 | 오류 안내 |
|---|---|---|---|
| 검색어 | 아니오 | 문서번호·문서명·대상 부분일치 | — |
| 구분·상태·연도 | 아니오 | 제공 선택값 사용 | — |
| 이력 식별값 | 행 조회 시 | 저장된 문서와 일치 | 문서 조회 실패 안내 |

## 6. 정책

- 메뉴명은 실제 화면과 동일하게 “결재 이력”으로 사용한다.
- 현재 화면에서 신규 상신은 불가능하며 기존 저장 이력을 조회하는 기능만 노출한다.
- 실데이터가 없는 상태를 정상 0건으로 처리한다.
- 문서번호·상태 등은 저장된 연계 결과를 표시하며 이 화면에서 수정하지 않는다.
- 공문 샘플은 아직 없고 현재 화면 필드나 문서 레이아웃을 추가로 확정하지 않는다.
- 빈 상태의 상신 안내문은 현재 구현 문구이며 신규 상신 차단 안내와 함께 노출된다.

## 7. 미결사항

없음.

### 확정 근거 기록
- 열람 범위: 재난안전과.
- 실데이터: 현재 없음.
- 공문 샘플: 추후 별도 제공.
