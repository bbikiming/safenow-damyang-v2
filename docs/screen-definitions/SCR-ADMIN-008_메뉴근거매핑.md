# 화면 정의서 — 메뉴 근거 매핑

> 화면 ID: SCR-ADMIN-008 · 화면 유형: 기타 (관리) · 기획 관점(v6)
> 판단이 필요했던 항목은 §6 정책에서 확정했습니다.

---

## 1. 화면 정보

| 항목 | 내용 |
|---|---|
| 화면 ID | SCR-ADMIN-008 |
| 화면명 | 메뉴 근거 매핑 |
| URL 경로 | admin-law-map.html · admin-law-map.html?page={화면 id} |
| 상위 메뉴 | 시스템 관리 > 기준정보 > 법령 관리 |
| 화면 유형 | 기타 (관리) |

**목적·설명** — 각 화면에 어떤 조문을 근거로 붙일지 지정하는 화면이다. 근거를 붙이려면 **검증 항목을 통과해야 저장되도록** 막는 것이 이 화면의 존재 이유인데(왜: 근거를 잘못 붙이면 시스템이 틀린 법조문을 주장하게 되고, 실제 점검에서 그런 결함이 반복해서 나왔다), 그래서 붙이기와 떼기를 비대칭으로 다룬다. 구성은 ① 판정 현황 요약 ② **대메뉴 › 중메뉴 트리**·상태 필터 ③ 반영 안 되는 매핑 ④ 근거 편집(판정·조문 목록·순서·역할) ⑤ 근거 추가 3단계(선택 → 원문 확인 → 검증) ⑥ **조문 선택 창**(검색·법령 계열 필터) ⑦ 미리보기 7영역이다. 핵심 연계는 조문을 확인하는 법령·조문(SCR-ADMIN-007)에서 넘어와 여기서 붙이고, 붙은 결과가 전 화면 상단 근거 표시(SCR-COM-008)로 나타나는 흐름이다.

---

## 2. 권한 정보

- 메뉴 접근·보기·수정은 시스템 관리의 메뉴 관리(SCR-ADMIN-005)와 권한 관리(SCR-ADMIN-006)에서 설정한다.
- 이 화면 정의서에서는 역할별 권한과 열람 범위를 별도로 정하지 않는다.
- 이 화면은 시스템 관리 메뉴군에 속한다. 근거 지정·해제와 검증 기록은 §3~§6의 업무 흐름과 상태 전이 조건을 따른다.

---

## 3. 진입 / 이탈 경로 및 사용자 시나리오

**진입 / 이탈**

| 구분 | 연결 화면 | 조건 / 트리거 |
|---|---|---|
| 진입 | 시스템 관리 좌측 메뉴 | [메뉴 근거 매핑] 선택 |
| 진입(딥링크) | 법령·조문(SCR-ADMIN-007) | 재검증 대상에서 이동 — 해당 화면이 선택된 상태로 열림 |
| 이탈 | 법령·조문 | 상단 [법령·조문] 링크 |
| 이탈 차단 | 현재 화면 유지 | 저장하지 않은 변경이 있으면 다른 화면으로 이동 시 확인을 받는다 |

**사용자 시나리오**

| 단계 | 사용자 행동 | 화면 반응 |
|:--:|---|---|
| 1 | 화면 진입 | 상단에 판정 현황 4분할(근거 있음·근거 없음(확정)·미판단·반영 안 됨)과 **진척률을 표시하지 않는 이유** 안내 |
| 2 | 좌측에서 화면 선택 | 우측에 그 화면의 적용 범위·판정·근거 목록 표시 |
| 3 | 적용 범위 확인 | 이 근거가 적용되는 화면 파일 목록. 여러 화면이 공유하면 그 사실을 강조 |
| 4 | 판정을 근거 있음/없음 중 선택 | 선택에 따라 아래 입력 영역이 바뀐다 |
| 5 | [+ 근거 추가] | 패널 안에서 3단계 진행 — 조문 선택 → **원문 확인** → 검증 항목 |
| 6 | 조문 선택 | 조문 원문 전체가 표시된다. 번호만 보고 붙이지 못하게 하는 단계 |
| 7 | 검증 항목 응답 + 판단 근거 입력 | 전부 만족하기 전까지 [근거 추가]가 비활성 |
| 8 | 근거 추가 완료 | 목록에 추가되고 검증 기록이 남는다 |
| 9 | 순서 조정·역할 지정 | 순서가 화면 표시 순서. 역할은 의무 근거 / 주기 근거 |
| 10 | 미리보기 확인 후 [저장] | 저장 후 적용 대상 화면 목록과 "새로 열 때 반영" 안내 |
| 11 | (근거 없음 선택 시) 사유 입력 후 [저장] | 사유 없이 저장하면 안내가 나오고 저장되지 않는다 |

**분기 로직**

- **판정 선택이 입력 영역을 바꾼다** — 근거 있음이면 조문 목록과 추가 버튼이, 근거 없음이면 사유 입력이 나타난다. **판정을 바꾸면 직전 판정에 딸린 값은 비워진다** — 안 비우면 근거 없음 사유가 근거 있음 매핑에 붙어 저장된다.
- **붙이기와 떼기가 비대칭이다** — 떼기는 과잉 주장을 줄이는 방향이라 즉시 가능하고, 붙이기는 결함이 나온 방향이라 검증 단계를 통과해야 한다.
- **반영 안 되는 매핑은 편집 대상이 아니다** — 어떤 화면도 그 화면 식별자를 쓰지 않는 경우이며, 화면에서 고칠 수 없어 별도 구역에 잠금 상태로 표시한다.

**엣지케이스**

- **한 근거가 여러 화면에 적용된다** — 목록 화면과 상세 화면이 같은 식별자를 공유하는 경우가 실재한다. 저장 전에 적용 대상을 반드시 보여준다. 목록과 상세는 하는 일이 다르므로 판단 시 유의가 필요하다.
- **근거를 지정해도 표시되지 않는 화면** — 화면에 제목 영역이 없으면 근거 표시가 나타나지 않는다. 이 사실을 경고로 밝히지 않으면 관리 화면이 거짓을 말하게 된다.
- **중복 추가** — 이미 있는 조문을 다시 추가하려 하면 안내하고 추가하지 않는다.
- **저장 없이 이동** — 변경 중 다른 화면을 고르면 확인을 받는다.
- **빈 저장** — 근거 있음인데 조문이 없거나, 근거 없음인데 사유가 없으면 저장되지 않는다.
- **검색·필터 결과 0건** — 조건에 맞는 화면이 없음을 알린다.

---

## 4. 화면 구성별 정리

### 4-1. 상단 — 판정 현황

- 구성: 근거 있음 / 근거 없음(확정) / 미판단 / 반영 안 됨 4분할 건수 + **진척률을 두지 않는 이유** 안내 + [법령·조문] 링크.

| 트리거 | 동작 결과 | 이동 / 메시지 |
|---|---|---|
| [법령·조문] | 조문 대장 화면으로 이동 | admin-law.html |

- 상태: 4분할 건수는 항상 표시된다. **완료율·진척률 지표는 만들지 않는다** — 근거는 채울수록 좋은 것이 아니라 맞아야 하는 것이고, 과잉으로 붙여서 생긴 결함이 실제로 있었다.

### 4-2. 좌측 — 관리 대상 화면 트리

- 구성: 검색 입력 + 상태 필터 + **대메뉴 › (구분) › 중메뉴 트리**. 화면 식별자가 아니라 **담당자가 실제로 보는 메뉴명**으로 표시한다.
- **메뉴명·구조·순서는 좌측 내비게이션과 같은 출처를 쓴다.** 이 화면이 메뉴명을 따로 갖고 있으면 메뉴가 개편될 때 여기만 옛 이름으로 남는다.
- **순서는 내비게이션 정의 순서를 그대로 따른다.** 담당자가 아는 위치에서 메뉴를 찾아야 하므로 임의로 재정렬하지 않는다.
- **구분(3뎁스)이 필요한 이유** — 안전보건교육은 '정기교육'이 현업근로자·관리감독자 양쪽에 있어, 구분 없이는 두 화면을 이름만으로 구별할 수 없다.
- 대메뉴 머리에 그 그룹의 **판정 분포**(근거 있음·근거 없음·미판단 건수)를 배지로 표시한다. 진척률이 아니라 분포다.
- **메뉴에서 뺀 화면은 목록에 남기되 「메뉴 제외」로 밝힌다(2026-09-03).** 그 화면들은 주소로 열리므로 근거 매핑이 계속 유효하고, 빼면 «관리 화면이 관리하지 못하는 근거»가 생긴다. 다만 표시하지 않으면 검토자가 그 화면을 메뉴에서 찾는다 — 메뉴 관리(SCR-ADMIN-005 §4-2)와 같은 표기를 쓴다.

| 트리거 | 동작 결과 | 이동 / 메시지 |
|---|---|---|
| 대메뉴 선택 | 하위 접기/펼치기 | 최초 진입은 전부 펼침 — 전체 구조를 한 번 보고 시작한다 |
| 검색어 입력 | **메뉴명(한글)·구분·식별자·파일명**을 함께 찾는다. 검색 중에는 접힌 그룹도 펼쳐 결과를 감추지 않는다 | 결과 없으면 검색어와 초기화 수단 표시 |
| 상태 필터 선택 | 해당 상태만 표시. 항목이 남지 않은 구분 헤더는 함께 사라진다 | — |
| 중메뉴 선택 | 우측을 그 화면의 편집 패널로 전환. 저장 안 된 변경이 있으면 확인 | — |

- 상태: **근거 있음 / 근거 없음(확정) / 미판단** 3종을 배지로 구분하고, 근거 표시가 불가한 화면은 별도 표기를 덧붙인다. 내비게이션에 없지만 파일이 살아 있는 화면은 **메뉴 미등록** 구역으로 따로 모은다 — 트리에서 빼면 관리 화면이 관리하지 못하는 근거가 생긴다.

### 4-3. 좌측 — 반영 안 되는 매핑

- 구성: 매핑은 있으나 어떤 화면도 쓰지 않는 식별자 목록. **편집 잠금**.

| 트리거 | 동작 결과 | 이동 / 메시지 |
|---|---|---|
| 행 표시 | 선택 불가. 사유 안내 | "어떤 화면도 이 식별자를 쓰지 않습니다 — 개발 수정 필요" |

- 상태: **없음**(구역 자체가 나타나지 않음) / **있음**(건수와 목록).

### 4-4. 우측 — 화면별 근거 검토

- 구성: **메뉴명 제목**(대메뉴 › 구분 중메뉴) · 화면 식별자 · 적용 화면 목록 · 표시 불가 경고(해당 시) · 근거 조문 목록(조문·역할·검토 논거) 또는 근거 없음 사유 · 미리보기 · 재검토 요청 · 검증 기록 건수.
- **편집 컨트롤이 없다.** 근거 조문의 추가·제거·순서·역할을 이 화면에서 바꾸지 않는다 — 매핑은 조문과 마찬가지로 **법령 데이터에서 생성되는 값**이고, 화면에서 고치면 재생성할 때 조용히 되돌아간다. 이 화면은 "무엇이 근거로 붙어 있고 왜 그렇게 판정했는가"를 확인하는 **검토 대장**이다.

| 트리거 | 동작 결과 | 이동 / 메시지 |
|---|---|---|
| 화면 선택 | 그 화면의 근거 조문·판정 사유·검토 논거를 표시 | 아무것도 고르지 않았으면 안내 표시 |
| [원문 보기] | 조문 대장 탭으로 이동해 그 조문을 연다 | 같은 화면 안의 탭 전환이라 맥락을 잃지 않는다 |
| 적용 화면이 둘 이상 | 목록과 함께 "N개 화면이 이 근거를 공유합니다"를 표시 | 하나를 고치면 여러 화면이 바뀐다는 사실을 감추지 않는다 |
| 근거 표시가 불가한 화면 | 경고를 덧붙임 | "근거가 있어도 칩이 표시되지 않습니다 — 개발 수정 필요" |
| 근거 없음으로 판정된 화면 | 사유를 표시 | 사유가 없으면 "판정 사유 미기록" |

- 상태: **근거 있음 / 근거 없음(확정) / 미판단 / 재검토 요청 중** 네 가지다. 역할 배지(**의무 근거 / 주기·기준 근거**)는 지정값이 아니라 **조문 성격에서 파생**한다 — 편집값으로 두면 주기를 정하는 조문이 의무 근거로 잘못 표시되는 일이 실제로 생겼다.

### 4-5. 우측 — 매핑 재검토 요청

- 구성: 이 화면에서 할 수 있는 **유일한 조작**. 사유를 받아 기록만 남기고 매핑 자체는 바꾸지 않는다(비파괴).

| 트리거 | 동작 결과 | 이동 / 메시지 |
|---|---|---|
| [매핑 재검토 요청] | 사유 입력 단계를 연다 | "틀렸다고 판단되면 사유를 남기세요 — 정비 큐와 변경 이력에 올라갑니다" |
| 사유 없이 등록 시도 | 저장하지 않음 | 무엇이 왜 틀렸는지 없는 요청은 처리할 수 없다 |
| 요청 등록 | 화면 상태가 "재검토 요청 중"으로 바뀌고 정비 큐 최상단 계열에 오름 | "재검토 요청을 남겼습니다 — 정비 큐와 변경 이력에서 확인할 수 있습니다" |
| [요청 철회] | 요청을 거두고 원래 판정 상태로 돌아감 | "재검토 요청을 철회했습니다" |

- 상태: **요청 없음 / 요청 중**. 요청 중에는 사유·시각·요청자가 함께 표시된다. 요청이 반영되는 경로는 이 화면이 아니라 **법령 데이터 재생성**이며, 그 사실을 패널 하단에 상시 명시한다 — 요청을 남기면 화면이 바뀔 것으로 오해하면 안 된다.

### 4-6. 우측 — 미리보기

- 구성: 지정된 근거가 대상 화면 상단에 어떻게 보이는지 그대로 표시.

| 트리거 | 동작 결과 | 이동 / 메시지 |
|---|---|---|
| 근거 있음이고 조문이 1건 이상 | 실제 표시 모양을 그대로 렌더 | — |
| 근거 없음이거나 조문 0건 | 표시하지 않음 | — |

- 상태: **표시 / 미표시**. 관리 화면이 "지정돼 있다"고만 말하고 실제로 어떻게 보이는지 알 수 없으면, 표시가 깨져도 아무도 모른다.

---

## 5. 유효성 검사

| 입력 항목 | 필수 | 형식/규칙 | 오류 안내 |
|---|---|---|---|
| 판정 | 필수 | 근거 있음 / 근거 없음(확정) 중 하나 | 미선택 상태로 저장 불가 |
| 근거 조문 | 근거 있음일 때 1건 이상 | 제공 목록에서 선택. 직접 입력 불가 | "근거 조문이 없습니다. 근거 없음으로 확정하거나 조문을 추가하세요" |
| 검증 항목 | 근거 추가 시 전 항목 필수 | 모두 확인해야 진행 | 미충족 시 버튼 비활성 + 사유 표시 |
| 판단 근거 | 근거 추가 시 필수 | 빈 값 불가 | 동상 |
| 근거 없음 사유 | 근거 없음일 때 필수 | 빈 값 불가 | "근거 없음 사유를 적어주세요" |
| 조문 중복 | — | 같은 조문 중복 추가 불가 | "이미 추가된 조문입니다" |

- **조문 직접 입력을 막는 것이 기획 의도다.** 자유 문자열을 허용하면 같은 조문이 화면마다 다르게 적히고, 조문이 개정돼도 아무도 알아채지 못한다.
- **검증을 통과 못 하면 저장 자체가 안 된다.** 경고 후 진행 허용이 아니라 하드 블록이다 — 경고만으로는 같은 결함이 반복된다는 것이 확인되었다.

---

## 6. 정책

- **매핑도 조문과 같은 생성물이다** — 이 화면에서 근거를 붙이거나 떼지 않는다. 매핑은 검증 6문을 통과한 판정의 결과이고, 그 판정은 법령 데이터와 함께 재생성되는 값이다. 화면에서 고칠 수 있게 두면 재생성 때 조용히 되돌아가 "고쳤는데 원래대로"가 반복된다. 이 화면의 역할은 **확인과 이의 제기**이며, 변경은 법령 데이터 재생성이라는 한 경로로만 들어온다.
- **판단 주체와 화면 위치가 다르다** — 이 화면은 시스템 관리에 놓여 있지만 "이 조문이 이 화면의 근거가 맞는가"를 판정하는 것은 **재난안전과의 업무**다(LWM-01 확정). 그래서 조회·재검토 요청은 이 화면에서 하고, **판정 확정은 화면 조작이 아니라 법령 데이터 재생성**으로 이루어진다 — 기능을 조회·요청·확정 셋으로 나누되 확정만 화면 밖에 둔 것이다. 조문 승인 주체와 같다(SCR-ADMIN-007 §6).
- **역할 배지는 편집값이 아니라 파생값이다(LWM-05 관련)** — 의무 근거인지 주기·기준 근거인지는 조문의 성격에서 나온다. 편집값으로 두면 주기를 정하는 조문이 의무 근거로 잘못 표시되는 일이 실제로 생겼다.
- **이의는 비파괴로만 받는다** — 매핑이 틀렸다고 판단되면 지우는 것이 아니라 **재검토 요청으로 기록을 남긴다**. 사유 없는 요청은 저장되지 않는다 — 무엇이 왜 틀렸는지 없는 요청은 처리할 수 없다. 요청은 정비 큐와 변경 이력에 올라가 묻히지 않는다.
- **판단 근거를 데이터로 남긴다** — 누가 언제 무엇을 보고 그렇게 판단했는지가 기록되지 않으면, 나중에 그 근거가 맞는지 다시 판정할 수 없다. 검증 기록에는 판단 당시의 조문 제목을 함께 남겨 조문 변경 시 재검증 대상으로 잡는다.
- **근거 없음도 판정이다** — 법령이 정한 의무가 아닌 화면(시스템 운영 기능)은 근거를 비우는 것이 정상이며, **사유를 남겨야 다음 담당자가 같은 검토를 반복하지 않는다**(SCR-COM-008 연계).
- **진척률을 만들지 않는다** — 완료율 지표는 담당자를 "채우는" 방향으로 민다. 근거는 많이 붙일수록 좋은 것이 아니라 맞아야 하는 것이고, 과잉 부착으로 생긴 결함이 실제로 있었다. 4분할 건수만 표시하고 행동 대상은 미판단뿐이다.
- **적용 범위를 저장 전에 보여준다** — 한 근거가 여러 화면에 동시에 적용되는 경우가 실재하므로, 저장 버튼을 누르기 전에 대상을 확인시킨다.
- **표시되지 않는 것을 숨기지 않는다** — 화면 구조상 근거 표시가 불가한 경우 그 사실을 경고한다. 관리 화면이 "지정했다"고만 말하고 실제로 안 보이면 그것이 곧 거짓말이다.
- **근거 변경은 다시 열 때 반영된다(LWM-08 확정)** — 이미 열려 있는 화면의 근거 표시를 즉시 갱신하지 않는다. 근거는 화면 진입 시 한 번 주입되는 값이라 실시간 반영을 만들려면 모든 화면이 변경을 구독해야 하는데, 그 복잡도에 비해 얻는 것이 거의 없다 — 근거는 하루에 몇 번 바뀌는 값이 아니다. 대신 **미리보기가 그 공백을 메우고**, 안내에 "다시 열 때 반영된다"를 명시한다.
- **표기 분류는 재난안전과가 확정하고 초안은 이 시스템이 낸다(LWM-04 확정)** — 화면에 흩어져 있던 근거 표기를 조문 키로 흡수하는 작업에서, 어떤 표기가 어느 조문에 대응하는지 초안은 코드가 낼 수 있지만 **맞는지 판단은 업무 지식이 필요**하다. 초안을 만들어 검토받는 순서로 하고, 확정 전 표기는 "미연계"로 드러낸다.
- **검증 항목이 바뀌면 기존 기록을 재검증 대상으로 올린다(LWM-06 확정)** — 검증 항목은 결함이 나올 때마다 늘어 왔다. 항목이 바뀌면 이전 기록은 **지우지 않고 그대로 두되 "이전 기준으로 검증됨"으로 표시**하고 재검증 대상 목록에 올린다. 지우면 그때까지 쌓인 판단 근거가 통째로 사라지고, 그대로 두면 새 항목을 통과하지 않은 매핑이 통과한 것처럼 보인다.
- **반영되지 않는 매핑은 개발 수정 대상으로 남긴다(LWM-07 확정)** — 매핑은 있는데 어떤 화면도 쓰지 않는 식별자는 화면에서 고칠 수 없다. 목록에 편집 잠금 상태로 두고 사유를 밝히며, 정리는 **법령 데이터 재생성 때 함께** 한다 — 별도 정리 작업을 만들면 그때까지 목록에 남아 담당자를 헷갈리게 한다.
- **상세 화면은 목록의 근거를 공유한다(LWM-03 확정)** — 상세가 목록과 같은 식별자를 쓰는 것은 같은 업무의 다른 단계이기 때문이고, 대개 근거도 같다. **상세에만 다른 근거가 필요하면 그것은 별개의 화면**이라는 뜻이므로, 근거를 나누는 대신 **식별자를 나눈다**. 한 식별자에 화면별 예외를 두기 시작하면 "이 화면의 근거"가 무엇인지 한 곳에서 답할 수 없게 된다.
- **검증 기록은 일반 이력과 같은 수준으로 보호한다(LWM-02 확정)** — 고칠 수 없고 지울 수 없으며, 누가 언제 무엇을 보고 판단했는지가 함께 남는다. 별도의 보호 장치(서명·봉인)를 더하지 않는다 — 이 기록은 법적 증빙이 아니라 **다음 담당자가 같은 검토를 반복하지 않게 하는 업무 기록**이고, 더 무겁게 만들면 기록 자체를 남기지 않게 된다.

---

## 7. 미결사항

없음. 판정 확정 주체와 기능 분리·검증 기록 보호 수준·상세 화면의 고유 근거·표기 분류 주체·대표 근거 지정·검증 항목 개정 시 처리·반영 안 되는 매핑의 정리·저장 후 반영 시점은 모두 §6에서 확정했다.

대표 근거를 따로 지정하지 않는 이유는 §6의 역할 배지 정책과 같다 — 순서는 표시 순서일 뿐이고 어느 조문이 더 중요한가는 화면이 정할 문제가 아니다.
