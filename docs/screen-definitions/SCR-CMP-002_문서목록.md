# 화면 정의서 — 문서 목록

> 화면 ID: SCR-CMP-002 · 화면 유형: 목록 · 기획 관점(v6) · 구현 정합 2026-08-25
> 판단이 필요했던 항목은 §6 정책에서 확정했습니다.

---

## 1. 화면 정보

| 항목 | 내용 |
|---|---|
| 화면 ID | SCR-CMP-002 |
| 화면명 | 문서 목록 |
| URL 경로 | cmp-docs.html |
| 상위 메뉴 | 업무 관리 > 문서 목록 |
| 화면 유형 | 목록 |

이행 관리(SCR-CMP-001)가 **할 일 축**으로 「이 의무의 증빙이 갖춰졌나」를 묻는다면, 이 화면은 **문서 축**으로 「그 서류가 어디 있고 어느 할 일의 증거인가」를 묻는다. 같은 자료를 두 방향에서 보기 위한 짝이며, 문서 하나가 여러 할 일에 걸리고 할 일 하나에 여러 문서가 걸리는 관계를 문서 쪽에서 펼쳐 본다.

담양군 문서 원장에서 온 문서, 이 시스템에서 올린 문서, 전용 화면이 관리하는 현행 업무문서가 한 목록에 모인다. 출처가 다르면 **말할 수 있는 것도 다르므로** 목록과 상세가 그 사실을 함께 표시한다. 화면은 다섯 영역으로 구성한다: ① 안내 ② 조회 조건 ③ 문서 목록 ④ 문서 상세 ⑤ 할 일 고르기.

새 증빙 등록은 업무 업로드(SCR-DOC-007)가, 할 일 축 판정은 이행 관리(SCR-CMP-001)가 맡는다. 이 화면은 **찾고·확인하고·이어받고·바로잡는 데까지**이며, 담당자가 이어서 처리할 일은 내 할일(SCR-MYW-001)로 넘긴다.

---

## 2. 권한 정보

- 메뉴 접근·보기·수정은 시스템 관리의 메뉴 관리(SCR-ADMIN-005)와 권한 관리(SCR-ADMIN-006)에서 설정한다.
- 이 화면 정의서에서는 역할별 권한과 열람 범위를 별도로 정하지 않는다.
- 상태를 바꾸는 행위(제목 수정·올해 이어받기·문서 삭제)의 수행 주체는 §3 시나리오와 §6 정책에 상태 전이 주체로 적는다.

---

## 3. 진입 / 이탈 경로 및 사용자 시나리오

**진입 / 이탈**

| 구분 | 연결 화면 | 조건 / 트리거 |
|---|---|---|
| 진입 | 업무 관리 메뉴 | 메뉴 선택 |
| 진입 | 이행 관리(SCR-CMP-001) | 부서 보유 문서 건수 · 지난해 문서 전체 보기 |
| 진입 | 이행 관리 등록 결과 | 방금 만든 문서의 상세 보기 — 돌아올 화면을 함께 받는다 |
| 진입 | 현황 통계(SCR-RPT-001) · 기준문서함(SCR-DOC-001) | **분야**별 문서 건수 — 그 분야로 좁힌 목록으로 |
| 진입 | 경영방침(SCR-SBM-001) · 프리셋 양식 관리(SCR-ADMIN-003) | 그 메뉴의 문서 통합 보기 |
| 진입 | 딥링크 | 기준연도·부서·할 일·**분야**·문서를 주소로 지정해 그 자리에서 열림(§3-0) |
| 이탈 | 이행 관리(SCR-CMP-001) | 할 일 칩 · 「돌아가기」(다른 화면에서 열러 온 경우) |
| 이탈 | 업무 업로드(SCR-DOC-007) | 문서 등록 |

### 3-0. 주소로 들어오는 조건 (딥링크)

| 조건 | 뜻 | 보내는 화면 |
|---|---|---|
| 기준연도 | 그 해 문서 | 전 진입 경로 |
| 부서 | 그 부서 보유 문서 | 이행 관리 부서 요약 |
| 업무단계 | 그 할 일의 증빙 문서 | 이행 관리 상세 · 문서 상세 |
| **분야** | 옛 분류축의 대메뉴 단위 — 현행 업무문서가 그 값을 갖고 있다 | 현황 통계 · 기준문서함 · 경영방침 · 프리셋 양식 관리 |
| 문서 | 그 문서 상세를 바로 연다 | 등록 결과 · 내 할일 |

- **조건이 걸린 사실을 화면이 말한다 (MUST)** — 조건 칩으로 무엇으로 좁혔는지 보이고 **그 자리에서 풀 수 있어야** 한다. 안 그러면 전체 5만여 건이 수십 건으로 줄어든 이유를 알 수 없어 「왜 이것밖에 없지」가 된다. `[필터 초기화]`도 그 조건을 함께 푼다.
- **분야로 들어오면 연도 기본값을 푼다** — 분야를 보러 온 것이지 특정 연도를 보러 온 것이 아니다. 부서 조건도 같다(0건으로 맞이하지 않는다).
- **보내는 쪽과 받는 쪽의 건수가 같아야 한다** — 분야별 건수를 보여 주고 보냈는데 목록 건수가 다르면 둘 중 하나가 틀린 것이다. 옮길 때 대조한다.
- **없는 값으로는 열지 않는다 · 주소는 지금 상태를 담는다** — 이행 관리(§3-0)와 같다.

### 3-1. 문서를 찾아 근거를 확인한다

| 단계 | 사용자 행동 | 화면 반응 |
|:--:|---|---|
| 1 | 화면에 들어온다 | 기준연도 문서가 먼저 열린다. 전체 건수와 할 일 연결 건수를 함께 밝힌다 — 한 문서가 여러 할 일에 걸려 두 수는 다르다 |
| 2 | 연도를 고른다 | 연도마다 건수가 붙은 칩으로 고른다. 고르기 전에 몇 건인지 보인다 |
| 3 | 조건을 건다 | 문서명 검색과 상세 조건(부서·결재상태·수발신처)으로 좁힌다. 할 일로 좁히려면 할 일 고르기를 연다 |
| 4 | 문서를 연다 | 제목을 누르면 같은 화면 안에서 상세가 열린다 |
| 5 | 근거를 확인한다 | 어느 할 일의 증빙인지, 어디서 온 문서인지, 지난해 어느 문서에서 이어받았는지를 본다 |

### 3-2. 지난해 문서를 올해로 잇는다

| 단계 | 사용자 행동 | 화면 반응 |
|:--:|---|---|
| 1 | 지난해 문서 상세를 연다 | 지난 연도 문서에만 「올해 이어받기」가 붙는다 |
| 2 | 이어받는다 | 제목의 연도를 바꾸고 할 일 연결과 첨부를 함께 가져와 올해 문서를 만든다 |
| 3 | 확인한다 | 상세가 **그 자리에서 새 문서로 바뀌고**, 무엇을 함께 가져왔는지 건수로 알린다 |
| 4 | 제목을 다듬는다 | 지난해 제목의 연도 표기가 남아 있으면 제목 수정으로 고친다 |

### 분기 로직

| 무엇이 바뀌면 | 무엇이 달라지는가 |
|---|---|
| 문서의 출처 | 상세에 붙는 카드가 달라진다 — 원장 문서는 조회와 분류 표시만, 이 시스템에서 올린 문서는 제목 수정과 삭제가 함께 붙는다 |
| 문서의 연도 | 지난 연도 문서에만 「올해 이어받기」가 붙는다. 올해 문서를 또 만들 이유가 없기 때문이다 |
| 이미 이어받은 문서 | 이어받기 대신 「이어받기 완료」와 만들어진 문서로 가는 길을 보여 준다 |
| 다른 화면에서 열러 옴 | 상세에 「돌아가기」가 함께 붙는다. 그냥 들어온 경우에는 목록으로 닫는 수단만 있다 |
| 할 일 연결 유무 | 연결이 없으면 왜 없는지를 갈라서 표시한다 — 원장 교정 대상인지, 분류에서 뺀 것인지, 이 목록 밖 문서인지 |

### 예외·경계 시나리오

| 상황 | 화면 처리 |
|---|---|
| 조건에 맞는 문서가 0건 | 빈 목록과 조건을 지우는 수단을 함께 낸다 |
| 없는 문서 번호로 진입 | 상세를 열지 않고 목록을 보여 준다 — 뒤로가기로 닫히는 전제를 지킨다 |
| 문서가 많은 목록 | 쪽으로 나눠 보여 주고 전체 건수를 함께 밝힌다 |
| 할 일이 여러 개인 문서 | 목록에서는 두 개까지 보이고 나머지는 접어 두며, 눌러 그 자리에서 펼친다 |
| 제목 수정 권한이 없는 문서 | 수정 수단을 내지 않고, 다른 경로로 시도해도 사유와 함께 막는다 |
| 삭제할 수 없는 문서 | 삭제 수단을 내지 않고 왜 지울 수 없는지를 대신 밝힌다 |
| 같은 원본을 두 번 이어받기 | 이미 만든 문서로 옮겨 주고 그 사실을 알린다 |

---

## 4. 화면 구성별 정리

### 4-1. 안내

- 구성: 전체 문서 수·할 일 연결 수·미분류 수를 한 줄로, 펼치면 자료 성격과 한계를 설명한다.
- 이벤트:

| 트리거 | 동작 결과 | 이동 / 메시지 |
|---|---|---|
| 안내 펼침/접힘 | 자료 성격과 한계를 펼친다 | 접어도 지금 상태 한 줄은 남는다 |
| 업무 목록 링크 | 같은 문서를 종전 방식으로 찾는 화면으로 | SCR-DOC-005 |

- 상태: 접힌 한 줄은 설명이 아니라 **지금 상태**여야 한다 — 전체 건수와 미분류 건수를 담는다. 펼치면 세 갈래(미분류·분류 제외·분류 확인)의 뜻과 각 건수를 밝힌다. 결재 완료 원본을 보관하지 않는다는 사실도 여기서 밝힌다.

### 4-2. 조회 조건

- 구성: 문서명 검색, 결과 건수, [필터 초기화], [할 일 고르기], [상세 조건], 연도 칩 줄. 상세 조건에는 부서·결재상태·수발신처가 들어간다.
- 이벤트:

| 트리거 | 동작 결과 | 이동 / 메시지 |
|---|---|---|
| 검색어 입력 | 문서명에서 부분 일치로 거른다 | 타이핑 중 결과 건수가 따라 바뀐다 |
| 연도 칩 | 그 해 문서만 남긴다 | 칩마다 건수를 함께 보여 준다 |
| 상세 조건 펼침 | 값이 걸려 있으면 자동으로 펼쳐진다 | 적용 중인 조건 수를 [필터 초기화]에 표시 |
| 부서 선택 | 그 부서 문서만 남긴다 | 실재하는 부서만 건수와 함께 낸다 |
| 할 일 고르기 | 선택 창을 연다 | 4-5 |
| 필터 초기화 | 조건을 지우고 기본 연도로 돌아간다 | 연도 전체가 아니라 기본 연도다 |

- 상태: 조건이 바뀌면 첫 쪽으로 돌아간다. 고른 할 일은 조건 줄에 칩으로 남아 하나씩 뺄 수 있다. 부서로 넘어와 열린 경우 그 조건을 칩으로 보여 주고 풀 수 있게 한다.

### 4-3. 문서 목록

- 구성: 문서명(번호·연도 함께) · 업무단계 · 부서 · 수발신자 · 보고일자 · 결재상태. 아래에 쪽 이동.
- 이벤트:

| 트리거 | 동작 결과 | 이동 / 메시지 |
|---|---|---|
| 문서명 | 같은 화면 안에서 상세를 연다 | 주소에 남아 뒤로가기로 닫힌다 |
| 문서 줄(빈 곳) | 같은 상세를 연다 | 줄 안의 링크를 눌렀을 때는 양보한다 |
| 할 일 칩 | 그 할 일의 이행 상세로 나간다 | SCR-CMP-001 |
| 「+N」 | 접힌 할 일을 그 자리에서 펼친다 | 목록을 벗어나지 않는다 |
| 쪽 이동 | 다음 묶음을 보여 준다 | 전체 건수를 함께 표시 |

- 상태: 할 일 연결이 없는 문서는 이유를 갈라서 표시한다 — **미분류**(어느 할 일의 증빙인지 아직 안 붙음, 교정 대상) · **분류 제외**(타 기관 소관 등으로 판단이 끝남) · **이 목록 밖 문서**(전용 화면이 관리하는 문서라 할 일 축이 없음). 할 일이 붙었어도 원장이 확신을 낮게 표시한 문서는 **분류 확인**으로 함께 표시한다 — 판정에는 들어가되 확인이 필요하다는 뜻이다. 예시 자료로 만든 해의 문서는 그 사실을 줄마다 표시한다.

### 4-4. 문서 상세

- 구성: 돌아가는 줄, 머리(연도·출처·상태·제목·수발신·할 일 칩·분류 표시), 문서 정보, 결재 완료 문서 자리, 올해 이어받기, 올해 같은 할 일 현황, 삭제 안내.
- 이벤트:

| 트리거 | 동작 결과 | 이동 / 메시지 |
|---|---|---|
| 문서 목록으로 | 상세를 닫고 목록으로 | 조건과 쪽을 유지한다 |
| 돌아가기 | 왔던 화면으로 나간다 | 다른 화면에서 열러 온 경우에만 나온다 |
| 제목 수정 | 제목을 고치는 창을 연다 | 이 시스템에서 올린 문서에만 나온다 |
| 가져온 원본 | 이어받기 전 문서로 옮긴다 | 같은 화면 안에서 열린다 |
| 할 일 칩 | 그 할 일의 이행 상세로 | SCR-CMP-001 |
| 올해 문서로 등록 | 지난해 문서를 올해로 이어받는다 | 상세가 그 자리에서 새 문서로 바뀐다 |
| 문서 삭제 | 확인을 받고 지운다 | 이 시스템에서 올린 문서만 가능하다 |

- 상태: 결재 완료 원본은 보관하지 않으므로 그 자리에 **왜 없는지**를 적고 그럴듯한 미리보기를 대신 그리지 않는다. 문서 정보에는 문서번호·보고일자·수발신·담당부서·기준연도·**데이터 구분**·가져온 원본·비고가 들어간다. 데이터 구분은 출처를 있는 그대로 말한다 — 문서 원장 실측 · 예시 자료 · 사용자 등록. 담당자 값은 원장이 갖고 있지 않아 「기록 없음」으로 둔다. 이어받기는 지난 연도 문서에만 붙고, 이미 이어받았으면 완료 표시와 그 문서로 가는 길을 대신 보여 준다.

### 4-5. 할 일 고르기

- 구성: 왼쪽에 법정 의무, 오른쪽에 그 의무의 할 일, 칸마다 검색, 아래에 고른 것.
- 이벤트:

| 트리거 | 동작 결과 | 이동 / 메시지 |
|---|---|---|
| 의무 선택 | 오른쪽에 그 의무의 할 일을 펼친다 | 칸마다 따로 스크롤한다 |
| 할 일 선택 | 여러 개를 고를 수 있다 | 고른 것을 아래에 칩으로 모은다 |
| 적용 | 목록 조건에 반영한다 | 조건 줄에 칩으로 남는다 |
| 모두 지우기 | 고른 할 일을 비운다 | 조건이 풀린다 |

- 상태: 고를 것이 많아 드롭다운이나 한 줄 목록으로는 찾을 수 없다. 두 칸으로 나누고 칸마다 검색을 두는 방식은 업무 업로드(SCR-DOC-007)와 **같은 선택기**를 쓴다 — 담당자가 두 번 배우지 않게 한다.

---

## 5. 유효성 검사

| 입력 항목 | 필수 | 형식·길이 규칙 | 오류 안내 |
|---|---|---|---|
| 문서명(제목 수정) | 필수 | 공백을 제거한 뒤 비어 있지 않아야 한다. 길이 상한을 둔다 | 저장하지 않고 문서명이 필요함을 알린다 |
| 제목 수정 대상 | — | 이 시스템에서 올린 문서만 고칠 수 있다 | 수단을 내지 않고, 다른 경로로 시도해도 사유와 함께 막는다 |
| 제목 수정 주체 | — | 자기 부서 문서만 고칠 수 있다 | 사유와 함께 막는다 |
| 삭제 대상 | — | 이 시스템에서 올린 문서만 지울 수 있다 | 수단을 내지 않고 왜 지울 수 없는지 밝힌다 |
| 올해 이어받기 | — | 지난 연도 문서에만 제공한다. 같은 원본을 두 번 이어받지 않는다 | 이미 만든 문서로 옮겨 주고 그 사실을 알린다 |

증빙 등록 자체의 입력 검증은 이 화면이 아니라 업무 업로드(SCR-DOC-007)가 정의한다.

---

## 6. 정책

**P1. 두 화면은 축이 다르다 — 합치지 않는다.**
이행 관리(SCR-CMP-001)는 할 일 축으로 「이 의무의 증빙이 갖춰졌나」를, 이 화면은 문서 축으로 「그 서류가 어디 있고 어느 할 일의 증거인가」를 묻는다. 문서 하나가 여러 할 일에 걸리고 할 일 하나에 여러 문서가 걸리므로 한 축으로는 반대편을 볼 수 없다. 두 화면이 서로를 가리키되 판정은 이행 관리 한 곳에서만 낸다.

**P2. 문서 수와 연결 수를 함께 적는다.**
한 문서가 여러 할 일의 증빙일 수 있어 「할 일별 문서 수를 모두 더한 값」은 실제 문서 수보다 크다. 한 수만 보여 주면 다른 화면의 수치와 어긋나 보인다. 두 수를 나란히 적고 왜 다른지를 함께 밝힌다.

**P3. 할 일 연결이 없는 이유를 갈라서 말한다.**
셋은 다른 것이다 — **미분류**는 어느 할 일의 증빙인지 아직 안 붙은 것으로 교정 대상이고, **분류 제외**는 타 기관 소관·자치사무라 판단이 끝나 교정할 것이 없으며, **이 목록 밖 문서**는 전용 화면이 관리해 애초에 할 일 축을 갖지 않는다. 한 칩으로 묶으면 「남은 할 일」이 부풀거나 「확인이 끝났다」로 잘못 읽힌다.

**P4. 할 일이 붙었어도 확신이 낮으면 드러낸다.**
원장이 분류를 「애매」로 표시한 문서는 **판정에 그대로 들어간다.** 미분류와 달리 붙어 있으니 맞다고 읽히기 쉬워, 눈에 보이지 않으면 확인할 기회가 없다. 목록에서 별도 표시로 낸다.

**P5. 결재 완료 원본을 보관하지 않는다는 사실을 감추지 않는다.**
문서 연계가 붙기 전이라 문서명·수발신자·보고일자·생산등록번호만 있다. 미리보기 자리에 그럴듯한 지면을 대신 그리면 담당자가 원본이 있다고 오해한다. 그 자리에 왜 없는지를 적고 무엇이 있으면 볼 수 있는지를 밝힌다.

**P6. 데이터 구분은 출처를 있는 그대로 말한다.**
문서 원장 실측·예시 자료·사용자 등록을 구분해 적는다. 출처를 틀리게 말하면 그 자체가 거짓 자료가 된다 — 실측 자료가 「사용자 등록」으로 보이면 담당자는 그것을 우리가 만든 값으로 여긴다. 담당자 값처럼 원장이 갖고 있지 않은 항목은 「기록 없음」으로 두고 지어내지 않는다.

**P7. 이어받기는 지난 연도 문서에만 붙고, 같은 원본을 두 번 쓰지 않는다.**
올해 문서를 또 만들 이유가 없고, 같은 원본으로 사본이 여럿 생기면 그 할 일만 증빙이 부풀어 보인다. 이미 이어받았으면 이어받기 대신 만들어진 문서로 가는 길을 보여 준다. 무엇을 함께 가져왔는지(할 일 수·첨부 수)를 알림에 적는다 — 딸려온 것을 모르면 담당자가 첨부를 또 올린다.

**P8. 이어받은 문서는 어디서 왔는지 되짚을 수 있어야 한다.**
제목의 연도 표기가 어긋나 보이거나 내용이 안 맞을 때 원본을 확인해야 판단할 수 있다. 상세에 「가져온 원본」을 두고 그 문서로 옮길 수 있게 한다. 제목이 길면 한 줄로 자르되 전체를 볼 수단을 함께 둔다.

**P9. 제목은 이 시스템에서 올린 문서만 고칠 수 있다.**
원장 문서의 제목은 실제 결재 문서의 제목이라 이 시스템이 고칠 값이 아니다. 조회 범위와 별개로 자기 부서 문서만 고칠 수 있으며, 수단을 감추는 것으로 그치지 않고 다른 경로로 시도해도 막는다. 고칠 수 없는 문서에는 수단을 내지 않고 **왜 못 고치는지**를 대신 밝힌다 — 확인까지 시키고 거절하는 것은 없는 동작을 약속하는 것이다.

**P10. 제목 수정이 필요한 이유를 화면이 밝힌다.**
지난해 문서를 이어받으면 제목의 연도 표기가 남는 경우가 있다. 이어받기는 「작년→올해」 두 값만 바꾸므로 그 밖의 연도 표기는 그대로다. 자동으로 판단할 수 없는 값이라 사람이 고쳐야 하고, 수정 창이 그 사정과 이 문서의 기준연도를 함께 보여 준다.

**P11. 삭제는 이 시스템에서 올린 문서만 가능하다.**
원장·예시 문서를 지우면 원자료와 어긋난다. 잘못 붙은 분류는 지우는 것이 아니라 고치는 것으로 처리한다. 지울 수 있는 문서에는 무엇이 함께 사라지는지(할 일 연결)를 밝히고, 지울 수 없는 문서에는 수단 대신 사유를 낸다.

**P12. 상세는 이 화면 안에서 끝난다.**
문서 정보·제목 수정·삭제·이어받기를 모두 이 화면이 갖는다. 종전에는 담당부서와 데이터 구분을 보려면 다른 메뉴의 문서 상세로 넘어가야 했다. 업무 관리에서 시작한 사람이 메뉴 밖으로 나가면 하던 일의 맥락을 잃는다.

**P13. 돌아가는 길은 「왔던 화면」과 「이 화면의 목록」 둘을 구분한다.**
다른 화면에서 문서를 열러 왔다면 그 화면으로 나가는 길이 있어야 한다 — 없으면 메뉴로 다시 들어가야 하고 보던 자리를 잃는다. 목록으로 닫는 것과는 다른 동작이므로 한 버튼으로 합치지 않고, 한 줄에 나란히 두어 지금 어디서 왔는지가 보이게 한다. 돌아갈 곳은 정해진 목록 안에서만 인정한다 — 주소로 들어오는 값이라 그대로 따라가면 외부로 나가는 통로가 된다.

**P14. 조건 선택지는 실재하는 값만 낸다.**
부서 조건은 문서에 실제로 부서 값이 있어 성립한다. 종전에는 원장이 한 부서뿐이라 드롭다운을 만들면 대부분 0건이 되어 「자료가 없다」로 오독됐고, 그래서 다른 화면에서 넘어온 경우에만 칩으로 보여 줬다. 그 전제가 사라졌으므로 선택 수단으로 올린다. 옵션은 문서가 있는 부서만 건수와 함께 낸다 — 고르면 0건이 되는 선택지는 헛걸음이다.

**P15. 할 일을 고르는 방식은 업무 업로드와 같은 것을 쓴다.**
고를 것이 수백 개라 드롭다운은 스크롤로 찾으라는 말이고, 한 줄 목록은 원하는 것을 만나기까지 계속 굴려야 한다. 좌우 두 칸에 칸마다 검색을 두는 방식을 두 화면이 함께 쓴다 — 같은 일을 하는 화면이 다른 방식을 쓰면 담당자가 두 번 배운다.

**P16. 이 화면에 부서 조회 범위를 걸지 않는다.**
조회는 열고 조작만 막는다. 좁힐지는 담양군이 정할 정책이며, 근거가 「자료가 없어서」가 아니라 「정책 미확정」이라는 것을 이행 관리(SCR-CMP-001)와 같은 기준으로 둔다.

---

## 7. 미결사항

| ID | 외부 입력 | 미수신 시 동작 | 제공 주체 | 기한 |
|---|---|---|---|---|
| DOC-01 | 문서 담당자 연계 규격 | 부서까지만 표시하고 담당자는 「기록 없음」으로 둔다(P6) | 온나라·새올 연계 | 연계 착수 시 |
| DOC-02 | 결재 완료 원본·본문 연계 | 원본을 보관하지 않고, 그 자리에 왜 없는지와 무엇이 있으면 볼 수 있는지를 밝힌다(P5) | 온나라 연계 | 연계 착수 시 |
| DOC-03 | 원장 분류 교정 결과 — 미분류 문서를 어느 할 일에 붙일지 | 미분류로 표시해 교정 대상임을 드러내고 판정에 넣지 않는다(P3) | 담양군 | 분류 정리 완료 시 |
| DOC-04 | 지난 연도 문서 원장 — 기준연도 이전 자료 | 받은 해만 목록에 싣고, 연도 칩에 실재하는 해만 낸다 | 담양군 | 자료 수신 시 |

문서 원본 파일을 이 시스템이 보관할지는 **본개발 구현 범위**다. 현재는 파일 이름과 크기만 기록하며 그 사실을 상세에 표시한다.
