# 담양군 중대재해 통합관리 시스템 — 프로토타입 v2

빌드/프레임워크 없는 정적 HTML + 전역 스크립트(IIFE) 프로토타입. 각 화면은 `*.html`, 로직은 `js/*.js`(전역 `DYV2`·`EDOC`·`DYREG`·`DYSETLIST` 네임스페이스), 스타일은 `css/style.css`(공통) + `css/v2.css`(v2 토큰·컴포넌트).

## 문서 위치 — 루트에 문서를 늘리지 말 것

프로젝트 루트의 `.md` 는 **`CLAUDE.md`(작업 규칙)와 `README.md`(개요) 2개뿐**이다. 그 외 문서는 전부 `docs/` 아래에 두고, 목록은 [`docs/README.md`](docs/README.md)가 관리한다.

| 위치 | 용도 |
|---|---|
| `docs/planning/` | **현행** 기획·PRD·검수 문서 — 새 기획 문서는 여기 |
| `docs/screen-definitions/` | 화면 정의서(SoT) + `_build-data.py` 생성 스크립트 |
| `docs/archive/` | 대체·완료된 문서와 과거 검증 기록(`audit/`) — 새 작업의 근거로 삼지 말 것 |
| `docs/` 루트 | RFP 기능요구사항 정리 · `DESIGN-TOKENS.md` |

문서를 옮기거나 새로 만들면 `docs/README.md` 표에 한 줄을 함께 갱신한다. 구현이 기획을 대체하면 **삭제하지 말고 `archive/`로 옮기고 대체 사유를 표에 적는다**(왜 지금 구조가 되었는지 추적 가능해야 한다).

---

## UI 규칙

### 1. 단일 모달 규칙 — 한 시점에 모달은 반드시 1개 (MUST)

화면에 떠 있는 모달/다이얼로그 오버레이는 **항상 1개**다. 모달 위에 또 다른 모달을 띄우는 적층(stacked modal)은 **금지**한다.

**근거** — 적층 모달은 (a) 닫기·ESC·백드롭 클릭의 대상이 모호해지고, (b) 포커스 트랩이 깨지며, (c) 부모 모달이 가려져 맥락을 잃고, (d) 오버레이가 잔류(orphan)하기 쉽다.

**올바른 패턴 — 모달 안 인라인 패널**
부가 선택(조직도에서 담당자 선택 등)이 필요하면 **새 모달을 띄우지 말고**, 현재 모달 본문 안에 자체 스크롤 영역을 가진 인라인 패널로 넣는다.
- 인라인 조직도 트리는 공용 클래스 `.org-inline` / `.org-inline-search` / `.org-inline-body`(`css/v2.css`)를 재사용한다.
  - 상시 노출형: `js/doc-register.js`의 `ownerBlock()` + `wireOwnerTree()` (담당자 트리를 `#reg-orgtree` 패널로 임베드).
  - 토글형(버튼으로 펼침): `js/edoc.js`의 `EDOC.openOrgTree(targetId)` — 입력을 `.orgpick-field`로 감싸고 그 안에 트리를 펼친다. (전자문서 점검자·경영방침 점검자 공용)
- 모달 안 '추가' 폼은 `PG._stackOpen()`(`js/menu.js`)처럼 모달 본문에 `.stack-inline` 패널로 붙인다(`#v2-modal .modal-body`에 append).
- 펼침 상세(법령 ⓘ 등)는 `EDOC.lawInfo(btnEl, basis)`처럼 해당 행 아래 `.lawinfo-inline`으로 펼친다.
- 단계가 많은 입력은 마법사 스텝(`doc-register.js`의 STEP 1~4)처럼 **같은 모달 안에서 본문을 교체**한다.

**공유 헬퍼만 사용**
- 모달 열기/닫기는 반드시 `DYV2.openModal(title, bodyHtml, footHtml)` / `DYV2.closeModal()`만 사용한다. `openModal`은 진입 시 `closeModal()`을 호출해 기존 레이어를 먼저 제거하므로, 이 경로만 쓰면 단일 모달 불변식이 자동 유지된다.
- `closeModal()`은 본 모달(`#v2-modal`)과 함께 부수 오버레이(`org-tree-overlay`·`stack-overlay` 등)도 제거한다.
- 직접 `document.createElement('div'); ...appendChild` 로 오버레이를 만들지 않는다.

**적층 오버레이 → 인라인 전환 완료 (2026-06-24)**
아래는 모두 별도 오버레이를 제거하고 인라인 패널로 전환됨. 신규 작업 시 같은(적층) 패턴을 다시 만들지 말 것.
- `js/edoc.js` `openOrgTree()` — 점검자 선택: 입력 아래 `.org-inline` 토글 패널.
- `js/edoc.js` `lawInfo()` — 관련 법령 상세: 점검 항목 아래 `.lawinfo-inline` 펼침.
- `js/menu.js` `PG._stackOpen()` — 관리 모달 추가 폼: 모달 본문 안 `.stack-inline` 패널.

### 2. 첨부파일 업로드 안내 — 단일 출처

지원 형식·용량·개수 제약은 `DYV2.FILE_LIMITS`(`js/common.js`)에 단일 정의하고, 업로드 영역 하단 안내 문구는 `DYV2.fileHint()`로 렌더한다. 새 업로드 UI를 만들면 문구를 직접 쓰지 말고 `DYV2.fileHint()`를 재사용한다. (현재: 지원 형식 HWP·HWPX·PDF·DOC(X)·XLS(X)·PPT(X)·JPG·PNG·ZIP, 파일당 최대 20MB, 최대 10개.)

**드롭존은 `DYV2.uploadDrop()`로만 렌더 (MUST)** — `.upload-drop`은 `cursor:pointer`·hover 강조로 "클릭하여 업로드" 어포던스를 약속하므로, 없는 동작을 시각적으로 약속하는 무핸들러 드롭존(`<div class="upload-drop">…</div>` 직접 작성)은 **금지**한다. 대신 접근성 렌더러 `DYV2.uploadDrop(labelHtml, onAct, opts)`(`js/common.js`)를 재사용한다. 이 헬퍼는 `role="button"`·`tabindex="0"`과 클릭·Enter·Space 활성화(`DYV2.dropKey`)를 배선하고, `opts.hint === true`면 `fileHint()`를 함께 렌더하며 `aria-describedby`로 제약 문구를 연결한다(WCAG 2.1.1/4.1.2/2.4.7, 포커스 링은 `css/v2.css`의 `.upload-drop:focus-visible`).
- `onAct`: 활성화 시 실행할 인라인 JS 표현식(기존 `onclick` 관례대로 작은따옴표 문자열). 생략 시 프로토타입 토스트. **실제 등록/제출은 모달 하단 `[등록]`·`[제출]` 버튼이 담당**하고, 드롭존은 파일 선택 어포던스만 응답한다.
- 예외 — 실제 파일 `input`을 배선한 드롭존(`js/facil.js` `#fac-drop`)과 정적 HTML 마크업(`base-bulk.html`)은 헬퍼 대신 같은 속성(`role`·`tabindex`·`onkeydown="DYV2.dropKey(event)"`)을 직접 부여해 실제 배선을 보존한다.

### 3. 조직도 데이터 — 단일 출처

조직도 데이터는 `DYV2.ORG`(`js/common.js`)에 단일 정의한다. 화면별 조직도는 `DYV2.orgFlat()` 등 파생 뷰만 사용하고 자체 조직도 데이터를 만들지 않는다.
- 스키마: 노드 `{ id, name, type('root'|'post'|'bureau'|'dept'|'team'|'office'|'town'), members:[{uid, name, role, team?}], children:[...] }`. 권한 시드가 uid 를 참조하므로 **uid 는 변경 금지**.
- 파생 헬퍼: `DYV2.orgFlat(opts)`(EDOC 호환 평면 투영 `[{dept, members:[[role,name]]}]`) · `DYV2.orgDepts()`(부서형 노드 `[{id, name, count}]`) · `DYV2.deptNames()` · `DYV2.orgNode(id)` · `DYV2.orgCount(id, includeSub)` · `DYV2.orgTotal()` · `DYV2.orgWalk(fn)`.
  - `orgFlat()`은 EDOC 호환을 위해 **id 를 버리고 부서명만** 투영한다. 부서를 `deptId`로 저장하는 도메인(위험성평가)은 `orgDepts()`를 쓴다.

**부서·담당자 선택 UI 는 `ORGPICK`(`js/org-pick.js`)으로만 렌더 (MUST)** — 새 부서 `<select>` 드롭다운을 만들지 말고 공용 인라인 조직도(`.org-inline` + `.otr-*`, 검색 + 240px 스크롤)를 재사용한다. 전 화면이 같은 트리 GUI를 공유해야 한다. `js/common.js` 뒤에 로드.
- `ORGPICK.toggle(fieldId, mode, '전역함수경로')` — 입력 필드(`.orgpick-field` 래퍼) 아래 펼치는 토글형. 고르면 패널이 닫힌다.
  - `'dept'` → `fn(부서명)`. 부서를 **이름**으로 저장하는 도메인(작업환경측정·건강검진).
  - `'deptId'` → `fn(id, name)`. 부서를 **id**로 저장하는 도메인(위험성평가 — 수시평가 등록).
  - `'member'` → `fn('부서 · 역할 / 이름')`.
  - ※ dept/deptId 두 모드는 저장 스키마가 실제로 갈려 있어 공존한다. 합치려면 스키마부터 통일할 것.
- `ORGPICK.deptsPanel(panelId, opts)` — **부서 다중 선택 · 상시 노출형**(트리가 본문 자체인 마법사 STEP 용, `js/rsk-list.js` 정기평가 생성 STEP1). `opts`: `selectedPath`(선택맵 반환 전역함수) · `onToggle` · `countId` · `allckId`.
  - `onToggle`에서 **전체 재렌더(`openModal`) 금지** — 패널을 다시 그리면 검색어·스크롤이 날아간다. 선택 개수·전체선택 indeterminate 동기화는 ORGPICK이 인플레이스로 처리하고, 선택맵을 바깥에서 통째로 바꾼 뒤(전체선택 등)에만 `ORGPICK.refreshDepts(panelId)`를 호출한다.
- 예외 — 부서 **필터** 드롭다운(목록 상단 조회 조건)은 등록이 아니므로 `<select>` 유지.
- 소비처: `EDOC.ORG_TREE`(= `orgFlat()` 파생 → 경영방침·의견청취·회의록·업무 등록 담당자·예산 기관) · 메뉴/권한/사용자 관리(`DYADM.ORG = DYV2.ORG` 참조) · 인력평가 평가자 선택(evl-eval.html `PICK_TREE` 파생) · 조직 화면(m=org) 요약 카드.
- `common.js` 는 `edoc.js`·`adm-perm.js` 보다 먼저 로드되어야 한다(전 페이지 검증됨).

### 4. 안전보건교육 — 별도 GNB 그룹 · 9메뉴 + 상세 1 (재설계 v1 적용, 2026-07-20 · 결재 이력 2026-07-24)

안전보건교육은 **독립 GNB 그룹 `edu`**(`js/layout.js`)이고, SNB 3뎁스 구조다 — 현업근로자(정기 `edu-reg` · 채용시 `edu-hire` · 기타 `edu-etc`) · 관리감독자(정기 `edu-sup` · 채용시 `edu-sup-hire` · 기타 `edu-sup-etc`) · 직속(이수현황 `edu-status` · 근로자 명단 관리 `edu-workers` · 결재 이력 `edu-approval`). 근거: `docs/planning/기획-안전보건교육-재설계-v1.md` — safe-damyang-v2 스냅샷의 UI/UX·메뉴 구조를 이식(2026-07-20). **구 "단일 화면 3탭" 확정(보완안 §7, 2026-07-19)은 이 이식으로 대체됨** — 다시 3탭으로 합치지 말 것.

- **'현업근로자'를 '근로자'로 넓히지 말 것 (MUST, 2026-08-13 확인)** — 이름이 좁은 것이 아니라 **법정 대상이 좁다**. 산안법 **시행령 별표1 제4호가목**(`oshe-t1`)이 공공행정을 **제3장(안전보건교육 §29~§33) 적용 제외**로 두고, *"청소, 시설관리, 조리 등 **현업업무에 종사하는 사람**으로서 고용노동부장관이 정하여 고시하는 사람"* 만 그 제외에서 다시 뺀다. 즉 담양군에서 교육 의무가 걸리는 사람은 현업업무 종사자다. 메뉴명을 '근로자'로 바꾸면 법정 대상 범위를 잘못 말하게 된다.
  - **다만 현재 모집단은 과대하다** — `DYEDU.fieldWorkers()` 는 '관리감독자가 아닌 전원'이라 사무직까지 들어간다. 현업업무 종사자 지정명단은 고시 기준으로 **담양군이 정할 자료**라 우리가 임의로 좁힐 수 없다. 화면·정의서에 **갭으로 드러내고**(이수율 분모가 법정 대상보다 넓다) 명단을 받으면 좁힌다. 지어내 채우지 말 것.
  - 별표4의 축은 **제1호(근로자)와 제1호의2(관리감독자) 2분**이고, 사무직·판매는 제1호 **안의** 하위 구분이다. '현업'은 그 하위 구분 이름이면서 동시에 위 적용범위 용어이기도 하다 — 두 뜻이 겹치는 것이지 틀린 것이 아니다.

- 파일 구성: 화면 HTML 10개(`edu-reg/hire/etc/sup/sup-hire/sup-etc/status/workers/reg-detail/approval.html`) + 모듈 7개(`js/edu-reg.js` EDUR · `edu-reg-detail.js` EDURD · `edu-hire.js` EDUH · `edu-etc.js` EDUE · `edu-status.js` EDUS · `edu-workers.js` EDUW · `edu-apv-log.js` EDUAPVLOG) + `js/edu-data.js`(전역 `DYEDU`, 데이터 단일 출처) + `js/edu-filter.js`(전역 `EDUFILTER`, 공용 필터 바) + `js/edu-doc.js`(전역 `EDUDOC`, 공문·결재 — `DYDOC` 골격 위) + `js/edu-tour.js`(전역 `EDUTOUR`, 시연 투어). 공용 스타일은 `css/v2.css`의 `edu-*` 블록 — **별도 `edu.css` 파일 없음**(재생성 금지).
- 로드 순서: `layout.js → common.js → org-pick.js → edu-data.js → doc-flow.js → edu-doc.js → edu-filter.js → 화면 모듈 → edu-tour.js`.
- **공문·결재는 `EDUDOC`(`js/edu-doc.js`)로만** — 공용 골격 `DYDOC`(§7-1) 위의 교육 파사드다. 화면마다 결재 버튼·문서를 새로 짜지 않는다. 근거: [`기획-안전보건교육-온나라결재상신-v1.md`](docs/planning/기획-안전보건교육-온나라결재상신-v1.md) **v1.2** · `SCR-EDU-001 §4-6` · `SCR-EDU-004 §4-5`.
  - **순서를 뒤집지 말 것 (MUST)** — `[공문 기안]` → 본문 **직접 작성** → `[문서 미리보기 →]` → `[온나라로 결재 상신]` → 확인 → `[상신]`. **기안 폼에는 상신 수단이 없다.** 발주처가 종전 `[결재 상신]`을 두 번 지우게 한 이유가 기능이 아니라 *"공문 작성 단계 없이 결재만 올라가는 순서"* 였다(*"왜 갑자기 뜬금없이 결재 상신이 있지"*).
  - **단위는 교육 1건뿐이다.** 종전 3종(총괄·교육별·개인별)에서 둘을 뺐다 — **기간 총괄**은 분기 실시 결과 제출을 업무 축(`DYWORK`, 실측 440문서·34부서)이 이미 담당해 정본이 둘이 되고, **개인별 이수 확인서**는 결재가 아니라 증명서라 `보고서·제증명`(`reports.html`) 소관이다. 갈래가 하나면 담당자가 무엇을 고를지 판단하지 않는다. **3종을 되살리지 말 것.**
  - **진입점은 한 자리다** — 종료 처리된 교육의 `[공문 기안]`(`EDUDOC.control(courseId)`). 정기(`edu-reg`)·기타(`edu-etc`)는 카드에, 상세(`edu-reg-detail`)는 제목 줄에, 채용시(`edu-hire`)는 이수 행에서 **그 사람이 이수한 교육 건**으로 연결한다. 우측 상단 총괄 버튼을 만들지 말 것.
  - **기안 조건은 종료 처리 하나다** — 진행 중인 교육의 실시 결과는 보고할 수 없다. **이수율은 조건이 아니다** — 전원 이수를 요구하면 이수율 낮은 부서가 실시 결과를 영영 보고하지 못한다(실측 38~56%). 미이수는 결과의 일부로 적는다.
  - **붙임은 이미 올려 둔 파일에서 고른다** — 교육 첨부(교육일지·교육사진)와 부서별 참석자 서명파일이 후보다. **공문 화면에서 파일을 새로 받지 않는다**(같은 증빙이 두 벌 생긴다). 이수자 명단만 파일이 아니라 **붙임 별지**로 짠다.
  - **본문은 자동 생성하지 않는다** — 시스템은 수치를 세어 **삽입 칩**(교육 개요·실시 결과·근거 문구)으로 건넬 뿐이다. 근거 라벨은 `DYLAW.basisTitle()` 파생이고 조문 표기를 화면이 조립하지 않는다(§10).
  - **결재가 올라간 뒤에는 잠근다** — 판정은 **`EDUDOC.lockOf(courseId)` 한 곳에서만**(결재중·결재완료=잠김, 반려=수정 가능). 화면이 스토어를 직접 읽어 잠금을 재구현하지 말 것. 근거: *"공문을 여기다 첨부하면은 문서들에 대한 기록이 남잖아요. 그러면 더 이상 수정이 안 돼요. 이건 문서 위조예요."*
  - **잠글 대상에 참석자 등록부 신청 취소가 포함된다 (MUST, 2026-08-13 추가)** — 공문의 이수자 명단(붙임 별지)은 저장된 사본이 아니라 **열 때마다 현재 이수기록에서 다시 파생**된다(`js/doc-flow.js`). 그래서 상신 뒤에 신청을 취소하면 **이미 결재선을 타고 올라간 문서의 명단이 사후에 줄어든다.** 수정·삭제만 잠그고 취소를 열어 두면 잠금이 뚫린 것이다. 버튼도 함께 잠금 표시로 바꾼다 — 누르면 거절하는 버튼을 남기면 담당자가 이유를 모른 채 두 번 누른다.
  - **상신 이력은 `edu-approval.html`(결재 이력, `js/edu-apv-log.js` EDUAPVLOG)에서 조회 전용으로** 본다. `EDUDOC.submissions()`·`openDocSid()`만 소비하고 스토어를 직접 읽지 않는다. **상신 경로가 바뀌면 이력 출처도 함께 옮긴다** — 옛 경로만 읽으면 공문을 올려도 목록이 0건으로 남는다(실제로 냈던 결함).
  - **결재 상태 회신(완료·반려)은 온나라 연동 몫**이라 이 화면에서 만들지 않는다. 연동 전에는 상신된 사실과 문서만 보여 준다 — 없는 기능을 버튼으로 약속하지 않는다.
  - **기안·상신 권한은 `EDUDOC.canDraft` 한 곳에서만 (MUST, 2026-08-13)** — 판정은 `DYROLE.canAct(course.deptId)` 파생이다. **등록 주체 = 기안 주체**이므로 그 교육을 등록한 부서 담당자와 재난안전과 담당자가 기안한다. 재난안전과로만 좁히면 자체·기타 교육을 등록한 과가 자기 실시 결과를 영영 보고하지 못한다(§4 "등록 주체는 각 과"). 관리·감독 계층은 조회만 한다.
    - 골격이 `cfg.canDraft` 훅으로 **기안·미리보기·확인·상신 네 경로 전부**를 막는다. 버튼만 감추면 전역 호출로 뚫린다.
    - 화면(`control()`)과 골격이 **같은 지역 함수**를 본다. 두 곳이 따로 판정하면 버튼은 보이는데 눌러도 거절되는 상태가 생긴다.
    - 권한이 없으면 버튼을 내지 않고 **누가 기안하는지**를 대신 밝힌다. 사유 문구는 `draftDenyNote()` 한 곳에서 낸다 — "재난안전과만 가능합니다"로 뭉뚱그리면 등록 부서 담당자에게 거짓말이 된다.
    - 위험성평가가 '주관부서만'인 것과 규칙이 갈리는 것은 결함이 아니다 — 위험성평가는 주관부서가 총괄해 내려보내고 교육은 각 과가 실시한다.
  - **결재 완료·반려를 수기로 받지 않는다 (2026-08-13 확정)** — 온나라 회신 연동 전까지 상태는 `결재중` 하나다. 대신 **막다른 길로 두지 않는다** — 상신 사실까지만 안다는 것과 진행 상황은 **온나라에서 문서번호로 확인**한다는 것을 공문 칩·결재 이력 양쪽에 밝힌다. 수기 등록은 "시스템은 결재완료인데 온나라는 반려"를 만들어 이력의 값을 없앤다.
  - **결재선을 코드에 고정하지 말 것** — 결재권자는 안전보건 법령이 아니라 **지자체 위임전결규칙** 소관이라 조직 개편·규칙 개정으로 바뀐다. 기본값(부서의 팀장 → 부서장)만 `DYV2.ORG`에서 직위명으로 파생하고, 변경은 `ORGPICK`(`member` 모드)으로만 받는다.
  - **구 모듈 `js/edu-approval.js`(EDUAPV)는 폐기했다 (2026-08-12).** 상신 단위 3종·`SUBMIT_ENABLED` 스위치·개인별 결재 칩이 전부 그 축이었고, 공문 흐름이 갖춰지면서 존재 이유가 사라졌다. **되살리지 말 것** — 되살리면 발주처가 거부한 "공문 없이 결재만" 상태로 돌아간다.

- **관리감독자 3화면은 별도 코드가 아니다** — `EDUR/EDUH/EDUE.init(mount, { supMode: true })` 플래그 재사용(`edu-sup.html`·`edu-sup-hire.html`·`edu-sup-etc.html`). 관리감독자용 모듈을 새로 만들지 말 것.
  - **채용 시 교육 기준일은 `채용일`과 `지정일` 중 늦은 쪽이다 (MUST, 2026-08-13)** — 판정은 `DYEDU.hireAnchor(worker)` 한 곳이다. 담양군은 기존 공무원 중에서 관리감독자를 지정하므로 채용일을 기준으로 삼으면 **그 시점에 관리감독자가 아니었던 사람**이 전원 미이수로 뜬다. 별표4 제1호의2 나목의 취지가 「그 지위에서 일하기 전에 교육」이라 지위에 오른 날로 본다.
  - **지정일이 없으면 판정하지 않는다** — `UNKNOWN`(판정 불가)으로 두고 **미이수·이수 어느 탭에도 넣지 않는다.** 이수로 넣으면 하지 않은 교육을 했다고 하는 것이고, 미이수로 넣으면 대상인지도 모르는 사람에게 의무를 지우는 것이다. `전체 = 미이수 + 이수 + 판정 불가`이므로 탭 합이 전체와 다르며, 그 사실을 화면이 밝힌다. 없는 기준일을 채용일로 대신하지 말 것 — 위 오판정이 그대로 돌아온다.
  - 담양군 관리감독자는 현재 **지정일 전원 미등록**이라 이 화면이 전건 판정 불가다. 결함이 아니라 자료 대기 상태다(99_미결사항 EXT-12).
- `edu.html`은 `edu-status.html`(대표 진입) 리다이렉트 스텁(쿼리 보존). 기존 링크는 무수정 동작.
- **`js/edu-data.js`는 삭제·덮어쓰기 금지** — `js/my-work.js`가 `global.DYEDU`로 참조하고, 현재본이 소스 스냅샷의 상위집합(v1r4, 독촉 시드 3건 포함)이다. 시드 변경 시 `SKEY` 버전 범프.
- **시연 투어(EDUTOUR)는 발표용 핵심 자산 — 제거 금지.** (2026-08-04 엔진을 `DYTOUR`(`js/tour-core.js`)로 이관 — 공개 API 12개는 계약이라 불변. 상세는 §4-3.) **5단계**(집합교육 등록 → 부서 신청·서명 업로드 → 교육 종료 처리 → 이수현황 반영 → 실시 결과 공문 기안·온나라 상신)를 실제 화면을 건너다니며 구동하는 크로스 페이지 투어다. 단계 상태는 sessionStorage(`dy-edu-tour-v1`), 모듈이 저장 성공 시 `EDUTOUR.onEvent('created'|'applied'|'closed')` 호출로 자동 진행하고 모달에는 `.edu-tour-inline` 시연 포인트가 자동 주입된다. **각 edu 화면은 모듈 init 후 `EDUTOUR.boot()`를 반드시 호출**(시연 바+패널 렌더). 시연 데이터 복원·초기화는 `DYEDU.reset()`.
- 표준 준수 이식: 탭 `.sub-tabs` · 표 `.table-figma`(+`.table-compact`·`.table-nowrap`·`tr.row-short` modifier) · 배지 `chip-status`(+`.chip-sm`)+`DYV2.toneOf` · 빈상태 `.v2-empty` · 게이지 `.progress` · 등록 폼 부서 선택 `ORGPICK('deptId')`(목록 상단 필터는 `<select>` 유지). 모달 상호작용 재렌더 전 `capture*()`로 입력값 보존.
- **목록 필터는 `EDUFILTER`(`js/edu-filter.js`)로만 렌더 (MUST)** — 화면마다 필터 UI 를 새로 짜지 않는다. `EDUFILTER.bar(fields, opts)` 가 [검색어][셀렉트…][체크] … [결과 건수][필터 초기화][액션] 한 줄을 만들고, `opts.reset` 은 적용 중인 조건 개수를 배지로 보여준다(0건이면 비활성). 보조: `yearOptions(dates)`(데이터에 실재하는 연도만) · `monthOptions()` · `match(q, parts)`(다중 필드 부분일치).
  - **검색어가 있는 화면의 재렌더는 반드시 `EDUFILTER.rerender(render)`로 감싼다** — mount 를 통째로 다시 그리는 이 코드베이스 관례상, 감싸지 않으면 타이핑 도중 포커스·캐럿이 날아간다.
  - 화면별 조건: 정기(검색·상태·연도·월·[자체]부서) · 채용시(검색·부서·고용형태·채용연도·[이수탭]이수구분) · 기타(검색·분류·부서·연도) · 이수현황(요약: 검색·완료율구간 / 상세: 검색·부서·구분·고용형태·이수상태) · 명단(검색·부서·구분·고용형태·출처·채용연도).
- **교육 시간은 회차(sessions)에서만 파생한다 (MUST)** — 단, **시간 모델이 셋이고 이 MUST 는 그중 하나에만 걸린다.** 예외를 모르면 나머지 둘이 규칙 위반으로 읽힌다.
  | 축 | 저장되는 `hours` | 왜 |
  |---|---|---|
  | 정기(집합·자체)·관리감독자 정기 | **회차 합계** — 아래 MUST 대상 | 실시 시간이 곧 인정 시간이다 |
  | 채용시(`edu-hire`·`edu-sup-hire`) | **대상자별 법정 필요시간**(근로자 1·4·8h / 관리감독자 8h 단일). 회차는 실시 기록으로만 남는다 | 고용형태마다 필요시간이 달라 한 건의 합계로 인정할 수 없다. **드러난 한계** — 실제 참석 시간과 인정 시간이 갈리는데 시스템이 실시 시간을 묻지 않아 둘을 구분할 수 없다(SCR-EDU-002 §6 에 명시) |
  | 기타(`edu-etc`) | **직접 입력** | 이미 실시한 교육을 기록하는 경로다. 법정 최소 미달은 막지 않고 드러낸다 |
  등록 폼은 `일자 + 시작~종료 시각`의 회차 탭(최대 5일)이고, 교육 시간 입력란은 없다. 합계는 `DYEDU.sumSessionHours()`(0.1h 단위) 단일 출처이며, 저장 시 `date`·`time`·`endTime` 은 1회차, `hours` 는 합계로 파생한다(기존 화면·집계 호환). 시간을 바꾸는 수정은 반드시 `DYEDU.syncCourseRecordHours()` 로 이미 쌓인 이수기록까지 함께 갱신한다 — 안 맞추면 교육 카드의 'Nh'와 이수현황의 '인정 Nh'가 조용히 어긋난다. 표시는 `courseDateTime()`(다회차면 '외 N일')·`courseSessions()` 를 쓰고 화면에서 직접 시각을 빼지 않는다.
- **이수현황 필터는 모집단 기준을 요약·상세가 공유한다** — 구분·고용형태·채용연도·출처는 `inPopulation()` 한 곳에서 판정하고, 부서별 요약의 완료율도 같은 조건으로 다시 집계한다(`DYEDU.deptSummary(date, filterFn)`). 필터를 건 화면에는 집계 모집단 배지(`.edu-pop-note`)를 함께 노출해 전체 수치로 오독되지 않게 한다.
- **첨부파일 슬롯은 `EDUFORM.renderAttach(F, ns, profile)` 프로필로만 (MUST, 2026-07-30 회의)** — 화면마다 첨부 칸을 다시 짜지 않는다. `'group'` 집합교육 = **참석자 명단 · 교육 자료 2칸**(교육 사진 없음 — 등록 시점엔 아직 실시 전이다), `'done'` 자체·채용시·기타 = **`교육일지 및 교육사진 등` 1칸**(실시 후 기록이라 한 파일로 온다). 저장은 `F.files[]` 그대로이고 `file.slot` 만 함께 남는다. **"계획서·교재" 표기는 금지**(발주처가 직접 지적). 등록 폼은 더 이상 `photos[]`를 만들지 않는다 — 기존 시드의 `photos` 표시는 하위호환으로 남겨둔 것이다.
- **CRUD 는 회수 경로까지 갖춘다 (MUST)** — 등록만 있고 지울 수단이 없으면 시연을 반복할수록 데이터가 쌓여 발표가 망가진다. 교육은 `수정`·`삭제`(EDUR/EDUE), 신청은 `참석자 등록부 등록 취소`(EDURD), 채용시 이수는 **`이수 수정`**(EDUH) 을 제공한다.
  - 채용시 이수는 전면 라벨이 **'취소'가 아니라 '수정'** 이다(2026-07-30 회의 — *"왜 취소는 왜 할 필요가 왜 있는지 모르겠네. 수정을 차라리 수정 버튼을 누르는 게 낫지"*). 회수(삭제)는 없애지 않고 **수정 모달 안**으로 내렸다 — 위 MUST 를 지키면서 발주처 지시도 만족시키는 유일한 형태다.
  - **결재가 올라간 뒤에는 잠근다** — 판정은 `EDUDOC.lockOf(courseId)` 한 곳에서만 한다(결재중·결재완료=잠김, 반려·미상신=수정 가능). 화면이 스토어를 직접 읽어 잠금을 재구현하지 말 것. 근거: *"공문을 여기다 첨부하면은 문서들에 대한 기록이 남잖아요. 그러면 더 이상 수정이 안 돼요. 이건 문서 위조예요."*
- **채용시교육 탭은 `전체 / 미이수 / 이수` 3개다 (2026-07-30 회의)** — 구 `교육 건` 탭은 제거 확정이므로 **되살리지 말 것**. 채용시교육의 공문은 **이수 처리된 교육 건 단위**로 기안한다(§ 공문·결재).
- **기타교육 유형은 모집단이 정한다 — 두 화면을 같은 목록으로 합치지 말 것 (MUST, 2026-08-13)** — 별표4가 **제1호(근로자)와 제1호의2(관리감독자)를 다른 표로** 두고 두 표의 내용이 실제로 다르다. 종전에는 배열 하나(`DYEDU.ETC_TYPES`)를 두 화면이 공유해 **관리감독자 화면이 법에 없는 의무를 있다고 표시**하고 있었다.

  | | 근로자(제1호) | 관리감독자(제1호의2) |
  |---|---|---|
  | 작업내용 변경 시 | 일용·1주 이하 1h / 그 밖 2h | **2h 단일** |
  | 특별교육 | 일용·1주 이하 2h(별표5 라목39호 8h) / 그 밖 16h | **16h 단일**(단기간·간헐 2h) |
  | 건설업 기초 | 건설 일용근로자 4h | **없음** |
  | 채용 시 | 채용시교육 화면(`edu-hire`) 소관 | 8h — **현행 화면 범위 밖**(갭으로 표기) |

  - 창구는 **`DYEDU.etcTypes(supMode)` · `DYEDU.etcTypeInfo(label, supMode)` 둘뿐**이다. 화면이 `ETC_TYPES` 배열을 직접 참조하지 않는다. 그 모집단에 없는 유형이면 `etcTypeInfo` 가 **null** 을 준다 — 없는 의무의 시간을 돌려주지 않는다.
  - **셀렉트에서 감추는 것만으로는 부족하다** — 저장 경로(`doCreate`)에도 가드를 둔다. 전역 호출(`EDUE.pickType`)로 뚫린다.
  - 관리감독자 표에 **일용 갈래가 없는 것은 오탈자가 아니다** — 제1호의2 는 교육대상 열 자체가 없고 교육과정별 시간 하나만 정한다.
  - 건설업 기초를 뺀 근거는 별표4만이 아니라 **산안법 §31①**(`osh-31`)이다 — 의무 주체를 「건설업의 사업주」가 「건설 일용근로자를 채용할 때」로 한정한다. ⚠ **"관리감독자는 정의상 일용근로자가 아니다"라고 쓰지 말 것** — 산안법 §16①은 관리감독자를 **직위**로 정의하고 시행규칙 §27③도 "관리감독자의 **지위에 있는 사람**"이라 쓴다. 별도 신분이 아니다.
  - **별표4 비고3의 '2분의 1' 감면은 담양군에 걸리지 않는다** — 그 감면은 「영 별표1 **제1호**」(광산·원자력·항공·선박안전법 적용 사업)와 「상시근로자 50명 미만 도매·숙박음식점업」 전용이고 담양군은 제4호다. 교육시간은 별표4 값을 그대로 쓴다.
  - **교육시간 면제·감면(시행규칙 §27)은 넣지 않는다** — ①조문이 *"100분의 50 **범위에서** 면제**할 수 있다**"* 는 재량이고 ②판정 입력(전년도 산업재해 이력·사업장 단위·경력)이 시스템에 없으며 ③`oshr-27`·`osh-30` 이 DYLAW 스냅샷에 없어 근거 칩을 붙일 수 없다(§10). 발주처 확인 항목이다.

- **기타교육 유형은 법정 구분만 쓴다** — 구 5종(MSDS·직무교육·자체·기타)은 법정 교육구분이 아니라 폐지했다. 유형별 **법정 최소 교육시간**은 `DYEDU.ETC_TYPE_INFO` 에 두고 근거(`oshr-t4`)를 DYLAW 칩으로 함께 낸다 — 값을 고칠 때는 화면이 아니라 **별표4 조문을 먼저 열어본다**. 특별교육 **대상 작업 목록은 별표4가 아니라 별표5 제1호라목** 소관이다 — 2026-08-11 수집 완료(`oshr-t5`, 39종). **목록을 화면에 복사해 두지 말 것** — `DYLAW.specialEduWorks()` 가 조문 원문에서 파생하고 `DYEDU.ETC_TYPE_INFO['특별교육'].works` 는 그 getter다. 복사본을 두면 조문 개정 때 화면만 옛 목록으로 남는다. 화면은 목록을 **기준 표에서 펼쳐 보여주고, 특별교육 등록 폼에서는 복수 선택으로 필수 입력받는다**(`js/edu-etc.js`). 하나의 특별교육이 여러 호에 걸칠 수 있어 단일 선택으로 제한하지 않고, 목록에 없는 사례는 `기타 — 법령 매핑 대기` 와 사유로 저장해 등록 불가로 만들지 않는다. **선택은 적용 작업의 증빙일 뿐 판정에 쓰지 않는다** — 특별교육은 `perCourseCheck:false` 라 한 건만으로 법정 최소 미달을 찍지 않는다(별표4의 분할·단기간 단서 때문이다). 받지 않는 것은 **분할 이력·최초 작업일·단기간/간헐 여부** 셋이다.
  - 종전 이 자리에 있던 `DYPOLICY` `edu-annex5` 참조는 **삭제했다** — `js/policy-open.js` `ITEMS` 에 그런 id 가 없고 `selfCheck()` 는 page id 만 대조해 항목 id 오타를 못 잡는다. 있지도 않은 미결을 규칙에 적어 두면 다음 작업자가 정상 기능을 규칙 위반으로 오인해 되돌린다.
  - 데이터 회수는 `DYEDU.removeCourse(id)`(교육+신청+이수기록 연쇄) · `DYEDU.removeEnroll(courseId, deptId)`(신청+해당 부서 이수기록) · `DYEDU.removeWorkerFromCourse(courseId, workerId)`(근로자 1명분만 — 여러 명이 묶인 교육 건에서 통째 삭제를 막는다. 남은 대상자가 없을 때만 건까지 회수)만 사용하고, 확인 모달에 **회수되는 이수시간·건수를 반드시 명시**한다.
  - 수정 모달에서는 **대상자를 바꾸지 않는다** — 이미 붙은 이수기록과 어긋나므로 신청 취소 → 재신청(집합) 또는 삭제 → 재등록(자체·기타) 경로로만 처리한다.
  - **한 교육에 같은 부서를 두 번 담지 않는다 (MUST, 2026-08-13)** — 판정은 데이터 계층 `DYEDU.addEnroll()` 한 곳이고(중복이면 저장하지 않고 `false` 반환), 화면은 `DYEDU.hasEnroll(courseId, deptId)` 로 미리 물어 사유를 안내한다. 종전에는 무조건 쌓여 **한 사람이 같은 교육을 두 번 이수한 것**이 됐고, 이수 판정(완료율)과 공문 붙임 별지 인원이 함께 부풀었다. 등록은 행 단위인데 취소는 부서 단위라 단위까지 어긋났다. 인원·서명파일 정정은 **신청 취소 후 재등록** — 위 대상자 변경 규칙과 같은 경로다.
    - 화면에서 검사하지 말고 **데이터 계층에서 막는다** — 호출부가 7곳이고 그중 넷이 채용시교육 자동 생성 경로라, 화면마다 검사를 두면 반드시 빠뜨리는 곳이 생긴다.
    - '신청 N개 부서'는 행 수가 아니라 **서로 다른 부서 수**로 센다.
- 딥링크: `edu-status.html?dept={deptId}&short=1`(상세뷰+미달 필터, `?status=done|short|over|soon` 도 지원) · `edu-reg-detail.html?id={courseId}`(백링크는 body `data-back-href`).
- `layout.js` `renderSidebar`의 SNB 3뎁스(`item.section`) 로직과 `style.css`의 `.dy-sidebar-section`·`.dy-sidebar-sep`·`.dy-sidebar-item.is-nested`는 **이 그룹이 실사용처**다.
- 잔여 후속(2026-08-14 현재): ① 이수현황 대상자 행 → 보고서·제증명 개인 이수 확인서 **연계 미구현**(정의서가 현재형으로 약속하던 것을 사실로 정정해 둠) ② 관리감독자 **지정일 변경 이력** 미구현 — **본개발 보존 확정**(SCR-EDU-006 §6, 2026-08-14), 현행은 최신값만 보관 ③ 현업업무 종사자 지정·관리감독자 지정명단은 담양군 확인 항목(99_미결사항 §5 EXT-11·12). ※ 구 "관리감독자 채용시교육 수용 화면 미정"은 해소됨 — `edu-sup-hire.html`이 존재하고 SNB에 등재돼 있으며 SCR-EDU-002가 정의한다(2026-08-14 확인).

### 4-2. 부서 전수 이행 체크리스트 — `DEPTCHK` (2026-07-30 회의 신설)

"과 쭈르륵 사업소 쭈르륵 해가지고 돼 있는지 안 돼 있는지"를 훑는 표는 **`js/dept-check.js`(전역 `DEPTCHK`) 하나로만** 그린다. 현재 소비처는 **경영방침 게시 현황**(`menu.html?m=policy` 게시 탭)과 **의무 이행점검**(`menu.html?m=comply` 부서별 이행 현황, 산업/시민 2탭)이며, 회의 결론이 *"경영방침 원본은 계획에서, 게시 여부는 이행점검에서 확인"* 이라 두 화면이 같은 부서 축을 공유한다. 화면마다 표를 새로 짜지 말 것.

- `DEPTCHK.render({ key, ns, labels, statusLabel, rateLabel, placeLabel, evidenceLabel, onChange })` — `ns` 는 표의 버튼이 부를 전역 객체 이름이고, 화면 모듈이 `open/setStatus/addPhoto/mDelPhoto/save/delPhoto/remind` 위임 래퍼를 노출한다.
- **상태는 코드(`DONE`/`TODO`/`NA`)로 저장하고 문구는 화면이 정한다** — 이행점검은 `이행/미이행`, 경영방침은 `게시/미게시`로 읽혀야 하는데 표시 문구를 저장하면 라벨을 바꿀 때 데이터가 깨진다. 표시 라벨은 `DYV2.STATUS_TONE` 이 아는 어휘여야 색이 붙는다(§7).
- **`이행`으로 등록하려면 증빙과 위치가 있어야 하고, `해당없음`은 사유 없이 저장할 수 없다 (MUST)** — 발주처가 실제 종이 서식에서 지목한 실패 모드가 미이행이 아니라 **'해당 없음'으로 적고 빠져나가는 것**이었다(*"도시과 뭘 해야 되는데 원래 해야 돼요 해당 없어 나 이해가 안 되네"*). 필수(*) 표시만 하고 통과시키면 종이 서식의 문제를 그대로 옮긴다. 사유 없는 기존 데이터는 행에 `사유 미기재 — 확인 필요`로 계속 드러낸다.
- **부서 명단 갭을 감추지 않는다** — 회의에서 확인된 대상은 과·사업소 **39개**인데 `DYV2.ORG` 에는 11개뿐이라, 화면 상단에 `11 / 39개 등록 · 나머지 28개 명단 미확보`를 노출한다. 실제 명단을 받으면 **`DYV2.ORG` 한 곳만** 늘리면 두 화면이 자동으로 따라온다(§3). 부서를 지어내 채우지 말 것.

### 4-3. 위험성평가 — 조치 완료 확인 → 공문 기안 → 온나라 이관 (2026-07-31 신설)

부서가 개선조치 완료를 누르면 **아무도 확인하지 않고 평가까지 끝나던** 구멍을 메운 단계다. 근거: [`docs/planning/기획-위험성평가-공문-온나라이관-v1.md`](docs/planning/기획-위험성평가-공문-온나라이관-v1.md).

- **완료 확인은 `improvement.status` 를 건드리지 않는다 (MUST)** — `status === 'DONE'` 이분법으로 세는 곳이 8군데(`deptImpCount`·`assessmentProgress`·`IMPCARD`·`rsk-imp` 필터·`dashboard staffCounts`·`my-work` 2곳·`rsk-my`)라, 값을 하나 더 넣거나 완료를 확인 이후로 미루면 8곳이 동시에 어긋난다. 확인은 **별도 축 `improvement.confirm{state,by,at,reason,round}`** 이고 `state` 는 `WAIT|OK|RETURNED` 다. `confirm` 이 `undefined` 인 옛 데이터는 `WAIT` 로 파생되므로 **SKEY 범프가 필요 없다**.
  - **반려해도 `status` 를 되돌리지 않는다.** 되돌리면 `refreshAssessmentStatus` 가 `COMPLETED` 를 되돌리지 못해(early return) 평가가 잠긴 채 개선조치만 미완료인 모순이 생긴다. 실질 제재는 ① **공문 기안이 막힌다**(`DYRSK.docReady`) ② 담당자 화면에 반려 사유·회차가 상시 뜬다.
  - **사유 없는 반려는 저장되지 않는다** — 담당자가 무엇을 다시 해야 하는지 모르면 반려가 작동하지 않는다.
  - 재제출(`completeImprovement` 재호출)은 `confirm` 을 `WAIT` 로 초기화하고 `round` 를 올린다.
  - **반려 건의 '할 일' 판정은 `DYRSK.needsAction(m)` 한 곳에서만 한다 (MUST)** — 담당자 화면 3곳(`my-work`·`rsk-my`·`rsk-imp-detail`)이 `status !== 'DONE'` 으로 직접 판정하면 반려 건이 완료 탭에 묻혀 **재제출 수단이 사라진다**(실제 있었던 결함). 집계는 이 함수를 쓰지 않는다 — `status` 기준 그대로다.
- **순서를 뒤집지 말 것 (MUST)** — 1차 진입점은 `[공문 기안]` 이고 `[상신]` 은 **문서 미리보기 화면에만** 있다. 발주처가 교육의 결재 상신을 지운 이유가 *"공문 작성 단계 없이 결재만 올라가는 순서"* 였다(§4의 `SUBMIT_ENABLED=false` 와 같은 근거). 기안 폼에서 바로 상신하는 버튼을 만들지 말 것.
- **본문은 자동 생성하지 않는다** — *"이게 다 직접 타이핑 하셔야 돼"*(녹취 822). 자동 연결에 합의된 것은 **붙임파일뿐**이다. 시스템은 붙임을 엮고 수치를 세어 '삽입' 칩으로 건네줄 뿐이다.
- **`공문 및 결재` 대메뉴를 만들지 말 것** — 참석자1이 *"공문만 이렇게 분류만 해놔… 도움도 하나도 안 되는데"*(714~720)로 명시적으로 부정했다. 진입점은 전부 `rsk-list` 안이고, **새 HTML·새 page id 를 만들지 않는다**(그래서 `DYLAW.MAP` 추가가 불필요하다).
- **법령 근거는 뒷단에 붙일 수 없다** — 공문 기안·온나라 이관은 의무 이행의 *형식*이지 직접 이행 수단이 아니라 검증 6문 #2 를 통과하지 못한다(`'edu-approval': []` 선례). 지면의 `관련 근거` 절은 담당자가 `rsk-list` 근거 3건 중 **고른 것만** 찍고, 안 고르면 절 자체가 사라진다. 근거 문자열 하드코딩 금지(§10 — `js/edu-approval.js` 의 교육 조문 평문이 위반 선례).
- **`DYRSKDOC.lockOf(aid)` 는 모듈 전역 state 를 읽지 않는다** — 폐기된 `EDUAPV` 가 `state.year` 로 `lockOf` 의 target 키까지 좌우해, 이력 화면에서 과거 문서를 한 번 열면 잠금이 조용히 풀리는 결함이 있었다(그 교훈으로 `EDUDOC.lockOf` 도 인자만 본다). 인자만으로 판정한다.
- **`IMPCARD.open({canConfirm})` 기본값은 `false`** — 수시평가 조치 상세는 확인 주체가 외부 용역 안전관리자라 흐름이 다르다. 무심코 켜지 말 것.
- **조작 권한은 계층까지 본다 (MUST, 2026-08-03)** — `RSKLIST.canManage()` 는 `p.tier === 'staff' && p.deptId === 'safety'` 다. 부서(`deptId`)만 보면 **재난안전과장(super)이 주무관과 똑같은 전권**(생성·검수·전달·완료확인·공문 기안·상신)을 갖는다. 판정 기준은 `my-work.js` `canAct()` 와 같다 — 관리·감독은 조회, 실무는 담당자. 조회는 막지 않으므로 super 에게 막다른 길은 생기지 않는다(`문서 보기`·`부서별 조치`·`이력` 유지, 상태 변경만 차단).
  - **차단 문구는 `denyToast()` 한 곳에서만** — 14곳에 흩어 놓은 `'주관부서(재난안전과)만'` 은 재난안전과장 본인에게 거짓말이 된다. 같은 화면 `manageNote()` 와 같은 말을 해야 한다.
  - **안내 문구는 도달 가능한 행동만 약속한다** — 끝난 평가(`COMPLETED`)엔 제출·재촉 버튼이 0개이므로 '재촉할 수 있습니다'를 쓰지 않고, 표가 `a.depts` 만 그리므로 군수에게 '전 부서'가 아니라 '대상 부서 N개'라고 쓴다(§10 검증 6문 #6 과 같은 기준). 부서명 뒤 조사는 `josa()` 로 붙인다(`공공시설사업소는`/`담양읍은`).
  - **재촉 이력의 발신자는 `remindBy()`** — `by: '재난안전과'` 하드코딩이면 담양읍장이 눌러도 재난안전과가 보낸 것으로 남는다.
- **대시보드 시드의 상태·기한은 파생한다 (MUST, 2026-08-03)** — `dashboard.js` 는 `dueSt(due, fallback)` 로 상태를, `miniCal()` 로 달력을 `DYV2.today()`(§11)에서 만든다. 시드에 `st: '진행'` 을 박아 두면 시연일이 바뀌는 순간 '기한 초과 0건' 옆에 지난 기한 3건이 '진행'으로 남는다(실제 결함).
  - `SUPER_SEED[dept].tasks` 중 **기한이 지난 건수 = `DEPT_RATES[dept].overdue`** 여야 한다 — 군수 뷰의 부서별 표와 관리감독자 뷰의 `기한 초과` 카드가 같은 부서에 다른 숫자를 내면 안 된다(safety 0 · facility 3 · town_damyang 2).
  - **실집계와 시드를 같은 카드에 섞지 말 것** — 개선조치 지연은 `staffCounts().over` 실집계 하나로만 말한다. 시드로만 그리는 카드(에이징 등)는 `.dsh-seed-note` 로 밝힌다.
- **시연 투어 엔진은 `DYTOUR`(`js/tour-core.js`) 하나다 (2026-08-04 신설)** — 정기 `RSKTOUR`(`js/rsk-tour.js`, **9단계**) · 수시 `OCCTOUR`(`js/occ-tour.js`, **5단계**) · 안전보건교육 `EDUTOUR`(`js/edu-tour.js`, **4단계**) · 업무문서 `DOCTOUR`(`js/doc-tour.js`, **4단계**, 2026-08-13) 넷이 같은 엔진에 단계 정의만 얹는다. 다섯 번째 투어가 생겨도 엔진을 복제하지 말 것. 각 투어는 ① 전체 흐름 보드(`openFlow()`)와 ② 단계별 가이드(`start()`)를 제공한다.
  - 진입 바는 그 도메인의 대표 화면에서만 — `rsk-list`(정기)·`rsk-occ`(수시)는 `boot({bar:true})`, `my-work` 는 두 투어 모두 `boot()`(바 없이 진행 중일 때만 패널). 여러 도메인이 모이는 화면에 도메인 바를 상시 노출하지 않는다.
  - **패널은 한 번에 하나** — `my-work` 는 두 투어를 함께 로드하므로 `DYTOUR` 가 인스턴스를 등록해 두고 `go()`·`boot()` 에서 다른 투어를 끈다(`stopOthers`). 두 패널이 겹치면 어느 쪽 안내인지 알 수 없다.
  - **관점(뷰) 3종 — 로그인 페르소나가 기본값을 정한다 (2026-08-04)**. 단계마다 `persona()` 가 이미 선언돼 있어 그 축으로 거른다.
    | 뷰 | 대상 | 동작 |
    |---|---|---|
    | `all` 전체 흐름 | 주관부서 담당자(`cfg.ownerPersona`) — 발표자 자리 | 9단계 전부 · 페르소나 자동 전환 |
    | `mine` 내 역할 | 부서 담당자 | 내 단계에만 커서가 머문다 · **전환하지 않는다** |
    | `read` 조회 전용 | 군수·과장·소장(`tier !== 'staff'`) | 액션 버튼 없음 |
    - **남의 단계를 지우지 말 것** — `.dy-tour-step.other` 로 흐리게 남긴다. 지우면 전체 그림과 "내가 왜 기다리는지"를 잃는다(`myDeptPanel` 의 `mine:false` 와 같은 원칙).
    - **`mine` 뷰에서 선행이 안 끝났으면 '아직 내 차례가 아닙니다'로 정직하게 멈춘다**(`.dy-tour-wait` + 막고 있는 단계와 그 주체를 나열). 실제 업무 감각이 그렇고, 이게 부서 담당자 시연의 핵심 메시지다.
    - **`mine`·`read` 에서는 `applyPersona()` 를 부르지 않는다** — 바꾸는 순간 '내 역할'이 아니게 된다.
    - 주체가 하나뿐인 투어(교육)는 `multiPersona()` 가 false 라 토글을 숨기고 `mine` 을 `all` 로 수렴시킨다.
    - **조회 범위 밖 데이터를 패널이 노출하지 말 것 (MUST)** — `safeNote()` 가 `scopeDept` 를 보고 범위 밖이면 `'조회 범위 밖 부서입니다'` 로 막는다. 화면(§12)에서는 안 보이는 남의 부서 건을 패널이 문서번호까지 읊으면 그게 곧 범위 위반이다(실제로 공공시설사업소장에게 환경과 `OCC-2026-02` 내용이 노출됐다). 부서 단계뿐 아니라 **그 건을 다루는 주관부서 단계에도 `scopeDept` 를 단다**.
    - **`read` 뷰에서는 `whyMissing()` 을 띄우지 않는다** — 조회하라고 해 놓고 "관점을 바꾸세요"라고 하면 모순이다. `mine` 의 대기 상태도 같다(설명은 `.dy-tour-wait` 가 한다).
    - **`read` 뷰에는 파괴적 버튼을 내지 않는다** — 교육 바의 `[시연 초기화]` 는 숨기고 `resetDemo`·`confirmReset` 에도 가드를 둔다(버튼만 숨기면 전역 호출로 뚫린다).
    - **진입 바 문구·버튼도 관점을 따른다** — `scopeSuffix()`·`scopeHint()`. 부서 담당자에게 '9단계'라고 하면 자기가 9번 뭘 해야 하는 줄 안다(실제 하는 건 2단계). 조회 전용의 버튼은 `흐름 따라보기` 다.
    - **사용자에게 파일명을 보여주지 않는다** — `cfg.pageLabels` 로 `my-work.html` → `내 할일`.
    - **`boot()` 에서 커서를 관점 안으로 스냅한다** — 페르소나가 바뀌면 옛 커서가 남의 단계에 남아 '내 역할'인데 "관점을 바꿔 진행합니다"가 뜬다.
    - **부서 판정은 로그인 부서를 먼저 본다** — `rsk-tour.js` `demoDept()` 는 `PREF_DEPT` 보다 접속자의 `deptId` 를 우선한다. 대상 부서가 아니어도 **내 부서를 가리키고** `note()` 가 "…은 2026년 평가 대상 부서가 아닙니다"로 말한다(`rsk-list` `manageNote()` 와 같은 말).
  - **수시는 흐름의 방향이 반대다** — 정기는 주관부서가 열어 부서로 내려보내고, 수시는 **부서가 사유가 생겨 올리고** 주관부서가 안전관리자 검토를 붙인다. 그래서 1단계 주체가 부서 담당자다. 완료 확인·공문 단계는 없다(§4-3 — 수시는 확인 주체가 외부 용역 안전관리자다).
  - **수시 추적 대상은 그 부서의 가장 최근 1건**이다(`O()`). 정기(연 1건)와 달리 여러 건이 쌓이므로 어느 건을 따라가는지 보드·패널에 밝힌다. 대상 id 를 저장하지 않는 이유는 진행 판정과 같다 — 저장하면 초기화·재시연 때 옛 id 를 가리킨 채 멈춘다.
  - **진행 판정은 저장하지 않는다 (MUST)** — `STEPS[].done()` 은 전부 `DYRSK` 파생이다. 커서 인덱스로 판정하면(EDUTOUR 방식) 뒤로 갈 때 완료 표시가 풀리고, 가이드를 끄고 손으로 처리한 건이 안 잡힌다. 자동 진행도 이벤트 훅이 아니라 **완료 판정의 변화**(`doneSig()`)로 감지한다 — 저장 경로가 화면마다 갈려 있어(③은 2경로, ⑥도 2경로) 훅 방식은 반드시 새는 곳이 생긴다.
  - **`validRows()` 는 `DYRSK.deliverFromReview` 의 행 채택 조건과 같은 식이어야 한다** — 다르면 "4단계 완료"라 표시한 뒤 5단계에서 전 부서가 '지적사항 없음'으로 빠진다.
  - **앵커는 `data-tour` 속성으로만** (정기 `rsk-create`·`rsk-survey`·`rsk-report`·`rsk-deliver`·`rsk-confirm`·`rsk-doc` / 수시 `occ-create`·`occ-imp`·`occ-review` / 공용 `mw-rskreport`·`mw-improve`). `render()` 가 `innerHTML` 을 통째로 갈아끼우므로 **DOM 참조를 보관하지 말 것** — 매번 새로 조회하는 idempotent `applyFocus()` 를 `MutationObserver(childList+subtree)` 가 부른다.
  - **버튼을 못 찾으면 이유를 말한다**(`whyMissing()`) — 관점 불일치·조회 범위 밖·선행 미완·이미 끝난 단계. 아무 데도 안 가리키는 투어는 안내가 아니라 소음이다.
  - **페르소나 전환은 투어가 한다** — 주관부서(박안전)와 부서 담당자를 오가는 흐름이라 `DYROLE.set()` 대신 `ROLE_KEY` 만 바꾸고 목적 화면으로 직접 이동한다(`DYROLE.set` 은 자체 reload 로 목적지를 덮어쓴다).
  - **시연 대상 부서를 하드코딩하지 않는다** — `demoDept()` 가 실제 `a.depts` 에서 고른다(부서 담당자 페르소나가 있는 `water`·`env` 우선). 대상 부서가 많으면 공문 기안 조건(`docReady` = 전 부서 확인 완료)을 채우느라 시연이 끝나지 않으므로, 1단계 모달에 한 곳만 남기는 도우미(`pickDemoDept()`)를 둔다.
  - **CSS 는 교육 투어와 공유한다** — `css/v2.css` 의 `.edu-tour-*`·`.edu-demo-*` 규칙에 중립 이름 `.dy-tour-*`·`.dy-demo-*` 를 **쉼표로 병기**했다(§7 계열 신설 금지). 단계 칩 줄만 `.dy-tour-steps` 로 분리한다 — 교육은 4단 고정 그리드이고 위험성평가는 9단계라 유연 배치가 필요하다(교육 투어 시각 회귀 0, 실측 확인). 흐름 보드의 단계 목록은 `.rl-my-step` 계열을 그대로 쓴다.
  - **교육 투어(EDUTOUR)도 2026-08-04 에 `DYTOUR` 로 이관했다 — 공개 API 12개는 계약이라 그대로다** (`boot`·`start`·`stop`·`go`·`next`·`prev`·`action`·`onEvent`·`restoreAndStart`·`resetDemo`·`confirmReset`·`active`). edu 화면 HTML 9곳이 `EDUTOUR.boot()` 를 **인자 없이** 부르므로 파사드가 기본값을 `{bar:true}` 로 맞춘다(엔진 기본은 바 없음). `onEvent()` 는 무동작이다 — 자동 진행을 `done()` 변화로 감지하므로 훅이 필요 없고, 호출부 5곳(`edu-reg.js` 3 · `edu-reg-detail.js` 2)은 계약이라 그대로 둔다.
    - 교육도 공문 단계가 붙어 **5단계**가 되면서 `cfg.stepsClass` 를 비웠다 — 위험성평가와 같은 유연 배치를 쓴다(종전 4열 고정 `is-grid4` 는 해제).
    - 데이터 복원(`restoreAndStart`·`resetDemo`·`confirmReset`)과 "항상 1단계부터 시작"은 교육 고유 동작이라 파사드가 `start()` 를 덮어쓴다 — 발표자가 아는 흐름을 바꾸지 않기 위해서다.
    - **추적 대상 폴백은 '접수 중'이 아니라 '가장 최근 집합교육'이다** — OPEN 만 보면 3단계 종료 처리 순간 대상을 잃어 전 단계 체크가 한꺼번에 풀린다(실제로 낸 결함).
  - **흐름 보드 모달에는 단계 안내(`modalGuide`)를 주입하지 않는다** — 보드는 전체 지도이지 특정 단계의 작업 모달이 아니다. `syncModalState()` 가 `.dy-tour-flow` 를 보고 건너뛴다.
- 인쇄에서 빼야 할 편집 크롬은 **`.pdf-noprint`** 를 단다. 인쇄 규칙에 모듈별 클래스 이름을 추가하지 말 것(다음 모듈에서 또 잊는다).
- **지면은 표준 공문 서식이다 (MUST)** — 「행정업무의 운영 및 혁신에 관한 규정 시행규칙」 별지 제1호서식(기안문·시행문)은 **전 행정기관 공통**이라 담양군 고유 양식이 따로 없다. `.pdf-gm` modifier 로 그린다: 행정기관명 → 수신/제목 → 본문(`1.→가.→1)`) → 붙임+`끝.` → 발신명의·관인 → **하단** 기안자/검토자/결재권자 서명 → 시행/접수 → 기관정보. **보고서 서식(절 구분·KPI 카드)을 쓰지 말 것** — 초판이 그 오류를 냈다. 결재란은 상단이 아니라 **하단**이고, 큰 표는 본문이 아니라 **붙임 별지**(`.pdf-gm-annex`)로 뺀다.
  - **내부결재 / 외부발송은 지면이 실제로 다르다** — 내부결재는 수신 `내부결재`·발신명의·관인·접수란·기관정보 없음. 하나로 합치지 말 것.
  - **이 규정을 근거 칩으로 인용하지 않는다** — DYLAW 스냅샷에 없다. 서식을 따르는 것과 조문을 인용하는 것은 다른 문제다(§10).
- **미확정 갭은 화면에 드러낸다** — 담양군 고유값(처리과 기호·주소·전화·팩스·전자우편·공개구분·관인)은 지면에 `미등록` · 문서번호는 `(온나라에서 부여)`·`임시 채번` 라벨 유지 · 수신 부서 명단 11/39 · 시행규칙 §37 미수집이라 *"법정 기록·보존 항목"* 이라고 쓰지 않는다 · 온나라 연동 미구현(회신은 시연).

### 5. 업무문서 — 세 화면의 축이 다르다 (2026-08-13 리뉴얼)

`업무문서` GNB 는 **기준문서함 · 업무 목록 · 이행 목록** 3개이고, 뒤 둘은 같은 문서 데이터를 공유하지만 **기본 단위와 목적이 다르다**. 근거: [`기획-업무문서-이행목록-업무목록-UX설계-v1.md`](docs/planning/기획-업무문서-이행목록-업무목록-UX설계-v1.md).

| 화면 | 파일 | 기본 단위 | 묻는 것 |
|---|---|---|---|
| 업무 목록 | `docs-preset.html` (`DOCLIST`) | **문서 1건** | 그 문서가 어디 있나 |
| 이행 목록 | `docs-exec.html` (`DOCEXEC`) | **법정 이행항목 1개**(78) | 이 의무의 증빙이 갖춰졌나 |

- **`docs-preset.html` 의 'preset' 은 옛 이름이다** — 전년도 프리셋 기능은 이 화면이 아니라 **이행 목록**에 있다. URL 은 `stats.html`·`docs-archive.html`·`my-work.js`·`menu.js` 의 기존 딥링크 호환 때문에 유지한다. 파일명으로 기능을 추측하지 말 것.
- **`?ver=v1|v2` 는 폐기**(분류 버전 토글 삭제). `?menu=` 는 현행 업무문서 426건이 `menuKey` 를 갖고 있어 **살아 있다** — `stats.html` 의 9개 대메뉴 링크가 전부 같은 전체 목록으로 떨어지지 않게 하기 위해서다.
- **`js/sets-data-v2.js` 는 삭제 금지** — 현행 업무문서 426건(`DY_DOCS_V2`)의 유일한 출처이고, 업무 목록·문서 상세·`my-work`·`stats`·`docs-archive` 가 전부 쓴다.
- **`js/setlist.js`(`DYSETLIST`)는 현재 살아 있는 소비처가 없다** — [`menu.js:1845`](js/menu.js:1845)가 `#pane-docs` 를 찾지만 그 요소는 이미 제거돼 있어(각 메뉴의 [문서] 탭 폐지) 항상 early return 한다. **그래도 지우지 않는다**: 세트 테이블(`SL_HEAD` 7열 ↔ `docRow` 1:1)은 세트 축을 되살릴 때의 유일한 구현이고, 지우면 그 축이 코드에서 사라진다. 되살릴 일이 없다고 판단되면 `docs/archive/` 이동이 아니라 **별도 커밋으로 근거를 남기고** 제거한다.
- **`DY_DOCS_V2` 는 426건이다** — `DY_DOC_SET_V2` 매핑 키 429 중 완전동일 3쌍(`V2-270`·`V2-277`·`V2-312`)이 통합돼 있다. 통합·집계는 **배열을 정본**으로 삼는다(키로 세면 없는 문서 3건이 생긴다). §14-4 의 "429문서" 표기는 매핑 키 수다.
- **문서 상세는 축에 따라 갈린다** — `js/doc-detail-ledger.js`(`DOCDET`)가 새 축(원장·등록분)을 그리고, 그 밖의 id 는 기존 `js/doc-detail.js`(세트·PDCA 축)로 넘어간다. **기존 화면은 손대지 않는다** — `docs-archive`·`menu.js` 문서탭·`screen-definitions` 가 그대로 쓴다.

**데이터·판정 단일 출처**

| 파일 | 전역 | 성격 |
|---|---|---|
| `js/doc-taxonomy-data.js` | `DYDOCT` | **생성물** — 이행항목 80 · 업무단계 177 (2026-08-18 분류기준 v2 재생성) |
| `js/doc-history-data.js` | `DYDOCH` | **생성물** — 2025 문서 원장 **20개 부서 57,765건** + 매핑 (2026-08-25 대시보드 xlsx 58,223행 적재). 생성기 `tools/build-ledger-2025.py` |
| `js/doc-seed-2026.js` | `DYDOC2026` | **생성물** — 2026 시연 시드(문서 82 · 진행상태 48단계). 생성기 `tools/build-doc-seed-2026.py` |
| `js/doc-progress.js` | `DYDOCS` | 상태·진행률·문서 축 판정 |
| `js/doc-pick.js` | `DYPICK` | 이행항목·업무단계 **2단 선택기** |

**이행항목·업무단계를 고르는 UI 는 `DYPICK` 으로만 (MUST)** — 고를 것이 항목 80개·단계 177개다. 드롭다운은 스크롤로 찾으라는 말이고, 80개 아코디언은 원하는 것을 만나기까지 계속 굴려야 한다. 채용 사이트(사람인·잡코리아)의 직무 선택이 답을 냈다 — **좌 대분류 / 우 소분류, 칸마다 따로 스크롤, 칸 위에 인라인 검색, 아래에 고른 것 칩.** 업무 목록 상세 조건과 업무 업로드 STEP2 가 **같은 선택기**를 쓴다(담당자가 두 번 배우지 않게).
- 선택값은 **소비처가 들고 있고** `DYPICK` 은 그리기와 검색어만 맡는다. 상태를 둘로 두면 어느 쪽이 진짜인지 알 수 없어진다.
- `render(cfg)` 의 `ns` 로 콜백 경로를 주고 `(kind, a, b)` 로 받는다 — `item`·`stage`·`all`·`clear`·`redraw`.
- 트리가 든 모달은 `.modal:has(.dyp)` 로 860px 까지 넓힌다. 500px 에서는 칸이 205px 로 눌려 긴 업무단계명이 네 줄로 접힌다.

**화면 문구는 담당자 말로 쓴다 (2026-08-13)** — 설계 용어를 화면에 내보내지 않는다. `증빙 문서 축`·`원자료 취합상태에서 파생`·`이행항목 축 없음`·`원장 미보유` 같은 말은 정의서와 주석에만 두고, 화면에는 `법으로 해야 하는 일`·`할 일`·`서류`·`기록 없음`·`이 목록 밖 문서`로 낸다. 안내는 접어도 남는 한 줄이 **지금 상태**여야 한다(§14-12).

- **생성물은 손으로 고치지 않는다** — 원본 CSV 를 고치고 `tools/build-docs-seed.py` 를 다시 돌린다.

- **2026 실적은 시연 시드다 — 실측과 섞지 말 것 (MUST, 2026-08-18 발주처 확정)** — 2025 는 문서 원장 **실측**(20개 부서 57,765건)이고 2026 은 아직 일어나지 않은 일이라 어디에서도 받을 수 없다. 문서 0건으로 두면 177단계가 전부 미이행이라 «시스템이 비었다»로 읽혀, 2025 실측에서 **규칙으로** 만든 시드를 얹었다(`tools/build-doc-seed-2026.py`).
  - **규칙이 곧 근거다** — ① 2025 에 실적이 있던 단계(135)만 후보 ② 회차의 창이 열리지 않았으면 만들지 않는다(문서 날짜 전부 기준일 이전) ③ 채택은 해시 순서(손으로 고르지 않는다) ④ 제목은 2025 문서에서 이어받고 연도 표기만 한 해 민다. 「받아야 할 외부 자료를 지어내는 것」(부서 명단·지정명단 등)과 다르다 — 그쪽은 여전히 금지다.
  - **시드임을 네 곳에 드러낸다** — `origin:'seed26'` · 문서 목록 칩 · 문서 상세 「데이터 구분」 · 연도별 안내. 감추면 «담양군이 올해 이만큼 했다»로 읽힌다.
  - **주기 파싱은 생성기와 `cmp-core.js` 가 한 글자도 달라선 안 된다** — 다르면 시드가 만든 회차와 화면이 세는 회차가 어긋나 «충족인데 미이행»이 생긴다(실제로 `2개월에 1회` 에서 한 번 갈렸다). 규칙을 고칠 때 두 파일을 함께 고치고 전건 대조한다.
  - **검증값은 고정 건수가 아니라 «깨지면 안 되는 성질»이다** — 2025 실적 없는 단계 시드 0 · 기준일 이후 날짜 0 · 회차 초과 0 · id 중복 0 · 미이행 비율 ≥60%. 분류 축이 늘면 분포는 따라 움직여야지 그 자리에서 막히면 안 된다(§5 2026-08-18 EXPECT 사고).
  - **시드가 옮긴 원본을 또 옮기지 못하게 한다** — 시드가 `seedOf`(2025 원본 id)를 남기고 `doc-progress.js` 가 `presetOf` 로 투영해, 「작년 문서 불러오기」·「올해 이어받기」의 **기존 중복 검사가 무수정으로** 이 경로까지 막는다. 안 막으면 그 단계만 회차를 넘어 잘못 «충족»이 뜬다.
  - **기준연도 기본값은 `DYDOCS` 상단 `DEMO_YEAR = 2025` 한 줄**이다(§11 `DEMO_TODAY` 와 같은 계약). `defaultYear()` 의 «진행이 있는 최신 연도» 파생에 맡기면 시드를 얹는 순간 **화면 5곳**(이행 관리·문서 목록·이행 목록·업무 업로드·업무문서 투어)의 기본 연도가 한꺼번에 2026 으로 움직인다. 빈 값으로 두면 파생으로 돌아간다.
  - **`DEMO_TODAY` 를 옮기면 시드를 다시 생성한다** — 회차 기한만 다시 계산되고 시드 문서 수는 고정이라 진행이 실제보다 뒤처져 보인다. `DYDOCS.seedStale()` 이 콘솔 경고를 낸다(자동 보정은 하지 않는다 — 그러면 시드가 규칙 밖에서 움직인다).

- **등록이 끝나면 만든 문서로 가는 길을 준다 — `DOCUP.saved()` 하나로 (MUST, 2026-08-25)** — 등록·불러오기·프리셋 세 경로가 전부 **토스트만 내고 끝나** 방금 만든 문서가 무엇인지·어디서 보는지 알 방법이 없었다(목록으로 돌아가 검색해야 했다). 문서 상세의 「올해 이어받기」만 예외적으로 상세로 이동하고 있어, 같은 일을 하는 네 경로가 서로 다르게 끝났다.
  - **자동으로 튕기지 않는다** — 이행 관리에서 불러온 사람은 그 화면에 남고 싶을 수 있어 강제 이동하면 하던 일의 맥락을 잃는다. 대신 «갈 수 있게» 한다: 1건이면 주 버튼이 곧 그 문서이고, 여러 건이면 목록에서 고른다.
  - **화면마다 결과 화면을 새로 짜지 않는다** — 네 화면이 모두 `js/doc-upload.js` 를 로드하므로 거기 한 곳에 둔다(§7).
  - **`backTo` 로 온 화면을 넘긴다** — 종전 `doc-detail` 의 `back` 은 **쿼리스트링만** 담고 목적지가 `docs-preset.html` 고정이라, 이행 관리·이행 목록에서 문서를 열고 돌아가면 **온 적 없는 업무 목록**으로 튕겼다. 문서를 여는 화면이 넷이므로 목적지도 함께 받되 **허용 목록(`BACKS`)으로 제한한다** — `back` 은 URL 파라미터라 그대로 `location.href` 에 넣으면 외부로 튕기는 통로가 된다.
  - **복사 범위를 사실대로 말한다** — 「제목·수발신자만 복사되었습니다」라고 토스트를 냈는데 실제로는 업무단계·첨부까지 가져오고 있었다. 무엇이 딸려왔는지 모르면 담당자가 첨부를 또 올린다.
  - **같은 일을 하는 두 경로가 갈리지 않게 한다** — 불러오기(`CMPST.pullRun`)가 수발신 **방향(`dir`)만 안 넘겨** 상대 기관은 따라오는데 방향이 «미상»이 됐다. 프리셋(`DOCUP.runPreset`)은 넘기고 있었다.

- **쪽 나눔은 `DYV2.pager()` 하나로 (MUST, 2026-08-25)** — 화면마다 페이저를 다시 짜지 않는다(§7). 이행 관리의 단계별 문서 목록이 **전건을 한 번에** 그리고 있었다 — 원장 적재 후 한 단계에 최대 **3,730건**이고 179단계 중 130개가 10건을 넘는다. 그 화면은 문서를 «보는» 곳이 아니라 스크롤로 잃어버리는 곳이 된다.
  - **쪽 번호를 입력으로도 받는다** — 373쪽짜리 목록에서 「‹ 이전」만으로 1쪽에 가려면 372번을 눌러야 한다. 번호 칸에 직접 쳐 넣고 Enter, 처음·끝 버튼(`«` `»`)이 그 위의 흔한 경우를 덮는다. **범위 밖·오타는 양끝으로 붙인다** — 클램프하지 않으면 오타 한 번에 빈 화면이 된다.
  - **한 쪽뿐이면 페이저 대신 건수만** 남긴다. 다음 쪽이 없는데 화살표를 내면 «더 있는데 안 보여준다»로 읽힌다.
  - **조건이 바뀌면 1쪽으로** 돌아간다. 3쪽에 머물면 결과가 2쪽뿐일 때 빈 화면이 된다. **단계·연도를 바꿔도 조건을 비운다** — 다른 목록이므로 들고 넘어가면 «검색은 걸려 있는데 왜 0건이지»가 된다.
  - 상세에 곁들이는 목록은 **10건**, 목록 전용 화면은 25건이다.
  - **필터 옵션은 지금 그 목록에 실재하는 값만** 낸다(건수 포함). 고르면 0건이 되는 선택지는 «있는데 안 보여준다»로 읽힌다. 선택지가 하나뿐이면 드롭다운을 두지 않는다(§12).

- **문서 제목 수정은 등록분만 (MUST, 2026-08-25)** — 판정은 `canEditDoc(doc) && doc.origin === 'user'` 다. `canEditDoc` 만으로는 부족하다 — 그 함수는 원장 문서에도 재난안전과 담당자를 허용하는데 그건 «잘못 붙은 분류를 고치는»(remap) 권한이고, **원장 제목은 실제 온나라 문서의 제목이라 이 시스템이 고칠 값이 아니다.** 실제로 `updateDocument` 가 「시드 문서는 수정할 수 없습니다」로 거절하므로, 버튼만 내면 **확인까지 시키고 거절하는 버튼**이 된다(삭제 버튼이 `origin` 을 함께 보는 것과 같은 근거).
  - **버튼만 숨기면 전역 호출로 뚫린다** — 진입(`editTitle`)과 저장(`saveTitle`) 양쪽에 가드를 둔다.
  - 못 하는 이유를 밝힌다(`titleDenyNote()`) — 버튼이 없으면 「왜 없지」가 된다.
  - 필요한 이유: 지난 연도 문서를 불러오면 제목의 연도 표기가 어긋난 채 남는다(치환은 「작년→올해」 두 값만 한다). 자동으로 판단할 수 없는 값이라 사람이 고쳐야 하는데 **고칠 수단이 아예 없었다**.

- **표 내려받기는 `DYV2.tableFile` 하나로 (MUST)** — 화면마다 직렬화를 다시 짜지 않는다. UTF-8 **BOM** CSV 다(빌드·라이브러리가 없어 진짜 .xlsx 를 만들 수 없다. BOM 이 없으면 엑셀이 CP949 로 읽어 한글이 전부 깨진다). 버튼 라벨은 담당자 어휘대로 `엑셀 내려받기` 로 두되 **파일명(.csv)·안내·토스트가 형식을 밝힌다**.
  - 방어 세 가지가 헬퍼에 모여 있다 — **수식 인젝션**(`= + @` 시작값은 작은따옴표로 고정, 음수는 예외) · **앞자리 0 보존** · **쉼표·따옴표·줄바꿈·세미콜론**.
  - **내보내는 것은 «지금 조건에 맞는 전건»**이지 «화면에 그려진 행»이 아니다. 표시 한도(관리대상 25행)는 읽기 편의이지 조회 조건이 아니라, 잘라 내면 조건에 맞는 자료를 조용히 버린다. 불변식은 **필터 바 건수 = 파일 데이터 행 수** 하나다.
  - **파일이 화면보다 더 아는 척하지 않는다** — 화면이 「분모 확인 필요」라 말하는 값을 파일에서 백분율로 바꾸지 않는다(초판에 실제로 냈던 결함). 표만 떼어 본 사람에게는 그 수치가 측정값이 된다.
  - 파일 끝 꼬리말(화면·기준연도·조건·행 수·기준일·원자료 구성·자료 한계)의 **건수는 세어서 쓴다**. 문장에 박으면 출처가 하나 늘 때마다 파일만 옛 수치로 남는다.

- **검증값은 CSV 를 갈아끼울 때 함께 올린다 (2026-08-18 사고)** — `EXPECT` 가 `items:78/stages:168` 로 굳어 있어, 분류기준 CSV 가 v2(80/177)로 갱신됐는데도 **재생성이 막혀 9개 단계가 화면에서 통째로 빠져 있었다**. 빠진 것은 `FAC-15`(공중이용시설 개별 안전 관계 법령 — 소방·전기·실내공기질) 4단계 · `FAC-16`(유해화학물질 취급시설 안전관리) 4단계 · `OSH-11-03`(MSDS) 1단계이고, 하필 **5개 부서 분류 1차 결과가 «문서 0건 불이행 의심»으로 지목한 항목들**이라 가장 위험한 미이행이 누락 점검에 뜨지 않았다.
  - 검증값이 막아 준 것은 «조용한 데이터 변형»이지 «정당한 확장»이 아니다. 스크립트가 불일치를 보고하면 **원본이 왜 달라졌는지 확인한 뒤 EXPECT 를 갱신**한다. 문서 원장 쪽 6종(`history_rows`·`docs`·`dup_rows`·`multi_stage_docs`·`max_stages`·`unmapped_rows`)이 그대로면 분류 축만 넓어진 안전한 확장이다.
  - **화면 수치는 하드코딩하지 않는다** — 카운트는 전부 `DYDOCT`/`DYDOCS` 파생이라 재생성만으로 80/177 이 따라온다. 문구에 숫자를 박으면 이런 확장 때마다 거짓말이 된다. 스크립트는 §9-3 검증값 9종(80·177·4,082·3,830·24·210·11·35·42)이 하나라도 어긋나면 **파일을 쓰지 않고 종료**한다.
- **2025년 진행상태는 합성이 아니라 `취합상태` 컬럼의 투영**이다(확인됨→완료 / 보완필요→진행중 / 취합대상→미이행 / 조건부→해당없음). 이 파생은 문서 존재 여부와 정합한다(확인됨119+보완필요16 = 문서 있는 단계 135, 취합대상26+조건부7 = 문서 없는 단계 33).
- **상태 전이는 `DYDOCS.transition()` 한 곳에서만** — 화면이 스토어를 직접 쓰지 않는다. 업로드 직후는 반드시 `진행중`이고, `완료`는 주관부서 담당자의 증빙 확인 뒤에만 붙는다. 완료된 건의 파일·매핑을 고치면 **진행중으로 환원**하고 재확인을 받는다.
- **`해당없음`은 사유 없이 저장되지 않는다** — §4-2 와 같은 근거(종이 서식의 실패 모드가 '해당 없음으로 적고 빠져나가는 것'이었다).
- **완료 확인자 = 재난안전과 담당자**(`DYROLE.current().tier === 'staff' && deptId === OWNER_DEPT`). ⚠ **`DYROLE.tier()` 는 `{label,tone,who,law,hideNav}` 서술 객체**라 `=== 'staff'` 비교가 조용히 false 가 된다 — 계층 id 는 `current().tier` 다(구현 중 실제로 낸 결함).
- **이 화면에는 부서 조회 범위(`scope()`)를 걸지 않는다 — 다만 이유가 바뀌었다 (2026-08-25)** — 종전 근거는 «원자료가 재난안전과 문서 원장이라 걸면 그 외 부서 담당자에게 0건이 된다» 였다. 원장이 20개 부서를 갖게 되면서 **그 근거는 사라졌다**. 지금 걸지 않는 이유는 **좁힐지가 담양군이 정할 정책**이기 때문이다(§12 의 «아직 권한 축이 없는 화면 — 발주처 정책 확정 후» 범주). 조회는 열고 **조작만 `canAct()`** 로 막는다. 근거가 바뀌었으므로 옛 문장을 근거로 «자료가 없어서»라고 다시 쓰지 말 것.
- **'미분류'는 원장에서 분류가 빠진 문서**(2025 기준 22,651건)이지 이행항목 축이 애초에 없는 현행 업무문서 426건이 아니다. 둘을 합치면 요약의 '미분류' 링크가 엉뚱한 수로 열린다. 매핑 상태는 `mapped` / `unmapped`(원장 교정 대상) / `noaxis`(현행 업무문서) 3값이다. **건수를 이 규칙에 적지 않는다** — 원장을 갈아끼울 때마다 규칙만 옛 수치로 남는다(§13 «개수를 규칙에 적지 않는다»와 같은 이유). 화면은 전부 파생값이다.
- **미분류·분류 제외·분류 확인 셋은 다른 것이다 (MUST, 2026-08-25)** — 한 칩으로 묶으면 «남은 할 일»이 부풀거나 «확인이 끝났다»로 잘못 읽힌다.
  | 값 | 뜻 | 이행 판정 |
  |---|---|---|
  | **미분류** (`!mapped && !excluded`) | 어느 할 일의 증빙인지 아직 안 붙었다 — 원장 교정 대상 | 들어가지 않는다 |
  | **분류 제외** (`excluded`) | 타 기관 소관·자치사무라 분류 단계에서 뺐다 — 판단이 끝났다 | 들어가지 않는다(단계가 애초에 없다) |
  | **분류 확인** (`mapConf === 'weak'`) | 할 일은 붙었는데 원장이 «애매»로 표시했다 | **들어간다** — 그래서 눈에 보여야 한다 |
  `mapConf` 의 «모호»(`vague`)는 대개 미분류와 겹쳐 따로 찍지 않는다(실측 24,880 중 24,677 이 미매핑).

**부서별 이행률은 분모가 없다 — 지어내지 말 것 (MUST, 2026-08-25)**

원장이 20개 부서를 갖게 되면서 «어느 부서가 무슨 서류를 갖고 있나»는 실측이 됐지만, **«그 부서가 무슨 할 일을 맡는가»는 여전히 없다.** 지금 쓰는 「적용(추정)」은 분류기준의 적용대상 문구에서 부서 이름을 찾은 값인데, 실측 대조 결과 **20개 부서 중 19개에서 «추정 밖인데 서류가 있는 할 일»이 추정 안쪽보다 많았다**(추정 23~48 vs 추정 밖 51~90). 재난안전과만 예외인데, 그건 「군 전체」·「재난안전과 주관」 문구가 다 걸리기 때문이다.

- **분모가 그 정도로 틀리면 비율을 내지 않는다** — `DYCMP.deptStats()` 의 `usable` 이 판정하고(`extra <= applied && docs > 0`), 못 낼 때는 「분모 확인 필요」로 **왜 못 내는지**를 말한다. 30% 를 칩으로 찍으면 담당자에게는 그것이 측정값이 된다.
- **문서가 0건인 부서도 비율을 내지 않는다** — 회계과·담양읍처럼 원장에 그 이름이 없는 부서에 「0%」를 찍으면 «아무것도 안 했다»가 된다. 「원장에 문서 없음」으로 둔다(§14-2 «대상이 아닌 부서를 완료로 세지 않는다»와 같은 이유).
- **표는 실측을 앞에 둔다** — `서류가 붙은 할 일` · `보유 업무문서` 두 칸만 추정을 거치지 않는다. 이 둘을 앞에, 추정 칸을 뒤에 둔다.
- 소관표는 담양군이 정할 자료다 — `DYPOLICY` `dept-duty`.

**부서 명단은 원장 ∪ 조직도다 (MUST, 2026-08-25)** — `DYCMP.deptList()` 한 곳에서 만든다. 어느 한쪽만 쓰면 조용히 잃는다: 원장만 쓰면 조직에는 있는데 문서가 0건인 부서(회계과·보건소·담양읍)가 사라지고, `DYV2.ORG` 만 쓰면 문서를 가진 12개 부서(경제교통과·산림정원과 등)가 통째로 안 보인다. 각 행에 출처를 밝힌다(`조직도 미등록` / `문서 없음`).
- **두 명단의 깊이가 다른 것을 우리가 합치지 않는다** — 조직도의 「보건소」를 원장은 「보건소보건행정과」로, 농업기술센터를 3개 과로 나눈다. 합치면 우리가 만든 매핑이 자료가 된다. 나란히 두고 드러낸다.
- **`js/cmp-dept-docs.js`(5개년 340,434건)를 이행 관리 표에 섞지 말 것** — 그 집계는 폴더 안의 파일을 세고 원장은 온나라 문서 1건을 세어 **단위가 다르다**(건설과 2025 가 원장 5,057 인데 그 집계로는 29,813 — 6배). 한 칸에 섞으면 시드가 실린 4개 부서만 자릿수가 커져 «그 부서가 일을 많이 했다»로 읽힌다. 5개년 축 자체는 값이 있어 `deptDocs`/`deptDocsMeta` 는 남긴다.

**'이행'을 말하는 축이 셋이다 — 합치지 말 것 (MUST)**

| 축 | 화면 | 완료 판정 |
|---|---|---|
| 증빙문서 | `업무문서 > 이행 목록` | `DYDOCS` `StageProgress` |
| 부서 점검 | `안전보건관리체계 > 이행관리` | `DEPTCHK` (§4-2) |
| 발행·배정 | `업무 관리 > 업무 발행 관리` | `DYWORK.deptDone` (§14-2) |

새 축은 남의 판정을 **읽지도 쓰지도 않는다** — 부르는 순간 §14-2 의 '완료는 한 곳에서만'이 두 곳이 된다. 대신 각 화면 안내에 서로의 역할을 한 줄로 밝힌다. 177 업무단계 ↔ `DYWORKT` 26종 매핑은 후속 과제다.

### 6. 디자인 토큰 — 원시값 금지 (MUST)

색·폰트 크기·반경·그림자·z-index 는 **`css/style.css` `:root` 토큰만** 사용한다. CSS·JS·HTML 어디서든 원시값(hex·rgba·px 폰트·리터럴 z-index)을 직접 쓰지 않는다.

**근거** — 원시값은 토큰과 같은 값이어도 "단일 출처"가 아니다. 토큰을 고쳐도 따라오지 않아 화면마다 색이 어긋나고, 새 화면이 자기 팔레트를 재발명하는 출발점이 된다.

| 종류 | 토큰 | 비고 |
|---|---|---|
| 표면 | `--surface`(#fff) · `--surface-alt`(#fafafa, 표 헤더 등) · `--surface-sunken`(페이지 바닥) | `--surface-alt`를 `--page-bg`로 수렴시키지 않는다 |
| 브랜드·중립·상태 | `--main`/`--brand-*` · `--text-*`/`--gray-*`/`--card-line` · `--status-{tone}-bg/border/fg` | |
| 폰트 | `--fs-12` ~ `--fs-38` · `--fw-*` | 스케일 밖 값은 **최근접 토큰으로 수렴** |
| 반경 | `--radius-xs/sm/md/button/progress/statbox/card/pill/full` | 동상 |
| 그림자·백드롭 | `--shadow-sm/md/lg` · `--overlay-scrim` | |
| 레이어 | `--z-base(1)` · `--z-nav(30)` · `--z-fab(90)` · `--z-drawer(100)` · `--z-overlay(110)` · `--z-modal(120)` · `--z-toast(130)` | 6단계 밖 임의 값 금지. FAB 는 드로어·오버레이보다 **아래** |
| **간격** | `--sp-0-5`(2) ~ `--sp-12`(48) — 4px 그리드, 6·10·14 는 반 단계 | 신설 2026-08-25. 5·7·9·11·13px 은 **최근접 토큰으로 수렴** |
| **수직 리듬** | `--stack-tight`(8) · `--stack-base`(16) · `--stack-section`(28) | 3티어. 같은 덩어리 / 블록 / 섹션 경계 |
| **컨트롤 높이** | `--ctl-sm`(32) · `--ctl-md`(36) · `--ctl-lg`(40) | 한 줄에 서는 컨트롤은 **반드시 같은 단** |
| 읽기 폭 | `--measure`(820px) · `--content-max`(1680px) | 설명 문단 **블록**에 건다(텍스트 아님) |

- `var(--x, #폴백)`의 폴백값은 반드시 실제 토큰값과 일치시킨다(불일치 폴백은 조용한 색 어긋남의 원인).
- **`.btn` 은 투명 테두리를 기본으로 갖는다 (2026-08-25)** — `border: none` 이면 테두리를 가진 variant(`.btn-outline`)만 2px 커져, 나란히 선 `[닫기]`(outline)와 `[저장]`(primary)의 **밑변이 어긋난다**. `<a class="btn">`(코드 51곳)과 `<button class="btn">`의 차이로 오인하기 쉬운데 원인은 태그가 아니라 variant 다. `.btn` 이 `border: 1px solid transparent` 를 갖고 variant 는 **색만** 바꾼다.
  - 높이도 못 박는다 — `line-height: 1.4` + `min-height: var(--ctl-md)`. 태그마다 상속 line-height 가 달라 2px 갈리던 것을 없앤다.
  - **세로 패딩은 6px 이다(8 아님)** — 8이면 자연 높이가 37.6 이 되어 **토큰(36)과 실제(38)가 어긋난다**. 다음 사람이 `--ctl-md` 를 믿고 맞추면 2px 틀린다. 현재 전 화면 버튼 높이는 **32 / 36 / 40** 세 단으로만 수렴한다.
  - `transition: all` 을 쓰지 않는다 — width·height 까지 애니메이트해 레이아웃을 흔든다. 바뀌는 것은 색뿐이므로 색만 지정한다.

- **없는 토큰을 쓰면 폴백이 없는 한 조용히 무시된다 (2026-08-25)** — `--fw-600`·`--fw-normal`·`--fw-extrabold`·`--fs-20`·`--fs-24` 다섯이 그랬다. 굵기 토큰명은 `--fw-regular/medium/semibold/bold` 이고 폰트 스케일에 **20·24 는 없다**. 실제 피해: 문서 상세 제목(`.cmp-detail-h`)이 20px 의도인데 **본문과 같은 16px** 로 상속돼 위계가 사라져 있었다. §7-1 «미정의 클래스는 오류를 내지 않고 그냥 기본 스타일로 그려진다»의 토큰 판이다.
  - 검사: `var(--x)` 중 정의에 없고 폴백도 없는 것 0을 확인한다(현재 0).
- 예외 — `onepager-docflow.html`·`screen-definitions.html`은 레이아웃 시스템 밖 독립 문서라 치환 대상이 아니다.
- 검사: `grep -nE '#[0-9a-fA-F]{3,6}|font-size: *[0-9]|z-index: *[0-9]' css/v2.css` 잔존 0.

**간격·리듬·컨트롤 높이 (2026-08-25 신설)** — 실측 근거: [`검수-GUI-간격레이아웃-v1.md`](docs/planning/검수-GUI-간격레이아웃-v1.md)

색·폰트·반경만 토큰이던 시절 간격에는 기준이 없어 화면마다 «대충 이 정도»를 다시 정했다. 실측 결과 `padding`·`margin`·`gap` 선언 **2,450개 중 14%가 4px 그리드 밖**이었다(5·7·9·11·13·15·17px). 아래 셋을 지킨다.

- **수직 리듬은 3티어다** — 종전 한 화면의 블록 간격이 **28px 하나 빼고 전부 10~16px** 이라 «맥락 / 요약 / 목록» 세 덩어리가 한 덩어리로 읽혔다. 어디서 하나가 끝나는지 눈으로 알 수 없다는 뜻이다. 섹션 경계에는 `--stack-section`, 블록 사이는 `--stack-base`, 밀착은 `--stack-tight`.
- **한 줄에 서는 컨트롤은 같은 높이여야 한다** — 필터 바에서 입력(43px) 옆에 버튼(34px)이 서서 **9px 어긋나** 있었다. 줄이 하나뿐인데 밑변이 안 맞으면 그것만으로 화면이 흐트러져 보인다.
  - ⚠ **높이는 태그로 건다** — `<textarea class="form-input">` 이 5곳 있어 클래스만 보고 `height` 를 박으면 여러 줄 입력이 40px 로 눌려 잘린다(집행 중 실제로 낸 회귀). `input.form-input` · `select.form-select` 로 좁힌다.
  - `min-height` 로는 안 잡히는 경우가 있다 — 패딩 합이 이미 목표치를 넘으면 무시된다.
- **읽기 폭은 텍스트가 아니라 블록에 건다** — 1920px 에서 안내 문단이 한 줄 **225자**였다(권장의 4배). 그런데 텍스트에만 `max-width` 를 걸면 박스는 전폭 그대로라 **접기 버튼이 텍스트에서 1,000px 떨어진 자리**에 남는다. 블록을 묶으면 버튼이 따라온다.
  - **표는 묶지 않는다** — 열이 많을수록 넓은 편이 낫다. 전역 `p` 에 걸면 표 안 셀·카드 안 문단까지 좁아진다.
- **브레이크포인트는 4개뿐이다(§8)** — 종전 `@media (max-width: 1599px)` 라는 다섯 번째가 있어 1600px 이상에서만 셸이 «특대»(패딩 60 · 사이드바 282 · 제목 22→26)로 벌어졌다. 그쪽이 오히려 헐거워 본문 여백만 커지고 읽기 폭은 나빠졌다. **그 블록의 값을 기본으로 올리고 블록을 없앴다** — 전 해상도가 같은 셸을 쓴다.
- **기존 342곳(스케일 밖 값)은 점진 치환 대상**이다. 한 번에 바꾸지 않는다 — 1px 차이는 안 보이지만 342곳을 동시에 옮기면 어딘가 깨진다(§7 «별칭 후 점진 치환»과 같은 방식). 새로 쓰는 곳은 예외 없이 토큰.

### 7. 표준 컴포넌트 클래스 — 계열 신설 금지

동일 요소는 **한 계열로만** 구현한다. 화면 전용 접두어(`sh-`·`edu-`·`evl-`·`rl-` 등)로 같은 요소를 다시 만들지 않고, 변형이 필요하면 표준 클래스의 modifier 로 붙인다.

| 요소 | 표준 | 폐기·별칭 대상 |
|---|---|---|
| 상태 배지 | `chip-status` + `DYV2.toneOf(라벨)` | `chip-mini.st-*` · `sh-st` · `.edu-src` · `.tag` (별칭 후 점진 치환) · `.badge-*`(미사용, 삭제) |
| select | `.form-select` | `.select`(별칭 유지) |
| 테이블 | `.table-figma` | `.sh-table` · `.edu-table` · 화면 로컬 `*-table` |
| 빈 상태 | `.v2-empty` | `.sh-empty` · `.edu-empty` |
| 토스트 | `DYV2.toast` | layout.js·screen-definitions.js 자체 구현 |
| 탭 | `.tabs` / `.tab` | `sh-vtab` · `sh-ptab` · `edu-tab` · `sd-tab` 등 |
| 모달 | `DYV2.openModal` (§1) | 자작 백드롭 `.ev-modal-backdrop` · `.evl-modal-backdrop` |
| 조직도 픽커 | `ORGPICK` / `.org-inline` (§1) | `.evp-*` · `.org-tree-panel` · `.bgt-tree-*` · `.admp-tree` |
| 이행항목·업무단계 픽커 | **`DYPICK` / `.dyp`** (§5) | 80개 드롭다운 · 80개 아코디언 · 종속 `<select>` |
| 법령 근거 | `DYLAW` / `.law-basis-chip` (§10) | 화면별 근거 문자열 하드코딩 |
| 개선조치 상세 카드 | `IMPCARD` (`js/rsk-imp-cards.js`) | 화면별 개선조치 표·카드 재구현 |
| 공문(기안문) 지면 | `.pdf-paper.pdf-doc.pdf-gm` + `.pdf-gm-annex`·`.table-doc` | 화면별 공문 CSS 계열 신설 |
| **공문 흐름 전체** | **`DYDOC`(`js/doc-flow.js`)** — §7-1 | 도메인별 공문 모듈 자작 |

**상태 어휘 → tone 은 `DYV2.STATUS_TONE`(`js/common.js`) 단일 출처**다. 화면에서 라벨별 색을 직접 고르지 않고 `DYV2.toneOf(label)`로 tone 을 얻어 `--status-{tone}-*` 토큰을 쓴다. 매핑에 없는 라벨은 `neutral` 로 수렴하므로, 새 어휘는 표에 추가한다.

### 7-1. 공문 골격 — `DYDOC` 하나로 (MUST, 2026-08-12)

공문을 쓰는 도메인이 늘어도 **골격은 `js/doc-flow.js`(전역 `DYDOC`) 하나**다. 도메인은 `DYDOC.define(cfg)` 로 **지면 내용과 저장 위치만** 얹는다. 시연 투어가 `DYTOUR` 하나로 모인 것과 같은 구조다(§4-3 선례).

**골격이 강제하는 순서 (뒤집지 말 것)**

```
[공문 기안] → 본문 작성 → [문서 미리보기 →] → [온나라로 결재 상신] → 확인 → [상신]
```

- **기안 폼에는 상신 수단이 없다.** 발주처가 교육의 결재 상신을 지우게 한 이유가 기능이 아니라 *"공문 작성 단계 없이 결재만 올라가는 순서"* 였다(2026-07-30). 도메인이 이 순서를 건너뛸 수 없도록 골격이 화면을 나눠 그린다.
- **본문을 자동 생성하지 않는다** — *"이게 다 직접 타이핑 하셔야 돼"*. 시스템은 붙임을 엮고 수치를 세어 **삽입 칩**으로 건넬 뿐이다.
- **붙임은 이미 올려 둔 파일에서 고른다.** 공문 화면에서 파일을 새로 받으면 같은 증빙이 두 벌 생긴다. 큰 표는 본문이 아니라 **붙임 별지**다.
- **확인 단계는 새 정보만 낸다** — 미리보기에서 본 문서·붙임·결재선을 반복하지 않고 **되돌릴 수 없다는 사실**만 말한다. 같은 내용을 세 번 보여주면 아무도 읽지 않는 확인이 된다(FMS `FSY-01` 과 같은 근거).
- **결재선을 코드에 고정하지 않는다** — 위임전결규칙 소관이라 바뀐다. 기본값만 `DYV2.ORG` 에서 직위명으로 파생하고 변경은 `ORGPICK`(`member`)으로만 받는다.
- **지면은 표준 공문 서식**(별지 제1호서식)이고 내부결재/외부발송은 실제로 다르므로 합치지 않는다. **이 규정을 근거 칩으로 인용하지 않는다** — DYLAW 스냅샷에 없다(§10).

**`cfg` 계약** — `targetOf` · `titleOf` · `subjectOf` · `ready`(기안 가능 여부와 사유) · `chips`(삽입 조각) · `attachOf`(붙임 후보) · `annexes`(별지) · `basisOf`(근거 후보, 없으면 절이 사라진다) · `save` · `docsFor` · `nextNo` · `lockNote`.

**⚠ 위험성평가 공문은 동결이다 — 이관을 이유로 손대지 말 것 (MUST)**

`DYRSKDOC` 은 같은 순서·지면을 자체 구현하고 있어 구조상 중복이지만, **기능 정의와 UI/UX 를 더 바꾸지 않기로 확정**했다(2026-08-12). 검수를 통과해 전달 범위에 든 부분이라 화면이 흔들리면 전달본이 무너진다.

- **지금은 두 구현이 공존한다.** 이것이 §7(계열 신설 금지)의 예외임을 알고 두는 것이지 모르고 방치하는 것이 아니다.
- **이관하려면 화면 무변경이 전제다.** 확인 단계·기안 폼·지면이 지금과 **픽셀 단위로 같아야** 하며, 다르게 만들 거리가 생기면 이관하지 않는다. 특히 확인 단계의 항목 표(문서·유형·대상·붙임·결재선)는 `DYDOC` 기본형과 다르므로, 이관한다면 `DYDOC` 이 확인 화면을 `cfg` 로 받도록 먼저 열어야 한다 — **위험성평가를 `DYDOC` 기본형에 맞추는 방향은 금지**다.
- **바꿀 일이 생기면 `DYDOC` 을 먼저 고치고 맞춘다** — 두 구현이 서로 다른 방향으로 벌어지지 않게 하는 최소 장치다. 위험성평가 쪽 변경은 그 자체로 발주처 확인 사항이다.
- 새 도메인의 공문은 **예외 없이 `DYDOC` 위에** 만든다.

**잠금은 도메인이 판정하고 한 곳에서만 한다** — 공문이 올라간 원본은 고칠 수 없다(*"이건 문서 위조예요"*). 교육은 `EDUDOC.lockOf(courseId)`.

**골격을 분리할 때 클래스 어휘를 새로 만들지 말 것 (MUST, 2026-08-13)** — `DYDOC` 을 `DYRSKDOC` 에서 뽑아낼 때 마크업은 옮겼는데 **CSS 는 옮기지 않았고**, 같은 자리에 새 이름(`rskdoc-line-body`·`rskdoc-ln`·`rskdoc-ln-k`·`rskdoc-chips`·`rskdoc-attach`·`rskdoc-att`·`rskdoc-row`·`rskdoc-basis`·`pdf-gm-annex-k`)을 지어 붙였다. 그 결과 **12개 클래스에 규칙이 하나도 없어** 교육 공문 기안 팝업의 근거·붙임 체크박스가 줄바꿈 없이 붙고 결재선 행이 통째로 무스타일로 떴다. `css/v2.css` 에 이미 있던 `rskdoc-ln-row`·`-ln-step`·`-ln-pick`·`rskdoc-cks`·`rskdoc-ck`·`rskdoc-block`·`rskdoc-inslist`·`pdf-gm-annex-h` 로 되돌려 해결했다.
- **증상이 조용하다** — 없는 클래스는 오류를 내지 않고 그냥 기본 스타일로 그려진다. 그래서 문법 검사·콘솔로는 안 잡히고 눈으로 봐야 드러난다.
- 새 화면·모듈을 붙이면 **쓰는 클래스가 CSS 에 실제로 있는지** 대조한다: `class="..."` 를 뽑아 `css/style.css`·`css/v2.css` 의 `.클래스` 정의와 맞춰 보고 미정의 0을 확인한다.
- 접두어 `rskdoc-` 는 위험성평가 전용처럼 읽히지만 **지금은 공문 공용 계열**이다. 이름이 도메인을 가리킨다고 새 계열을 파지 말 것 — 그것이 이 사고의 원인이었다. 접두어 개명은 동결된 `js/rsk-doc.js` 와 그 CSS 를 함께 건드려야 하므로 별도 결정 사항이다.
- 예외 — `.rskdoc-send` 는 두 구현이 함께 쓰는 **규칙 없는 의미 훅**이다. 스타일을 주면 동결된 위험성평가 확인 모달이 함께 움직이므로 그대로 둔다.

| 도메인 | 모듈 | 상태 |
|---|---|---|
| 안전보건교육 | `js/edu-doc.js` (`EDUDOC`) | `DYDOC` 위에 있다 — 단위는 **교육 1건**뿐(§4) |
| 위험성평가 | `js/rsk-doc.js` (`DYRSKDOC`) | **동결 — 현행 유지 (2026-08-12 발주처 지시)**. 아래 참고 |

### 8. 브레이크포인트 — 4단계 표준

`xl 1280 / lg 1024 / md 768 / sm 560` **4개 값만** 사용한다. 그 밖의 폭(1599·1279·1100·900·860·767·720·640·479 등)으로 미디어쿼리를 새로 만들지 않는다.

- CSS: `@media (max-width: 1023px)`처럼 표준값−1 을 쓴다(토큰 `--bp-*`는 문서·참조용 — `@media`는 `var()`를 지원하지 않는다).
- JS: `matchMedia` 리터럴 금지. `DYV2.BP`(`{xl:1280, lg:1024, md:768, sm:560}`) 또는 `DYV2.below('lg')`를 쓴다.
- 셸(햄버거+드로어) 전환은 `lg`(1023px 이하) 기준이며, 컴포넌트도 같은 시점에 접혀야 한다.

### 9. 신규 화면 체크리스트

새 화면·컴포넌트를 추가하기 전 아래를 만족시킨다.

1. 색·폰트·반경·z-index 에 원시값 0 (§5).
2. 배지·select·테이블·빈상태·탭·토스트·모달은 표준 클래스/헬퍼 사용, 새 계열 신설 없음 (§6).
3. 조직도는 `DYV2.ORG` 파생만, 업로드는 `DYV2.uploadDrop()`/`DYV2.fileHint()` (§2·§3).
4. 모달은 `DYV2.openModal` 1개만, 적층 없음 (§1).
5. **375px에서 확인** — 가로 스크롤 없음(표는 스크롤 래퍼 안), 겹침·과밀 없음, 미디어쿼리는 4단계 표준값만 (§7).
6. 본문·배지 최소 폰트 **12px**(장식 글리프 `▸▾` 등은 예외), 주요 터치 타깃 44px.
7. 법령 근거가 있는 화면이면 `DYLAW.MAP`에 매핑을 추가한다 (§10). 근거가 없으면 `[]`로 명시한다.

### 10. 법령 근거 — 단일 출처 (MUST)

화면·문서의 근거 조문은 `DYLAW`(`js/law-map.js`)에만 정의한다. 근거를 문자열로 화면에 직접 쓰지 않는다. 근거: `docs/planning/기획-법령연계-v1.md`.

- **조문 텍스트는 손으로 고치지 않는다.** 이 파일은 법제처 국가법령정보 OPEN API 스냅샷의 **생성물**이다(수집일 `DYLAW.SNAPSHOT.fetchedAt`). 내용이 틀렸으면 재수집해서 파일을 다시 만든다. 요약·의역해서 넣으면 "원문"이라는 약속이 깨진다.
- **`MAP` 은 페이지 id → 조문 키.** 법령 근거가 없는 화면(시스템 관리 등)은 **빈 배열 `[]` 로 명시**한다 — 억지로 조문을 붙이지 않는다. 근거 없음도 정보다.
- **칩 주입은 `layout.js` `mount()` 한 곳**에서 `DYLAW.inject(main)` 으로 처리한다. 화면 모듈이 자체 근거 UI 를 그리지 않는다. `menu.html` 은 `?m=`·`?sub=` 까지 `DYLAW.pageId()` 가 환원하므로 페이지 id 를 직접 넘기지 말 것.
- **조문 펼침은 인라인**(`.lawinfo-inline`)이다 — 모달을 띄우지 않는다(§1).
- 기존에 흩어진 `basis:` 문자열은 `DYLAW.resolveBasis()` 로 흡수한다. 새 근거 표기를 만들지 말고 별칭 표(`BASIS_ALIAS`)에 추가한다.
- `lawId`·`mst` 는 **배치 재조회 키**다. 실 개발에서 백엔드가 이 키로 법제처 API 를 직접 호출해 개정을 감지한다. 임의 변경 금지.

**매핑 추가 시 검증 6문 (MUST)** — 2026-07-27 감사에서 5회 연속 결함이 나왔다. 근거는 "붙이면 끝"이 아니다. 매핑을 추가·변경할 때 아래를 전부 통과시킨다. 근거: `docs/planning/기획-법령연계-v1.md` §4.3~§4.7.

1. **조문을 실제로 열어봤나** — 조문 번호만 보고 붙이지 않는다. (실제 사고: 기준규칙 §132를 '낙하물 방지'로 붙였는데 그 조문은 「양중기」였다. §310은 '정전작업'이 아니라 「전기 기계·기구의 조작 시 안전조치」였다.) (2026-08-12 추가 사례: 경영방침 **게시 현황**에 「산업안전보건법」 **§14** 를 붙여 놓았는데, 그 조문은 *「상법」상 **주식회사**의 **대표이사**가 안전보건계획을 **이사회**에 보고·승인받는 의무*라 **지자체인 담양군에는 적용 자체가 되지 않는다**. 같은 법 §34(법령 요지 등의 게시)도 대상이 *법령 요지와 **안전보건관리규정***이라 경영방침이 아니다.)
2. **"관련이 있다"가 아니라 "그 의무의 직접 이행 수단"인가** — 데이터를 준비하거나 결과를 보관하는 화면은 관련은 있어도 이행 수단이 아니다. 이 기준을 느슨하게 하면 거의 모든 화면에 같은 조문을 붙일 수 있게 되어 근거가 정보로서 값을 잃는다.
3. **화면 근거인가 항목 근거인가** — 여러 도메인 업무가 모이는 화면(내 할일)은 화면 근거가 성립하지 않는다. 항목 근거만 둔다.
4. **법률의 다른 축이 빠지지 않았나** — 중대재해처벌법은 **중대산업재해(법 §4·§5)와 중대시민재해(법 §9, 시행령 §8·§10·§11) 두 축**이다. 담양군은 공중이용시설·원료제조물을 관리대상으로 두므로 양쪽 다 적용된다. 한 축만 붙이면 절반만 근거를 댄 것이다.
5. **주기를 정하는 조문이 그 조문인가** — 법률 조문은 대개 "대통령령/고용노동부령으로 정한다"로 **위임만** 한다. 주기는 시행령·시행규칙·별표에 있다(산보위 분기 → 산안법 시행령 §37, 작업환경측정 반기 → 시행규칙 §190, 교육시간 → 시행규칙 별표4). 주기를 표시하려면 **주기 근거 조문을 함께** 댄다.
5-1. **그 조문에 그 주기가 실제로 있는가** — 옆 호에 있다고 이 호에도 있는 것이 아니다. 중처법 시행령 §4 에서 *반기 1회 이상 점검*을 요구하는 것은 **3·5·7·8·9호**이고 **1호(목표·경영방침 설정)에는 그 문구가 없다**. 관리 편의로 반기 점검을 두는 것은 되지만, **법정 의무라고 쓰면 안 된다** — 화면이 하지 않아도 되는 일을 의무로 만든다.
6. **화면이 하지 않는 일을 한다고 쓰지 않았나** — 설명 문구에 조문을 나열하기 전에 그 조문의 점검 항목이 실제로 화면에 있는지 확인한다. (실제 사고: 이행관리가 "§4·§5를 점검한다"고 적어 두고 §4만 점검하고 있었다.)

**관리 화면 (2026-07-29 신설 · 2026-07-30 탭 통합)** — 시스템 관리 > 법령 관리 **단일 화면**(`admin-law.html`)의 3탭에서 관리한다: 법령·조문(`DYADMLAW`) / 메뉴 근거 매핑(`DYADMLAWMAP`) / 변경 이력. 탭 셸은 `LAWTABS`(`js/admin-law.js`), 공용 코어는 `js/law-admin.js`(전역 `LAWADM`). 구 `admin-law-map.html` 은 매핑 탭 리다이렉트 스텁(쿼리 보존) — 별도 메뉴로 되돌리지 말 것.
- **매핑 탭은 검토 대장이다 — 편집 UI 를 되만들지 말 것 (MUST, 2026-07-30)** — 매핑도 조문과 같은 생성물이라 화면에서 저장·제거·순서·역할을 편집하지 않는다(근거 추가 UI 제거 af303ba 의 완결). 이견은 `[매핑 재검토 요청]`(비파괴 기록, 정비 큐·이력 표출)으로만 받고, 변경은 `law-map.js` 재생성으로만 반영한다. 역할(의무/주기·기준)은 `ARTICLES[].cycle` 파생 배지다 — 편집값으로 두면 주기 조문이 "의무 근거"로 오표시된다(실제 있었던 결함).
- **쉬운 설명 레이어는 `js/law-plain.js`(전역 `DYLAWPLAIN`, 사람 해설·생성물 아님)** — 조문별 `한 줄 요약+누가·무엇을·언제`(48건)·법령 소개·자주 찾는 질문. `DYLAW.plainHtml()`이 로드된 화면에서만 "요약 — 원문 아님" 라벨과 함께 그린다. **원문(text)에 요약을 섞는 것이 금지**이지 분리 레이어는 허용이며, 조문 개정 시 해설도 재검토 대상이다. 법제처 수집 상태 UI 는 배치 기록(`law-sync-seed.js` `health`) 기반 — 화면이 API 를 직접 호출하는 코드를 넣지 말 것.
- **초기 DB 시드** — 2026-07-30 전수 검토의 검증 6문 기록·근거 없음 사유·표기 분류는 `js/law-admin-seed.js`(전역 `DYLAWADMSEED`, 생성물 아님·사람 판단 기록)가 초기값으로 싣는다. `LAWADM` 스토어(`dy-lawadm-v2`)는 빈 상태가 아니라 **이 시드가 영점**이다 — [데모 데이터 초기화]도 시드로 복귀. 매핑·판정을 바꾸는 커밋은 시드도 함께 갱신한다.
- **편집 경계** — 조문 사실(본문·제목·시행일·`lawId`·`mst`)은 **입력란 자체를 두지 않는다.** 사람이 남기는 값(매핑·검증 기록·적용 축·표기 분류)은 `localStorage` override 에 따로 쌓여 조문 사실을 덮지 못한다.
- **삭제가 없다** — 조문키는 저장 스키마(`hazard.basis` 등)라 지우면 조용히 끊긴다. `참조 0 → 보관`만 제공하고 원문은 남긴다.
- **매핑 병합은 `LAWADM.effective()` 하나만 쓴다** — 화면이 각자 병합하면 옛 스토어를 가진 브라우저에서만 다르게 보이는 사고가 난다.
- 화면이 법제처를 직접 호출하지 않는다(방화벽 명세는 배치 서버 단독 아웃바운드). 수집은 시뮬레이션이며 화면에 명시한다.

### 13. 외부 자료·연계 준비사항 — `DYPOLICY` 단일 출처 (2026-08-12 개정)

자체 기획 결정은 각 화면 정의서 §6에서 끝낸다. `js/policy-open.js`(전역 `DYPOLICY`)에는 담양군 제공 자료·타 기관 연계 규격처럼 시스템이 만들 수 없는 외부 입력(`DYPOLICY.ITEMS`)과 미수신 대체 동작만 정의한다. 화면이 자체 정책 질문 UI를 다시 만들지 않는다.

- **개수를 이 규칙에 적지 않는다 (2026-08-19)** — 종전 이 자리에 «외부 입력 **10건**» 이라고 못박혀 있었는데 `ITEMS` 는 실제로 11건이었다. 항목이 하나 늘 때마다 규칙만 옛 수치로 남고, 다음 작업자는 «규칙이 10건이라는데 11건이네» 를 결함으로 읽어 멀쩡한 항목을 지운다 — 바로 아래 `selfCheck()` 가 막으려는 «없는 것을 규칙에 적어 두는» 사고와 같은 구조다. 개수가 필요하면 `DYPOLICY.ITEMS.length` 를 세라. 화면(모달 제목·요약·헤더 칩)도 이미 그 값에서 파생하므로 항목만 늘리면 전부 따라온다.

- **외부 자료의 수신 준비를 확인하는 창구다** — 헤더의 `자료 준비 N` 칩을 누르면 지금 화면 관련 미수신 항목이 맨 위에 온다. 기획 선택지를 묻는 창구로 되돌리지 않는다.
- `MAP` 은 **page id → 항목 id** (`DYLAW.MAP` 과 같은 방식). 화면 매핑이 없으면 남은 건수만 표시한다.
  - **page id 는 `data-dy-page` 가 아니라 `DYLAW.pageId()` 환원값이다** — `menu.html` 계열은 `?m=`·`?sub=` 이 `MENU_ALIAS`/`OPINION_SUB` 로 바뀐다(경영방침 = `sbm-policy`, 이행점검 = `sbm-comply`). `'safety-policy'` 라고 적어 경영방침 화면에서 영영 안 뜨는 결함을 실제로 냈다. 오타의 증상이 "그 화면에서만 조용히 없음"이라 `selfCheck()` 가 `DYLAW.MAP`(page id 전수 목록) 과 대조해 콘솔 경고를 낸다 — 지우지 말 것.
  - **`now` 는 코드를 열어보고 쓴다** — 화면이 이미 하고 있는 걸 "안 합니다"라고 쓰면 시연 중 그 자리에서 들킨다. 실제로 냈던 3건: 업무문서에 보존연한 select 가 이미 있는데 "표시하지 않습니다" · 의견청취에 익명 축이 없는데 "익명 건만 제한" · 관리대상에 [수정] 버튼이 없는데 "등록·수정".
  - **선택지 라벨은 자립형으로** — 정리본이 고른 라벨 한 줄만 찍으므로 `'위 + 본인 것만'` 은 회의록에서 뜻이 서지 않는다.
- **한 항목은 `{ title, now, options[] }` 세 조각이 전부다 (MUST)** — 요청할 자료명, 미수신 대체 동작, 수신 상태 선택지다. `options`는 정책안이 아니라 `자료 대기/부분 자료/규격 대기/수신 완료` 같은 준비 상태만 허용한다.
  - `title`은 외부에 요청할 자료·규격 이름으로 쓴다.
  - `now`는 미수신 동안 시스템이 어떻게 동작하고 무엇을 성공으로 가장하지 않는지 적는다.
- **수신 상태를 기록한다** — 선택지 칩은 `localStorage('dy-external-readiness-v1')`에 쌓이고 `[준비 목록 보기]`가 전달용 요청 목록을 만든다.
- **수신돼도 항목을 지우지 말 것** — `수신 완료`로 남겨 외부 의존성과 대체 동작의 근거를 보존한다.
- **진입점은 헤더가 아니라 `시스템 관리 > 연계 관리`(`admin-integration.html`)다 (2026-08-19 이동)** — 헤더는 사용자 자리이고, «외부에서 무엇이 들어오는가»는 그 화면의 축이다. 종전 헤더 칩(`dy-policy-chip`)과 `syncChip()` 은 소비처가 없다. **헤더로 되돌리지 말 것.**
- **셸 크롬 모달은 `openModal(..., {chrome:true})` 로 연다 (MUST)** — 외부 준비 목록은 업무 흐름이 아니라 헤더에서 언제든 여는 창구이므로 투어 단계 안내를 주입하지 않는다. 현재 대상은 `DYPOLICY` 3개 모달 + `DYHELP` 도움말 시트다.
- **헤더 드롭다운(알림·권한 전환)이 열리면 투어 패널이 접힌다** — 패널은 `--z-fab`(90), 드롭다운은 `--z-nav + 2`(32) 라 패널이 알림 목록의 오른쪽을 가렸다. **레이어를 뒤집지 말 것**(패널이 뜨는 자리는 FAB 자리가 맞다) — 모달과 같은 규칙으로 접는다. 드롭다운은 class 토글이라 투어의 `MutationObserver`(childList only) 가 못 잡으므로 `layout.js` 가 여닫을 때 `DYTOUR.syncChrome()` 를 **명시 호출**한다. `attributes` 를 관찰하게 바꾸지 말 것 — `classList.add` 가 관찰자를 다시 깨워 루프 위험이 생긴다.
  - 모바일 드로어는 손댈 필요가 없다 — 백드롭(99)이 패널(90) 위라 이미 가려진다.

### 14. 업무 자동발행 — `DYWORK` 단일 출처 (2026-08-10 신설)

부서별 반복 업무를 시스템이 발행하고, 부서가 담당자를 정해 처리하고, 주관부서가 확인하는 축이다. 근거: [`docs/planning/기획-업무자동발행-v1.md`](docs/planning/기획-업무자동발행-v1.md) · 실측 원장 [`분석-업무문서-5개년-실측-v1.md`](docs/planning/분석-업무문서-5개년-실측-v1.md)(담양군 5개 부서 5개년 문서 430,089행) · 법령 세트 매핑 [`기획-업무자동발행-법령세트매핑-v1.md`](docs/planning/기획-업무자동발행-법령세트매핑-v1.md).

**구성** — `js/work-catalog.js`(전역 `DYWORKT`, 템플릿 26종 · 사람 판단 기록) · `js/work-data.js`(`DYWORK`, 발행 엔진) · `js/work-assign.js`(`WKASSIGN`, 배정 패널 공유) · `js/work-admin.js`(`WKADM`, 4탭) · `js/work-dept.js`(`WKDEPT`, 2탭). 화면 2개(`work-admin.html`·`work-dept.html`) + `my-work` 개편. 로드 순서는 `common.js → org-pick.js → dept-check.js → edu-data.js → work-catalog.js → work-data.js → work-assign.js → 화면 모듈`.

**14-1. 자동발행은 앞단 레이어다 — 수행 화면을 대체하지 않는다 (MUST)**
템플릿은 **언제·누구에게·언제까지·누가 할지**만 만든다. 점검표 작성·교육 등록·개선조치·검진 결과는 전부 기존 전용 화면(`menu.html?m=comply`·`edu-*`·`rsk-*`·`health-exam`·`work-env`)이 한다. 발행 모듈이 같은 입력 폼을 다시 그리면 §7(계열 신설 금지)이자 데이터 이중화다. **카탈로그 26종 중 대체는 0개**다.

**14-2. 완료는 `DYWORK.deptDone(t)` 한 곳에서만 판정한다 (MUST)**
`profile:'menu'` 인 업무는 그 도메인의 판정을 **읽어온다**(`doneProbe`). 발행 모듈이 자기 완료 상태를 따로 저장하면 "이행점검 화면은 완료인데 업무 카드는 미착수"가 조용히 생긴다(§4-3 `improvement.status` 8곳 사고와 같은 구조). `doneProbe` 를 댈 수 없는 도메인은 `menu` 를 쓰지 못하고 `attach` 로 강등한다 — **파생 원본이 영속 스토어를 갖는지 반드시 확인**한다(산업안전보건위원회 회의록이 그래서 첨부다).
할 일 노출은 `DYWORK.needsAction(t, uid)` 전용이다. 집계(회수율)는 이 함수를 쓰지 않는다. 반려(`confirm.RETURNED`) 건은 다시 할 일로 올라와야 한다 — 안 그러면 재제출 수단이 완료 탭에 묻힌다.

**배선보다 어려운 것은 판정 기준을 업무의 실체와 맞추는 일이다 (2026-08-11 실증).**
· `분기 교육 실시 **결과 제출**` 의 완료는 **결과가 있는가**이지 전원 이수가 아니다. 이수율 100% 로 잡았더니 실측 이수율 38~56% 인 부서가 **영원히 미완료**로 남아 회수율이 늘 0이었다. 반대로 `미이수 조치` 는 100% 여야 끝난다 — 그래서 `EDU:quarter`(기록 존재)와 `EDU:complete`(이수율 100%)를 나눴다.
· 그 회차 **대상이 아닌 부서를 완료로 세지 않는다**. `true` 를 돌려주면 아무것도 안 한 부서가 완료로 잡혀 회수율이 부풀고, 부풀린 수치는 아무도 들여다보지 않는다. `null`(판정 불가)로 두어 미완료로 남겨야 사람이 대상 산정 오류를 알아챈다.

**14-2-1. 대상 부서도 도메인이 알면 도메인에서 받는다 — `deptSource` (MUST)**
속성 파생(§14-5)은 **폴백**이다. 실측 대조 결과 속성이 절반쯤 틀렸다 — 교육은 속성 8곳 vs 실제 10곳(기획예산실·재난안전과·회계과 누락, 재무과 과다), 위험성평가는 속성 6곳 vs 그 해 평가 대상 3곳, 작업환경측정은 속성 8곳 vs 실제 측정대상 4곳.
도메인이 답을 주는 축은 그것을 쓴다 — `deptSource:'EDU'`(근로자 명단) · `deptSource:'RSK'`(그 해 정기평가 `a.depts`, 평가가 없으면 **발행 대상 0**).
**단, 계획이 등록돼야 대상이 되는 축은 도메인을 쓰지 않는다** — 작업환경측정을 `DYSH` 로 파생하면 계획 미등록 부서가 영영 업무를 못 받는 순환에 빠진다. 이런 축은 속성을 유지하고 **불일치를 화면에 드러낸다**.

**14-3. 주기가 있으면 자동 발행, 발생시만 수동 생성 (MUST · 발주처 확정 2026-08-11)**
`schedule.kind` 가 `MONTH·QUARTER·HALF·YEAR` 면 **기간에 맞춰 스스로 나간다** — 사람이 [발행]을 누르지 않는다. `ADHOC`(발생시)만 수동 생성한다. 중간 등급(`MANUAL_REVIEW`)을 두지 않는다 — 주기가 있는데 사람이 눌러야 하면 그건 자동이 아니다.
**발행 주체는 시스템이다** — `issuedBy: '시스템 자동발행'`. 누가 화면을 열었느냐로 발행자가 달라지면 이력이 거짓이 된다. **발행일은 규칙상 날짜**를 쓴다(오늘이 아니다). 조회 전용 계층이 열어도 발행된다 — 배치는 사람과 무관하다.
프로토타입은 **화면 진입**이 트리거이고 실 개발은 **배치 스케줄러**다. **구조는 같고 트리거만 다르다**는 것을 화면에 명시한다(숨기면 "자동인 척"이 된다).
**⚠ 시점 근거의 세기는 업무마다 다르다** — 실측으로 ±2주에 수렴한 것은 8종이고 나머지는 편차 1~3개월이거나 표본 3~4건이다. 전부 자동으로 돌리되 `confidence.timing` 을 화면에 **반드시 노출**해 어느 발행일이 추정인지 드러낸다. 발주처가 확정하면 `issueMD` 를 고친다.
**수동 생성 폼은 회차를 묻지 않는다** — 발생시 업무는 회차가 없다. **발생일 + 사유(필수)** 로 받고 `periodKey` 는 `ADHOC-<날짜>-<순번>` 으로 만들어 멱등키를 유지한다. 사유 없는 생성은 저장되지 않는다.
**지나간 회차를 소급 발행하지 않는다** — `due2issue()` 는 최근 30일 창만 본다. 7월에 1월분 월교육을 내보내는 것은 실제 업무가 아니고, 창이 없으면 발행 예정 블록이 지난 회차로 덮인다. 창 밖 건수는 `missedIssues()` 로 **숨기지 않고 드러낸다**.

**14-4. 템플릿과 발행 인스턴스는 다른 것이다 (MUST)**
템플릿 1 : 발행 N(연도×회차×부서). 멱등키는 `templateId|periodKey` 이고 같은 회차 재발행은 **거부**한다. **`DY_DOCS_V2`(429문서)를 확장해 쓰지 않는다** — `cycle` 이 복합 자유텍스트이고 `due` 가 없으며 `dept` 일부가 `DYV2.ORG` 에 없는 플레이스홀더다. 세트·문서는 `setRef`·`docRef` 로 **역참조만** 한다(`sets-data-v2.js` 무수정).

**14-5. 대상 부서는 명단이 아니라 부서 속성으로 파생한다 (MUST)**
`DYV2.ORG` 노드의 `attrs`(`fieldWorker`·`publicFacility`·`chemical`·`hazard`·`riskSite`)를 `DYWORK.targetDepts(tpl)` 가 읽는다. 템플릿에 `deptIds` 를 박지 않는다 — 회계과는 5년 사이 사실상 폐지됐고(2021년 2,924건 → 2026년 54건) 세무회계과→재무과 등 개편이 6건이다. **속성값 자체는 제출 실적에서 뽑은 추정치**이므로 발주처 확정 대상이다. ⚠ 종전에 여기 적혀 있던 DYPOLICY `work-dept-attr` 는 **실재하지 않는 항목**이다(2026-08-13 확인) — 항목을 만들거나 이 표기를 지운다. 없는 미결을 규칙에 적어 두면 다음 작업자가 정상 기능을 위반으로 오인한다(§13 selfCheck 와 같은 근거). 전 부서 축의 분모는 `DEPTCHK.EXPECTED_DEPTS`(39) **하나만** 쓴다.

**14-6. 기한은 앵커로 정하고 근거를 값 옆에 남긴다 (MUST)**
`dueAnchor: 'PERIOD_END'|'RELATIVE'`. 실측이 "반기 말일에 몰린다"고 말하는 업무는 `PERIOD_END` 가 기본이고, 상대일수는 **중앙값이 아니라 p75** 를 쓴다(중앙값을 기한으로 잡으면 정의상 절반이 발행 즉시 지연). `dueBasis{metric,n,years}` 를 함께 실어 표본 3~4건짜리가 24~47건짜리와 같은 확신으로 보이지 않게 한다. **공문에 적힌 실제 기한은 문서 목록에 없다** — 화면에 "실제 회신 소요일 기반 추정"이라고 밝힌다.
날짜 판정은 전부 `DYV2.today()`/`daysTo()` 파생이다(§11). 시드에 `D+16`·`st:'진행'` 을 박지 않는다.

**14-7. 배정은 조회·조작과 다른 **세 번째 축**이다 (MUST)**
`DYROLE.assignKind(deptId)` → `''|'dept'|'team'` **한 곳에서만** 판정한다(`DYWORK.canAssign` 을 만들지 말 것). `canAct` 는 한 줄도 건드리지 않는다 — 그쪽이 막는 것은 "담당자 이름으로 대신 서명 = 문서 위조"이고, 배정은 저작이 아니라 **지휘**다(선례: `rsk-list` 가 감독 관점에 재촉을 주며 "산안법 §16 지휘·감독"이라 적었다).
**배정은 중간 관리자가 한다 (발주처 확정 2026-08-11)** — 과장·소장·읍면장(`tier==='super'`)만 `assignKind !== ''` 다. 담당자(`staff`)는 남에게 배정하지 못하고 **자기가 맡는 것만** 할 수 있다(`DYROLE.canClaim`). 자임은 '남을 지정하는 행위'가 아니라 '내가 하겠다'는 표시라 지휘계통을 건너뛰지 않고, 부서장 부재 시 일이 멈추지 않게 하는 안전판이다.
**주관부서(재난안전과)는 남의 부서에 배정하지 못한다** — commit `e9a3244` 가 월권으로 지적했다("재난안전과는 부서까지 정해 내려보내고, 담당자는 그 부서가 정한다"). 군수도 배정하지 않는다.
**권한마다 다른 조직도가 열린다 (발주처 확정 2026-08-11)** — 목록에서 **행을 누르면** 조직도가 열리고, 무엇을 고르는지는 권한이 정한다. 별도 버튼을 찾아 누르는 단계를 두지 않는다.

| 누가 | 여는 것 | 고르는 것 | 판정 |
|---|---|---|---|
| 재난안전과 **담당자** | 부서 조직도(`ORGPICK.deptsPanel`) | 이 업무를 받을 **부서** | `DYROLE.canScopeDept()` |
| 과장·소장·읍면장 | 부서 구성원(`memberUid` + `rootId`) | 그 부서 **담당자** | `assignKind === 'dept'` |
| **팀장** | 자기 팀 구성원(`+ teamOnly`) | 그 팀 **담당자** | `assignKind === 'team'` |
| 담당자 | — | 본인만(`[내가 맡기]`) | `DYROLE.canClaim()` |
| 군수 | — | 조회만 | — |

**대상 부서 조정은 자동 파생을 대체하지 않고 보정한다** — 발행은 대상 부서까지 정해서 나가고 재난안전과가 뺄·더할 뿐이다. 자동발행이 대전제이므로 매 회차 손으로 고르게 하면 자동이 아니다. **이미 제출·완료한 부서는 뺄 수 없다**(`setIssueDepts` 가 막는다) — 제출 기록이 사라지면 이력 변조다.
**`assignCandidates()` 는 권한이 없으면 빈 배열이다** — 조회 범위가 `'all'` 인 주관부서 담당자도 남의 부서 **사람 이름**을 받을 이유가 없다. 범위와 권한은 다른 축이고 후보는 권한 쪽 개념이다.
**팀장 데이터는 시연용 시드다** — `teamLead:true` 5명(정수팀·수질관리팀·안전관리팀·환경지도팀)은 담양군 실제 팀 편제가 아니다. 자료를 받으면 통째로 교체한다.

**14-12. 첫 데이터까지의 거리를 잰다 (MUST · 2026-08-11 감사)**
접근성(대비·44px·12px)과 반응형만 보고 **정보 밀도**를 안 봤더니 업무 발행 관리의 첫 표 행이 **813px** 아래에 있었다(1280×900에서 표가 한 줄만 보였다). 새 목록 화면을 만들면 **1280×900 기준 첫 데이터 행이 600px 이내**인지, **900px 안에 데이터가 3행 이상** 들어오는지 확인한다.
· **제목을 두 번 쓰지 않는다** — 페이지 제목(`data-page-title`)과 카드 헤더가 같은 문자열이면 60px 를 정보 0 으로 쓴다. 액션은 `.page-head-action` 으로 페이지 제목 줄에 올린다(`EDUAPV` 선례). 부서·관점 같은 **한정어는 제목에 붙인다**(`내 할일 — 물순환사업소`).
· **안내는 접을 수 있어야 한다** — `DYV2.notice(id, lead, rest, opts)`. 실측상 안내가 화면 글자수의 15~26% 였다. 기본은 펼침(처음 오는 사람이 놓치면 안 된다), 닫으면 `localStorage` 에 기억한다.
  - **예외 — 그 화면에 시연 가이드 바가 있으면 `opts.foldedByDefault` 로 기본 접힘**(2026-08-13). '기본 펼침'의 근거가 *처음 오는 사람이 놓치면 안 된다* 인데, 가이드 바가 이미 그 역할을 하면 안내는 같은 말을 반복하며 목록만 아래로 민다(업무문서에서 실측 91px 바가 붙자 첫 카드가 656px 로 밀렸다). 한 번이라도 여닫으면 그 선택을 따른다 — 그래서 토글은 **저장값이 아니라 지금 화면 상태**를 보고 뒤집는다(저장값만 보면 기본 접힘일 때 첫 클릭이 먹지 않는다). **접혀도 한 줄은 남긴다** — 그 한 줄은 설명이 아니라 **지금 상태**(`대상 부서 11 / 39개 등록`)여야 매일 봐도 값이 있다.
· **같은 축을 두 컨트롤로 묻지 않는다** — 내 할일은 상태를 KPI 칩과 셀렉트로 **두 번** 물었다. 둘이 다른 값을 가리키면 어느 쪽이 적용된 건지 알 수 없다. 칩이 클릭 필터면 셀렉트는 지운다.
· **KPI 칩은 축이 겹치거나 남의 도메인이면 뺀다** — '오늘 마감'은 '이번주'의 부분집합이고 '결재 대기'는 담당자 처리 축이 아니다. 6칩 중 4개가 0인데 카드가 3장이면 칩이 데이터보다 많다.
· **상시 블록은 접고, 값이 있을 때만 펼친다** — 자동 발행 블록은 322px 를 늘 차지했다. 기본 접힘 + **방금 발행분이 있으면 자동 펼침**으로 바꿔 시연 첫인상(자동이 돈다)과 일상 사용성을 함께 지켰다. **펼친 상태에서도 둘을 동시에 보여주지 않는다** — 방금 나간 것이 있으면 예정은 한 줄로 접는다.

**목록은 담당자와 상태를 한 줄에 보여준다** — 카드 두 덩어리(배정 대기/완료)로 나누면 "누가 맡았고 어디까지 왔나"를 한눈에 못 본다. 표 한 장에 `업무 · 기한(D-day) · 담당자 · 상태`를 두고 행 전체를 타깃으로 삼는다(터치 44px 자연 확보). 상태는 색만으로 말하지 않고 **라벨을 함께** 쓴다(§7 색 단독 의미 금지). **완료된 건의 '미지정'은 강조하지 않는다** — 전용 화면에서 이미 처리된 경우라 손댈 게 없는데 주황으로 뜨면 부서장이 헛걸음한다.

**조직도 출처를 배정 화면에 밝힌다** — 자체 명단이 아니라 **연계 데이터**여야 한다. "왜 우리 팀원이 안 보이지"를 어디에 물을지 알 수 있어야 한다. 연계 관리(`admin-integration`)는 조직도를 **행정포털(SSO·조직도)** 소관으로 잡고 있는데 실무에서는 '온나라 조직도'라 부른다 — 같은 것인지 확인 대상이다. ⚠ 종전에 여기 적혀 있던 DYPOLICY `work-org-source` 도 **실재하지 않는 항목**이다(2026-08-13 확인). 업무문서 쪽은 `doc-dept-src`(새올·온나라 담당부서 연계)로 실제 항목을 만들어 두었다.
**자동 배정하지 않는다** — 가짜 배정은 안 보이고 미배정은 보인다. 대신 `DYROLE.leadOf(deptId)`(ORG `lead:true`, 정규식 금지)로 책임자를 지목하고, 미배정 지연은 제출 기한이 아니라 **배정 기한**(`assignDue`) 으로 따로 센다.
**팀 축은 데이터가 없다** — ORG 42명 중 '팀장' role 은 1명뿐이고 나머지는 `team` 문자열이다. `'team'` 자리만 확보하고 화면에 `팀 구조 미등록` 을 드러낸다. 저장은 `assign.to`(uid) 하나뿐 — 합성 팀 id 를 만들지 말 것.

**14-8. 자동발행은 결재를 올리지 않는다 (MUST)**
발행은 "할 일이 생겼다"까지다. 인스턴스에 **문서번호 필드를 두지 않는다** — 문서번호가 붙는 순간 공문이고, 공문 순서는 확정돼 있다(§4-3: `[공문 기안]` → 미리보기 → `[상신]`). 근거는 `EDUAPV.SUBMIT_ENABLED=false` 와 같다. 이미 결재 경로가 있는 도메인은 그 경로만 쓴다(교육 `EDUAPV` · 위험성평가 `DYRSKDOC`).

**14-9. 재촉은 자동 발송하지 않는다 (MUST)**
실측 촉구율 **1.8%**(3,608건 중 65건)이고 전부 사람이 보낸 2차 공문이다. 시스템은 `remindAdvice()` 로 **권고만** 하고 발송은 사람이 누른다. 발신자는 `DYROLE.actorLabel()` — `by:'재난안전과'` 하드코딩이면 담양읍장이 눌러도 재난안전과가 보낸 것으로 남는다. 재촉 버튼은 **미제출 부서에만**, 종결·취소 배치에는 0개다.
**배치 종결은 '전 부서 완료'가 아니다** — 5개년 실측상 전 부서 제출이 성립한 해가 한 번도 없다(2023 32/40 · 2026 25/40). 100% 게이트를 걸면 배치가 영원히 갇힌다. 종결 모달에 미제출 부서 수를 명시한다.

**14-10. 회수 경로를 함께 만든다 (MUST)**
`cancelIssue`·`unassign`·`reset` 을 제공하고 확인 모달에 **회수되는 부서·배정·제출·첨부 수를 명시**한다(§4 교육 CRUD 와 같은 근거). 저장 키는 `sessionStorage['dy-work-v1']` **하나**다 — 별도 키로 빼면 `reset()` 이 새는 곳이 생긴다.

**14-11. `DEPTCHK` key 에는 회차가 들어간다 (2026-08-10 v3)**
`rowKey(key, deptId)` 에 회차가 없어 상·하반기가 한 행을 공유했다 — 5월에 찍은 이행이 11월 업무를 발행 즉시 완료로 만들고 이듬해도 영구 완료였다. 회차는 **호출자가 key 에 실어 보낸다**. `dept-check.js` 자체는 무수정이고 `SKEY` 만 올린다. 새 소비처도 반드시 회차를 붙인다.
  - **회차 축은 '그 업무가 무엇을 기준으로 다시 하는가'로 정한다** — 시간 축이 항상 답은 아니다. 의무 이행점검은 반기마다 다시 하므로 `'comply-industrial|2026-H1'` 이고, **경영방침 게시는 방침이 바뀔 때 다시 붙이므로 `'policy-post|v2.0'`(방침 버전)** 이다(2026-08-13 v4). 연도로 묶었더니 방침을 개정해도 옛 게시 기록이 '게시함'으로 남아, 새 방침을 아직 안 붙인 부서가 완료로 보였다 — 상·하반기가 한 행을 공유했던 것과 같은 결함이다.

**14-12. 미확정 갭은 화면에 드러낸다**
부서 등록 갭(`11 / 39개 등록`) · 속성 미확인 · 표본 3~4건 기한 · 공휴일 미등록(주말만 판정) · 팀 구조 미등록 · 온나라 접수함 연동 미구현(`DOCUMENT_TRIGGERED` 는 시드 시뮬레이션). **그럴듯하게 채우지 않는다.**

> **미구현 갭은 그럴듯하게 채우지 말고 화면에 드러낸다.** 관계 법령 목록처럼 발주처가 확정할 대상은 "목록 미등록"으로 표시하고 무엇을 정해야 하는지만 안내한다. 빈 상태를 드러내야 결정이 넘어간다.

### 11. 시연 기준일 '오늘' — 단일 출처 (MUST)

프로토타입은 시드 날짜가 고정이라 **'오늘'도 고정해야** D-day·기한초과·이수 판정이 서로 맞는다. 기준일은 `js/common.js` 최상단 `DEMO_TODAY` **한 줄**이고, 전 모듈은 `DYV2.today()` / `DYV2.daysTo(iso)` 만 본다.

```js
/* js/common.js — 시연 당일에 이 한 줄만 바꾼다 */
const DEMO_TODAY = '2026-07-16';   // '' 로 두면 실제 시스템 날짜
```

**배경** — 종전에는 화면마다 '오늘'을 따로 박아 두어 **6곳이 3가지 값**이었다: `2026-07-16`(dashboard·my-work·edu-data) · `2026-07-14`(rsk-data `isOverdue`·sh-data) · `2026-07-02`(menu 의견청취). 그래서 대시보드 개선조치 집계와 내 할일 D-day 가 이틀 어긋나고, 작업환경측정 기준일만 다른 날을 가리켰다. 수렴 후 전부 `DEMO_TODAY` 파생이다.

- 새 모듈에서 `new Date()` 로 오늘을 만들지 말 것 — `DYV2.today()`(ISO 문자열) · `DYV2.daysTo(iso)`(일수 차) 를 쓴다. 실제 시각이 필요한 타임스탬프(`nowTs()`)만 예외다.
- `js/common.js` 보다 먼저 로드되는 파일은 없어야 한다(전 페이지 검증됨). 도메인 데이터 모듈(`rsk-data`·`edu-data`)은 `DYV2` 부재 시 실제 날짜로 떨어지는 폴백을 갖는다.
- 하위호환 상수 `DYEDU.TODAY` · `DYSH.TODAY` 는 로드 시점에 `DYV2.today()` 로 채워진다(소비처 8곳 무수정).

### 12. 조회 범위 — `DYROLE.scope()` 단일 출처 (MUST)

"무엇이 **보이는가**"는 `DYROLE.scope()` / `DYROLE.inScope(deptId)`(`js/layout.js`) 하나로 판정한다. 화면에서 `p.deptId` 로 직접 필터하지 않는다.

| 계층 | scope | 근거 |
|---|---|---|
| head (군수) | `'all'` | 총괄 책임자 |
| 재난안전과 (`DYROLE.OWNER_DEPT`) — 과장·주무관 | `'all'` | 주관부서는 전 부서를 봐야 총괄 업무가 된다 |
| 그 밖의 실과장·사업소장·읍면장·주무관 | 자기 `deptId` | 소속 부서만 |

**조작 권한(`canManage`/`canAct`/`canEdit`/`canReview`)과는 다른 축**이다 — 그쪽은 "무엇을 **바꿀 수 있는가"**다. 조회 범위가 `'all'` 이어도 조작은 담당자(staff)만인 경우가 정상이다(재난안전과장).

**배경** — 종전에는 4개 화면이 각자 달랐다. 물순환사업소 주무관이 재난안전과 개선조치를 열람하고 `＋ 개선조치 등록`까지 눌렀고, 환경과 주무관이 물순환사업소 재해 건에 `＋ 검토파일 등록`(안전관리자 서명)을 붙일 수 있었다. 적용처: `rsk-list`(부서별 조치 표) · `rsk-imp`(개선조치 대장) · `rsk-occ`(수시평가 목록) · `my-work`(부서 관점 전환).

**조작 권한은 `DYROLE.canAct(deptId)` 단일 출처 (MUST, 2026-08-04)** — 조회 범위와 **다른 축**이다. 관리·감독(head/super)은 조회만 하고, 담당자(staff)는 자기 부서 건만, 주관부서(재난안전과) 담당자는 전 부서. 화면마다 `tier`·`deptId` 로 직접 판정하지 말 것. 안내 문구도 `DYROLE.readOnlyNote(what)` 한 곳에서 낸다.

- **부서를 이름으로 저장하는 도메인은 `DYV2.deptIdOf(name)` 로 id 를 얻어 물어본다** (§3 dept/deptId 이원화) — 작업환경측정·건강검진이 그렇다.
- **적용 완료 (1단계)** — 작업환경측정·특수건강검진(조회 범위 + 조작) · 이행점검·경영방침(`DEPTCHK` 부서별 행 조작) · 도급관리·조직(`menu.js` 진입 함수) · 예산(`bgt.js`). 종전에는 **물순환사업소 주무관이 전 부서 건강검진 결과를 보고 계획 등록까지** 할 수 있었고, 군수·과장에게도 `확인 등록` 11개가 떴다.
- **아직 권한 축이 없는 화면 (2단계 — 발주처 정책 확정 후)** — 시설물 대장·위험도(80건 전건) · 관리대상 현황 · 예산 총괄표 조회 · 의견청취 · 인력 평가. 이들은 "전 부서 공개가 맞는가"가 정책 판단이라 임의로 좁히지 않았다. `base-targets`·`evl-eval` 은 전용 모듈 없이 HTML 인라인이라 손대는 방식도 다르다.
- **필터 드롭다운도 범위를 따른다** — 목록에서 지워 놓고 부서 `<select>` 에 남기면 "있는데 안 보여준다"로 읽힌다.
- **표에서 지운 행은 전역 호출로도 못 연다** — `openDept`·`detail`·`setDept` 에 `inScope` 가드를 함께 둔다. 렌더에서만 지우면 URL·콘솔로 뚫린다.
- **선택지가 1개면 `<select>` 를 쓰지 않는다** — 옵션 1개짜리 드롭다운은 "더 있는데 안 나온다"로 읽힌다(`my-work` 부서 전환).
- **빈 표와 안내가 같은 말을 두 번 하지 않게 한다** — 안내는 *왜*, 빈 상태는 *무엇이 생기면 채워지는지*를 맡는다.

---

### 15. 화면 어휘 — 제작 과정을 말하지 않는다 (MUST, 2026-08-19)

화면은 **완성된 시스템의 말로만** 한다. `검토 전 시연본`·`시연`·`프로토타입`·`데모`·`더미`는 사용자 어휘가 아니다. 근거·집행 기록: [`기획-시연문구-소거-도움말전환-v1.md`](docs/planning/기획-시연문구-소거-도움말전환-v1.md).

**다만 어휘를 지우는 것이 고백을 지우는 것은 아니다 (MUST)** — §14-12(미구현 갭을 드러낸다)와 §5(2026 실적은 예시 자료다)는 그대로다. 바꾼 것은 표현이지 사실이 아니다.

| 하지 말 것 | 대신 |
|---|---|
| `toast('…했습니다 (프로토타입)')` — 저장도 안 하고 저장했다고 말한다 | **`DYV2.notReady(무엇, 무엇이후)`** — `'문서 뷰어는 전자문서 연계 적용 후 제공됩니다'` |
| 예시 자료에 표기를 안 다는 것 | **`DYV2.sampleChip(사유)`** — 「예시 자료」 칩. **지우지 말 것**(지우면 실적으로 읽힌다) |
| `시연 시드`·`시연용`·`더미` | `예시 자료`·`예시 값`·`예시 명단` |
| 화면에 「시연 초기화」 버튼 | 도움말 시트 안 **「예시 데이터 되돌리기」**(§15-1) |
| 헤더 `검토 전 시연본` 칩 | 없앴다 — `PRE_REVIEW`·`preReviewInfo`·`.dy-prereview` 전부 제거됨. **되살리지 말 것** |

**미구현 토스트는 «새로고침하면 남나»로 가른다** — 남으면 괄호만 떼고, 이 화면에서만 남으면 범위를 밝히고(`목록에 추가했습니다 — 파일 저장은 … 연계 후`), 아무것도 안 남으면 **성공 문구를 쓰지 않는다**(`notReady`).

**화면정의서 진입 FAB 는 기본 숨김이다** — `?screendef=on` 으로 켜고 `off` 로 끈다(`localStorage('dy-screendef-show')`). 정의서 문서·뷰어(`screen-definitions.html`)는 그대로이며 앞으로도 갱신한다. **기본값을 다시 켜지 말 것.**

### 15-1. 도움말 — `DYHELP`(`js/help-center.js`) 단일 출처

"이 화면에서 무엇을 하는가"는 **한 자리에서만** 답한다. 화면마다 상시 안내 바를 붙이지 않는다(§14-12 — 첫 데이터까지의 거리).

- 진입점 3층 — ① 헤더 **[?]** 버튼(상시) ② **첫 방문 배너**(그 화면 최초 1회, `localStorage('dy-help-seen:<pageId>')`) ③ 제목 줄 `DYHELP.link()`.
- **매핑이 없는 화면에는 버튼을 내지 않는다** — 열어 봐야 "준비 중"이라고 말하는 버튼은 도달할 수 없는 수단이다. key 는 `DYLAW.pageId()` 환원값이고 `selfCheck()` 가 `DYLAW.MAP` 과 대조해 오타를 콘솔 경고한다(§13 선례).
- **법령은 저장하지 않는다** — `DYLAW.forPage()` 를 그 자리에서 읽는다(§10).
- **`자주 하는 일`의 바로가기는 이미 있는 진입점만 가리킨다** — 도움말 때문에 새 동작을 만들지 않는다. 실재하지 않는 전역을 적으면 눌러도 아무 일이 없는 죽은 버튼이 된다(집행 중 실제로 3건 고쳤다).
- **되돌리기는 도움말 시트 안에만 있다** — 모듈마다 진입 함수 이름이 다르므로(`resetDemo` 우선, 없으면 `confirmReset`) `resetOf()` 가 흡수한다. 권한 가드는 그 함수 안에 그대로 있다 — **버튼만 숨기면 전역 호출로 뚫린다**.

**`DYTOUR` 는 라벨만 바뀌었다 — 엔진·단계 정의·공개 API 12개는 무수정이다 (MUST)** — `시연 가이드 시작`→`단계별 안내 시작` · `시연 멘트`→`이 단계에서 하는 일` · `시연 포인트`→`입력 도움말` · `가이드 종료`→`안내 닫기`. 진입 바는 `insertBar()` 안에서 `DYHELP.seen()` 을 보고 **최초 1회만** 그리며 `dismissBar()` 가 닫는다. `.dy-demo-*`·`.dy-tour-*` **클래스명은 그대로다** — 개명하면 §7-1 계열 신설 사고를 다시 낸다.
